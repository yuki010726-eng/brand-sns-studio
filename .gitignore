# OS
.DS_Store
Thumbs.db
desktop.ini

# 편집기
.vscode/
.idea/

# 빌드 도구를 쓰지 않지만, 나중에 붙일 경우를 대비
node_modules/
dist/

# 비밀값은 저장소에 두지 않는다.
# API 키는 기본적으로 파일이 아니라 브라우저 localStorage 에만 저장된다 (README 참고).
# lib/apikeys.local.js 는 그 예외 — 로컬에서만 쓰는 기본 키 파일이라 반드시 커밋에서 뺀다.
.env
.env.*
*.key
secrets.json
lib/apikeys.local.js
