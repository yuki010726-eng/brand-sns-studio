# OS
.DS_Store
Thumbs.db
desktop.ini

# 편집기
.vscode/
.idea/

# 빌드 도구를 쓰지 않지만, 나중에 붙일 경우를 대비
node_modules/
dist/

# 배포 (Vercel)
.vercel/

# 비밀값은 저장소에 두지 않는다.
# 배포본의 API 키는 Vercel 환경 변수에만 있다. 로컬 실행에서는 브라우저 localStorage 에만 있다.
.env
.env.*
*.key
secrets.json
