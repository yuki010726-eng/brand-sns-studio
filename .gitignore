# OS
.DS_Store
Thumbs.db
desktop.ini

# 편집기
.vscode/
.idea/

# 빌드 도구를 쓰지 않지만, 나중에 붙일 경우를 대비
node_modules/
dist/

# 비밀값은 저장소에 두지 않는다.
# OpenAI API 키는 파일이 아니라 브라우저 localStorage 에만 저장된다 (README 참고).
.env
.env.*
*.key
secrets.json
