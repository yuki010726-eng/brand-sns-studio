# CLAUDE.md — 브랜드 SNS 스튜디오

Claude Code가 세션 시작 시 자동으로 읽는 프로젝트 지침이다.
**다음 작업은 STEP 5다. 아래 「다음 작업」 절부터 보면 된다.**

---

## 1. 이 프로젝트가 뭔가

브랜드 4개 상품의 SNS 게시물을 만드는 도구. 흐름은 4단계다.

```
1단계 프로필 세팅 → 2단계 상품·주제 선택 → 3단계 아이디어 문서화 → 4단계 카드뉴스 템플릿(이미지 포함)
```

**1단계 프로필 세팅은 건너뛸 수 있다.** 게시물마다 하는 일이 아니라 계정을 한 번 잡는 단계다.
`reachedStep()` 이 최소 2를 돌려주는 이유다 — 1을 돌려주면 프로필을 만들기 전에는 상품 선택으로 못 넘어간다.

**이미지 제작은 원래 별도 단계(`pages/image.js`)였다가 3단계 안으로 합쳤다.**
이미지가 필수가 아닌데 단계로 세워 두니 흐름을 막는 것처럼 보였기 때문이다.
지금은 문구 입력칸 반대쪽(미리보기 아래)에 이미지 패널로 붙어 있고, 원할 때만 쓰면 된다.

상품 4종: KBS N 브랜드어워즈 / 포브스 브랜드어워즈 / KCST 대한민국 고객만족도 신뢰도 대상 / 중소기업 AI TV CF

| 단계 | 파일 | 상태 |
|---|---|---|
| 1 | `pages/profile.js` + `lib/profile.js` | ✅ 완료 (8-5 참고) |
| 2 | `pages/home.js` | ✅ 완료 |
| 3 | `pages/copy.js` + `lib/copywriter.js` | ✅ 완료 (블로그 형식은 8-3 참고) |
| 4 | `pages/template.js` + `lib/templates.js` `lib/cardrender.js` `lib/concepts.js` | ✅ 완료 |
| 4의 이미지 | `components/imagepanel.js` + `lib/imageprompt.js` `lib/imagestore.js` | ✅ 완료 (프롬프트만 표시, API 생성 없음 — 2026-08-11) |
| 보관함 | `pages/library.js` | ⬜ **자리표시자 — 다음 작업** (검색·필터·정렬) |
| 로그인·동기화 | `lib/supabase.js` `lib/auth.js` `lib/sync.js` | ✅ 코드 완료 · **설정 대기** (11 참고) |

---

## 2. 실행

빌드 도구를 쓰지 않는다. ES 모듈을 쓰므로 `file://`로 열면 동작하지 않는다. 반드시 서버로 띄운다.

```bash
python -m http.server 5610
```

`.claude/launch.json`에 등록돼 있으므로 `preview_start`로 실행하면 된다.
`python`이 없고 `py`만 있으면 `launch.json`의 `runtimeExecutable`을 `py`로 바꾼다.
`serve.cmd`를 더블클릭해도 된다 (python → py → python3 순으로 시도).

### 캐시 주의

`python -m http.server`는 캐시 헤더를 붙이지 않아, 코드를 고쳐도 브라우저가 옛 파일을 쓴다.
브라우저에서 검증하기 전에 반드시 캐시를 비운다.

```js
Promise.all([...document.styleSheets].filter(s=>s.href).map(s=>fetch(s.href,{cache:'reload'})))
  .then(()=>location.reload())
```

JS 모듈까지 바꿨다면 해당 파일도 `fetch(path,{cache:'reload'})` 한 뒤 reload 한다.
이걸 빼먹으면 "고쳤는데 반영이 안 된다"고 잘못 판단하게 된다.

---

## 3. 절대 임의로 바꾸지 말 것

### 디자인 토큰 (`styles/tokens.css`)

```
--primary #3182F6 / --primary-dark #1B64DA / --text #4E5968 / --sub #8B95A1
--bg #F2F4F6 / --surface #FFFFFF / --border #E5E8EB
```

버튼은 둥근 알약: `border-radius:999px; padding:12px 22px; font-weight:700`
폰트는 Noto Sans KR (400/500/700). 아이콘은 라인, 24x24, `stroke-width:1.5`, `fill:none`.

### 접근성 (요청자 지정 규칙 — 디자인 값과 충돌하면 이쪽이 우선)

- 모든 버튼에 `aria-label`, 장식 아이콘은 `aria-hidden="true"`
- **명도 대비 4.5:1 이상**
  - `--sub`(#8B95A1)는 흰 배경 3.08:1이라 본문 텍스트에 쓰지 않는다. 아이콘·구분선·비활성만.
  - 작은 보조 텍스트는 `--sub-strong`(#5F6B7A) — 흰 배경 5.43:1 / 회색 배경 4.92:1
  - CTA 배경은 `--cta-bg`(=`--primary-dark`). `#3182F6`+흰 글자는 3.71:1로 미달이라
    **요청자 승인을 받아** 변경했다. 되돌리려면 `--cta-bg` 한 줄만 바꾸면 된다.
  - 카드뉴스 **카드형의 파랑**도 같은 이유로 낮췄다. 레퍼런스 `#3B93F7` 은 흰 글자 3.13:1 이라
    큰 글씨 기준(3:1)은 겨우 통과하지만 여유가 없다 → `#2673D2` (4.7:1).
    `lib/concepts.js` 의 `card.layout` 두 줄만 바꾸면 레퍼런스 색으로 되돌아간다.
- `:focus-visible` 3px 아웃라인. 시각적으로 숨긴 radio/checkbox도 label에 포커스링을 전달한다.
- 이미지 `alt` 필수. canvas는 `role="img"` + `aria-label`.

### 폼 컨트롤에 `autocomplete="off"`

새로고침 시 브라우저가 예전 선택을 복원하며 `change`를 발생시켜 **저장된 상태를 덮어쓴다.**
실제로 컨셉 선택이 초기화되는 버그가 있었다. 라디오·체크박스·textarea에 반드시 붙인다.

---

## 4. 콘텐츠 사실성 원칙 (위반 시 재작업)

상품 정보 출처는 `07_BRAND_INFORMATION.md`(기준일 2026-07-23)이며 `data/products.js`에 옮겨져 있다.

- 매출 상승·광고 성과를 **보장하지 않는다**
- 공식 근거 없이 **최고·유일·1위·최고 권위** 단정 금지
- **종료된 행사**(`events[].status !== 'open'`)를 모집 중처럼 쓰지 않는다
- 마무리 문장은 반드시 `product.closings` 배열에서만 가져온다 (승인된 표현)
- KCST 심사 기준 비율 합계 100%를 정확히 유지한다
- AI TV CF의 '300만 원' 관련 표현은 근거 확인 전까지 쓰지 않는다
- 생성 결과는 `BANNED_PHRASES` 검사를 통과해야 한다 (`lib/copywriter.js`의 `findBanned`)

각 상품의 `cautions` 배열은 화면에 항상 노출한다.

---

## 5. 작업 규칙

1. **단계별 진행 + 검수.** 한 번에 다 만들지 말고, 한 단계 끝나면 요청자 검수를 받고 다음으로 간다.
2. **배치가 애매하면 임의로 정하지 말고 질문한다.**
3. **백엔드·API·배포가 필요한 항목은 먼저 물어본다.** 지금은 PART 1(프론트엔드)이다.
4. **검증 없이 "완료"라고 하지 않는다.** 브라우저로 실제 확인하고, 못 한 검증은 못 했다고 말한다.
5. 기존 구조·네이밍을 따르고, 요청 범위 밖 리팩토링은 하지 않는다.

---

## 6. 지금까지 내린 결정과 이유

이 맥락을 모르면 되돌리기 쉬운 것들이다.

| 결정 | 이유 |
|---|---|
| 빌드 도구 없는 정적 구조 | 작업 PC에 node/npm이 없다. Vite를 쓰면 실행·검증이 불가능하다. |
| 카드뉴스 **1080×1350 (4:5)** | 레퍼런스 세 계정이 모두 세로형이고, 인스타 피드에서 화면을 더 크게 차지한다. **원래는 1080×1080이었다** — 블로그 본문 재사용 때문이었는데, 레퍼런스 반영을 우선해 2026-07-28에 요청자 승인을 받아 바꿨다. 되돌리려면 `lib/cardrender.js` 의 `H` 한 줄이다. |
| 채널별 화법 완전 분리 | 처음엔 데이터를 불릿으로 덤프해서 "AI로 뽑은 것 같다"는 지적을 받았다. 지금은 `product.voice`의 문장 재료를 조합한다. |
| 쓰레드만 별도 화법(`voice.threads`) | 공지 톤이 아니라 "알게 된 걸 흘리는" 톤이 필요하다. **해시태그·계정·CTA를 넣지 않는다** — 하나라도 붙으면 광고 티가 나서 톤이 무너진다. |
| 블로그는 Q&A 구조 | 검색 유입용. 질문과 답을 `voice.qa`에 **쌍으로** 묶었다. 따로 뽑으면 소제목과 본문이 어긋난다. |
| 카드 장면(`shot`)도 qa 항목에 동봉 | 주제에 따라 순서가 바뀌어도 그림과 글이 어긋나지 않게. |
| 이미지 프롬프트는 **영문** | 이미지 모델이 영문 지시를 훨씬 정확히 따른다. |
| 프롬프트에 `no text, no letters` | 문구는 4단계에서 얹어야 수정 가능하고, 생성 이미지의 한글은 깨진다. |
| 이미지는 IndexedDB | localStorage(약 5MB)에 1024px PNG 6장이 안 들어간다. |
| 4단계에서 배경 이미지를 **선택 사항**으로 | API 비용 때문에 이미지 생성을 아직 못 쓴다. 이미지가 없어도 카드뉴스를 완성할 수 있어야 한다. 그래서 `reachedStep()`도 이미지가 아니라 **글귀**가 있으면 4단계를 연다. |
| 이미지 제작을 **별도 단계에서 빼서 템플릿 안으로** | 이미지는 선택 사항인데 단계로 세워 두니 반드시 거쳐야 하는 것처럼 보였다. 문구와 이미지를 오가며 보는 것도 번거로웠다. 없어진 `#/image` 경로는 `app.js`의 `MOVED`가 템플릿으로 넘긴다. |
| 블로그 글에 **이미지 자리 표시**(`📷 [이미지 N · 역할]`) | "생성된 이미지가 어디에 들어가는지" 알 수 없다는 지적. 카드 6장이 글의 어느 위치에 대응하는지 한 줄로 박아 둔다. 붙여넣고 지우기도 쉽다. |
| 인스타는 **질문(?)과 사실(습니다)을 번갈아** 배치 | 전부 '~습니다'로 끝나 리듬이 없고 읽히지 않는다는 지적. 사실 문장 자체는 못 고치므로(사실성 원칙) 사이에 다른 종결을 끼워 리듬을 만든다. |
| 쓰레드 맨 앞에 **후킹 한 줄**, 맨 뒤에 **질문** | "왜 읽어야 하는지 모르겠다"는 지적. 후킹 문장은 사실 주장이 아니라 화자의 경험·태도라서 사실성 원칙에 걸리지 않는다. |
| 카드 6장을 한 화면에 늘어놓지 않고 **탭으로 한 장씩** | 슬롯이 카드당 4개(라벨·제목·본문·핸들)라 6장을 동시에 펼치면 미리보기와 입력칸의 대응이 무너진다. 요청자가 제시한 레퍼런스 UI도 한 장씩 편집하는 구조였다. |
| OpenAI를 브라우저에서 직접 호출 | 백엔드가 없다. **요청자가 개인·내부용으로 쓰기로 합의한 방식이다.** |

---

## 7. API 키 취급 (중요)

AI 제공자는 **OpenAI 하나만** 쓴다(요청자 결정, 2026-08-11). 전에는 Gemini/OpenAI 둘 다
지원하는 제공자 선택 레이어(`lib/imagegen.js`, `lib/llm.js`의 provider 분기)가 있었는데
Gemini 를 완전히 뺐다 — `lib/gemini.js` · `lib/imagegen.js` 는 삭제됐다.
텍스트 생성은 `lib/openai.js` 하나가 담당하고, `lib/llm.js`는 그걸 그대로 다시 내보내는
얇은 통로다. **이미지는 API로 만들지 않는다** — 8절 "이미지 패널" 참고.

- OpenAI 텍스트 모델은 **무료 한도가 없다.** 게시물 1세트에 약 $0.03(모델에 따라 다름).
- 키는 화면(3단계 AI 설정)에서 요청자가 직접 입력하고 `localStorage`(`bboggl.openai-key`)에 저장된다.
- **키를 코드·저장소·문서에 절대 넣지 않는다.** 대화로 받지도 않는다.
  > 예외가 두 가지다.
  > 1. **Supabase `anon public` 키**는 Supabase 가 클라이언트에 심으라고 만든
  >    공개 키라서 `lib/supabase.js` 에 둔다. 실제 보호는 테이블의 RLS 가 한다.
  >    ⚠️ **`service_role` 키는 진짜 비밀키다.** 절대 파일에 넣지 말 것 — 모든 권한을 가진다.
  > 2. **`lib/apikeys.local.js`** — `localStorage`에 값이 없을 때 대체값으로 쓰는 로컬 전용
  >    파일이다. `.gitignore`에 있어 **절대 커밋되지 않는다.** 화면에 매번 입력하지 않고
  >    기기에 한 번 채워 두려는 용도(요청자 결정, 2026-08-11). 기기를 옮기면 이 파일은
  >    git으로 안 따라가니 새 기기에서는 다시 채워야 한다(11절 동기화 정책과 같은 맥락).
  >    파일이 없어도(신규 clone) `lib/openai.js`가 동적 import 실패를 잡아 빈 값으로 넘어가
  >    앱이 깨지지 않는다.
- 이 저장소는 Private이지만 그것과 무관하게 키는 파일에 두지 않는다.
- 커밋 전 `git grep -nI "sk-[A-Za-z0-9]\{16,\}"`로 확인하는 습관을 유지한다.

> ⚠️ 브라우저 직접 호출은 키가 노출되므로 **로컬·내부용 전용**이다.
> 외부 배포 시에는 서버 프록시로 옮기고 키를 서버에만 둔다 → PART 2.

---

## 8. 완료된 STEP 4 — 카드뉴스 템플릿 (구조 메모)

`pages/template.js` + `lib/templates.js` + `lib/cardrender.js`. 손댈 때 알아야 할 것만 적는다.

### 화면 구조

**템플릿 3종 선택 → 카드 탭(01~06) → 2열 편집.**
왼쪽은 미리보기 canvas + 저장 버튼 + **이미지 패널**, 오른쪽은 슬롯별 입력칸이다.
이미지 패널을 문구 입력칸 **반대쪽**에 둔 건 요청자 지정이다 — 글을 고치면서 그림을 같이 보게 한다.
입력칸 라벨에 '카드 어디에 들어가는 글인지'를 반드시 함께 적는다
(계정 이름(상단 중앙) / 대주제(파란 박스) / 부제(중앙, 회색) …).
미리보기와 입력칸을 눈으로 잇는 유일한 단서라 라벨에서 위치 표현을 빼지 말 것.

시스템이 `buildDeck()` 결과로 문구를 미리 채우고, 사용자는 칸마다 고친다.
요청자가 명시한 요구사항이다 — "추천 글을 먼저 작성해주고 수정할 수 있도록".

### 템플릿 3종 (레퍼런스: 바탕화면 `브랜드 sns 스타일 레퍼런스 자료/`)

| 템플릿 | 레퍼런스 | 장별 배치 |
|---|---|---|
| `magazine` 매거진형 | ai.brief.kr | **모든 장 동일.** 상단 중앙 계정명 · 하단 검정→투명 그라데이션 · 카테고리 pill · 흰 제목 + **형광색 강조줄** · @아이디 |
| `card` 카드형 | soosangmarket | **표지** 배경 사진 + 하단 테마색 그라데이션 + 테두리 pill + **아주 큰 흰 제목** / **본문** 상단 테마색 알약(제목) + 하단 흰 박스(본문·출처) / **마무리** 테마색 단색 + 중앙 팔로우 문구 (이미지 안 씀) |
| `note` 노트형 | sslmo.lab | **표지** 종이 배경 + 중앙 제목·부제 + 일러스트 / **본문** 좌상단 실험실 심볼 + 테두리 알약 라벨 + **왼쪽 정렬** 아주 굵은 검은 제목 + 회색 본문 + 검정 박스·번호 박스 |

> ⚠️ **카드형 표지와 본문은 배치가 다르다.** 한 번 같은 배치로 합쳤다가 되돌렸다(2026-08-03).
> 요청자 지적: "카드형 표지는 기존 작업이 맞다." 레퍼런스 표지 3장이 전부 하단 큰 제목 구조다.

### ⚠️ 글자를 박스에 얹을 때는 `centerLift()` 를 쓴다

`draw()`·`drawRich()` 는 줄마다 **먼저 행간만큼 내려간 뒤** 그린다. 그래서 첫 줄 위에 행간이
통째로 여백으로 남고 마지막 줄 아래는 거의 붙는다. 그대로 두면 글이 박스 중앙보다 아래로 쏠린다.
요청자가 **두 번** 지적했다(흰 박스 → 고침, 그다음 알약 제목 → 또 지적).
새로 박스 안에 글을 얹을 때는 반드시 `y - centerLift(size, lh)` 로 그린다.

### 카드형 테마 색 · 노트형 종이

- **`state.cardTheme`** 한 곳이 카드형 전 장의 색을 정한다. 카드마다 두지 말 것 —
  요청자 요구가 "한번 바꾸면 나머지 페이지도 다 바뀌도록" 이다.
  후보 6색 전부 흰 글씨 대비 4.5:1 이상이다. **노랑은 밝은 노랑이 아니라 짙은 호박색**이다(밝으면 미달).
- **`state.notePaper` / `state.noteGrain`** — 종이 색 3종(흰색·회색·블루 그레이)과 결 4단계.
  기존 `#F2F2F0` 은 누런 계열이라 뺐다. 결의 점 위치는 소수 곱셈으로 만들어 **매번 같은 무늬**가 나온다
  (렌더가 결정적이어야 한다 — 검증에서 두 번 그려 0px 차이를 확인한다).
- **`state.noteSymbol`** — 실험실 무드 심볼 9종. 레퍼런스 로고가 실험실 아이콘이라 같은 결로 만들었다.
  **실제 로고 이미지가 있으면 그쪽이 우선**이고 심볼은 그려지지 않는다.

### 카드 장수는 1~6장

2단계에서 고른다(`state.cardCount`). API 비용을 줄이려는 요청자 요구다.
`DECK_PLAN` 이 장수별 구성을 정하고, **장수를 줄여도 글은 기승전결을 그대로 쓴다** — 줄어드는 건 이미지뿐이다.
1장이면 표지가 마무리 문장까지 안고 간다.

### 제목은 900(Black)이다

레퍼런스의 제목은 700으로 그리면 눈에 띄게 얇다. `styles/tokens.css` 의 폰트 import 에
900 을 넣어 두었고 `ensureFonts()` 가 900 을 먼저 로드한다. **둘 중 하나라도 빠지면
조용히 700 으로 그려진다** — 굵기를 바꿀 때 두 곳을 같이 본다.

### 슬롯 정의는 `lib/templates.js` 한곳에

템플릿마다, 그리고 표지/본문/마무리마다 들어가는 글의 자리가 다르다.
`roleOf()` 가 kind → role 을 정하고, `slotsFor()` 가 입력칸을, `defaultsFor()` 가 초기값을 준다.
**폼과 렌더러가 같은 정의를 보게 해야 한다** — 한쪽만 고치면 조용히 어긋난다.

매거진형 기본 문구는 `headline()` → `splitHeadline()` 을 거친다.
카드 문구를 그대로 넣으면 줄이 늘어 글자가 작아지고 행간이 무너진다("글이 넘치는 느낌").
**핵심 한 문장만** 쓰고 자연스러운 어절 경계에서 두 줄로 나눈다 — 뒷줄이 형광색이다.
어절 중간에서는 자르지 않는다. 마무리 장은 승인된 문장이라 자르지도 쪼개지도 않는다.

### 렌더러 (`lib/cardrender.js`)

`renderCard(canvas, texts, { conceptId, kind, image, accent })` 한 곳이 진입점이다.

- **이미지는 선택 사항이다.** 없으면 `fallbackBg()` 가 템플릿별 배경을 그린다.
  이 덕분에 OpenAI 키 없이도 4단계를 끝까지 쓸 수 있다.
- 노트형에서 이미지는 **배경이 아니라 아이콘**이다 (`drawContain`). 나머지는 배경(`drawCover`).
- `fit()` 은 `weight`·`lh` 를 결과에 담아 돌려준다. 여러 블록을 **먼저 재고 나중에 그리는**
  바닥 정렬 배치라 `draw()` 가 자기 폰트를 스스로 복구해야 하기 때문이다.
- `fitPair()` 는 매거진형 전용 — 흰 제목과 형광 강조줄은 한 덩어리로 읽혀야 해서
  **크기가 달라지면 안 된다.** 따로 `fit()` 하면 안 된다.
- 노트형 본문은 서식을 지원한다: `**강조**` → 굵고 진하게, 줄 앞 `1.` `2.` → 번호 박스, 이모지 그대로.
  `parseBody` → `fitRich` → `drawRich` 순서다.

### 문구 상태 — 낡는 이유가 두 가지다 (자주 틀리는 곳)

`state.card = { key, concept, texts[], base[] }`

| 무엇이 바뀌었나 | 어떻게 다루나 |
|---|---|
| 상품·주제·톤 (`key`) | 내용만 낡는다. **편집본을 지키고 안내만** 띄운다. |
| 템플릿 (`concept`) | **슬롯 구성 자체가 달라진다.** 반드시 다시 세운다 — `mergeTexts()` 가 직접 고친 값만 살리고 나머지는 새 기본값으로 채운다. |

둘을 한 지문으로 합치면 안 된다. 실제로 그렇게 만들었다가, 템플릿을 바꿔도 이전 슬롯이
그대로 남아 **새 템플릿에 없는 칸이 빈 채로 보이는** 버그가 났다.

### 이미지 패널 (`components/imagepanel.js`)

**AI가 여기서 이미지를 직접 만들지 않는다** (요청자 결정, 2026-08-11). 전에는 "AI로 생성"·
"전체 생성" 버튼이 Gemini/OpenAI 이미지 API를 브라우저에서 바로 불렀는데, 그 버튼과 관련
키 설정 UI를 뺐다. 지금은 한 카드 기준으로 **프롬프트 보기/복사 · 파일 올리기 · 지우기**만 담는다.
프롬프트를 복사해 원하는 이미지 도구에서 만든 뒤 「파일 올리기」로 넣는 흐름이다.

- 카드형 마무리 장은 단색 배경 고정이라 패널이 안내문만 남기고 비활성된다(`usesImage()`).
- 이미지가 바뀌면 `applyImage()` 한 곳을 통해 IndexedDB·상태·비트맵·미리보기·안내문을 함께 맞춘다.
  개별로 갱신하면 어느 하나가 빠져서 화면이 어긋난다.
- `lib/gemini.js` · `lib/imagegen.js` 는 삭제됐다. `lib/openai.js`의 이미지 생성 코드(`generateImage`)도
  같이 뺐다 — 아무 데서도 부르지 않는 죽은 코드였다. 다시 API 생성 버튼을 붙이려면 git 이력에서
  복구하거나(`git log -- lib/gemini.js lib/imagegen.js`) 새로 짜야 한다.

### 선택 요소

- **강조 색상 (매거진형)** — `ACCENTS` 6종 + hex 직접 입력. `state.accent`.
  후보 색은 전부 하단 스크림(최악 #1A1A1A) 기준 4.5:1 을 넘긴다 — 색을 추가하면 다시 계산할 것.
- **우상단 마크 (카드형)** — `MARKS` 10종. `state.mark`.
  `draw` 는 캔버스 도형(브랜드 색을 따른다), `glyph` 는 시스템 이모지 폰트로 찍는다.
- **본문 서식 넣기 (노트형)** — 폼의 칩 버튼이 커서 자리에 `**강조**` · `> ` · `1. ` · 이모지를 꽂는다.

### 잊지 말 것

- 1080×1350 고정. 전체 저장은 브라우저가 연속 다운로드를 막으므로 350ms 간격을 둔다.
- 입력 중에는 캔버스만 다시 그린다(120ms 디바운스). 폼을 다시 그리면 캐럿이 튄다.
- 이미지 업로드는 3단계와 **같은 키**(`imageKey`)를 쓴다. 두 화면이 같은 이미지를 본다.
- 카드형 파랑은 레퍼런스(#3B93F7)보다 한 단계 낮은 **#2673D2** 다. 이유는 아래 접근성 절 참고.

---

## 8-2. AI 글귀 생성 (`lib/copyai.js`)

2단계에서 「AI로 쓰기」를 누르면 규칙 기반 글 대신 LLM 이 쓴 글로 바꾼다.
규칙 기반 생성은 그대로 남아 있다 — **키가 없어도 앱은 완전히 동작한다.**

**OpenAI 텍스트 모델에는 무료 한도가 없다.** 게시물 1세트에 약 $0.03(모델에 따라 다름).

### ⚠️ 주제가 중심이다 — 프롬프트 순서를 바꾸지 말 것

처음엔 상품 자료를 먼저 넣고 주제를 한 줄로 덧붙였다. 그랬더니 모델이 **주제를 제쳐두고
자료를 순서대로 요약**했다. 요청자 지적: "기존 내용만 가져오면 어떡하냐."

지금은 이렇게 한다. 셋 다 필요하다.

1. **주제를 맨 앞에 세운다.** 자료는 그 뒤에 '재료'로 넣는다.
2. 자료 항목 이름에 `[참고 재료]`, `문장을 그대로 베끼지 말 것`을 박아 둔다.
3. 프롬프트 **맨 끝에서 주제를 다시 한 번** 못박는다.

그리고 `offTopic()` 이 주제 낱말이 글에 충분히 나오는지 확인한다(기준 35%). 안 나오면 재시도한다.

같은 상품에 주제만 바꿔 두 번 돌려 **소제목이 실제로 달라지는지** 확인할 것.
(예: '슈퍼자막 패키지가 뭔가요' vs '수상 이력을 홍보에 활용하는 법')

### AI는 버튼을 눌러야 쓴다 (2026-08-11, 요청자 지시로 변경)

2026-08-03엔 "키가 있으면 3단계에 들어오자마자 자동으로 AI가 세 채널을 다시 썼다.
**지금은 자동 호출을 없앴다** — 3단계에 들어오면 2단계에서 고른 상품·주제·톤(ctxbar)과
규칙 기반 초안만 보여주고, 「전체 다시 쓰기 (AI)」 버튼을 직접 눌러야 그때 AI를 부른다.
「전체 재생성」과 「전체 AI로 쓰기」는 버튼 하나로 합쳐져 있다 — 키가 있으면 AI, 없으면 규칙 기반이다.

- 편집본이 있으면 자동으로 덮어쓰지 않는다(버튼을 눌러도 확인을 한 번 더 받는다).
- **규칙 기반 생성기를 없애면 안 된다.** 키가 없거나 한도를 넘겼을 때 앱이 멈춘다.

### 이 파일의 본체는 생성이 아니라 검증이다

규칙 기반은 승인된 문장만 조합해서 사실 위반이 **구조적으로 불가능**했다.
LLM 은 글은 좋아지지만 없는 사실을 지어낼 수 있다. 그래서 게이트를 통과한 글만 화면에 내보낸다.

```
프롬프트(상품 사실 통째로 + "이 안에서만") → 생성 → repairDraft() → validateDraft()
   └ 통과 못 하면 무엇이 걸렸는지 알려주고 재시도(최대 3회) → 그래도 실패하면 던짐
     → 호출한 쪽이 기존 글을 그대로 둔다 (나쁜 글로 덮어쓰지 않는다)
```

### ⚠️ 형식은 반려하지 않고 고친다 — `repairDraft()`

요청자 지시(2026-08-03): **게이트를 느슨하게 풀어 통과율을 올린다.**
이미지 자리 한 줄이 빠졌다고 멀쩡한 글 2,000자를 버리는 건 낭비였고,
재시도해도 같은 이유로 또 막히는 일이 잦았다.

| | 처리 |
|---|---|
| 목차 · 이미지 자리 · 캡션 · 길이 초과 · 쓰레드 해시태그 | `repairDraft()` 가 **고친다** |
| 금지 표현 · 근거 없는 단정 · 주제 이탈 · 너무 짧음 | `validateDraft()` 가 **반려한다** |

- **형식 검사를 `validateDraft()` 에 다시 넣지 말 것.** 통과율만 떨어지고 글은 나아지지 않는다.
- `repairDraft()` 는 **형식만** 만진다. 사실 관계를 손대면 안 된다.
- 빠진 이미지 자리는 **글 끝에** 순서대로 채운다. 본문 사이 배치가 더 좋지만 AI 글의 문단 경계를
  기계가 정확히 알 수 없어 엉뚱한 자리에 박히면 더 나쁘다. 서대문구 레퍼런스가 카드뉴스를
  글 끝에 통째로 붙인 방식과 같다.
- 짝 없는 `⤷` 캡션은 버린다. 안 그러면 캡션 수와 이미지 수가 어긋나 인스타 카드와 1:1로 못 맞춘다.
- **보정을 두 번 돌려도 결과가 같아야 한다**(idempotent). 검증 항목에 넣어 뒀다.

마지막 실행 기준: 상품 4 × 톤 4 × 망가뜨린 형태 5종 = 80조합 보정 실패 0건.
실제 Gemini 호출로 세 채널 전부 통과 확인(2,285 / 800 / 458자, 8초).

### ⚠️ `BANNED_PHRASES` 만으로는 부족하다 (실제로 뚫렸다)

`data/products.js` 의 `BANNED_PHRASES` 는 **정확히 일치하는 문구 5개**뿐이다.
조합만 하던 시절엔 충분했지만 LLM 상대로는 "업계 1위", "최고 권위", "매출이 보장됩니다"가
전부 그냥 통과했다. 그래서 어형 변화를 잡는 정규식 `RISKY` 를 따로 뒀다.

두 번 헛디뎠으니 규칙을 건드릴 때 반드시 확인할 것:

1. **부정문을 주장으로 잡으면 안 된다.** 승인된 반론이 "매출이 오르지는 **않습니다**" 라서
   정상 글 336건이 막혔다 → `NOT_NEGATED` 로 뒤따르는 부정어를 확인한다.
2. **상품 데이터의 고유명사를 막으면 안 된다.** 포브스 행사 이름이 실제로
   "고객신뢰도 **1위** 프리미엄 브랜드 대상" 이라 96건이 막혔다 →
   `safePhrasesOf()` 로 승인된 원문을 지운 뒤 남은 부분만 검사한다.

규칙을 고쳤으면 **기존 규칙 기반 글 576조합에 오탐 0건**인지 반드시 다시 확인한다.

---

## 8-3. 블로그 형식 — 네이버 레퍼런스 반영 (2026-08-03)

요청자가 네이버 블로그 4편을 레퍼런스로 줬다. 저장본을 파싱해 구조를 측정하고 옮겼다.

> ### ⚠️ 레퍼런스에서 가져오는 것은 **레이아웃 · 화면 구성 · 이미지 배치**다. **문체가 아니다.**
>
> 한 번 레퍼런스의 '~다' 평서형까지 옮겼다가 같은 날 되돌렸다. 상품을 **소개하는** 글인데
> 말끝마다 '~다'로 끊으니 읽는 사람에게 설명하는 느낌이 사라졌다. 요청자 지적 그대로다.
> 문체는 **읽는 사람에게 설명하듯 하는 존댓말**이다. `voice` 재료를 원문 그대로 쓰면 된다.
>
> 문체 검사도 두지 않는다. 뒀더니 정상 문장을 계속 잡아 AI 글이 매번 반려됐다
> (따옴표 안 인용문 `"대기업만 되는 거 아니에요?"`, '아니다'의 '니다').
> `SYSTEM` 프롬프트의 "존댓말"과 채널 규칙의 "평서형"이 충돌해 모델이 무한 반려되기도 했다.
> **문체를 강제하는 장치를 다시 만들지 말 것.**

| 레퍼런스 | 성격 |
|---|---|
| 로라의 행복한상상(653864) 3편 — 김부장 / 피스마이너스원 / 유미의세포들 | **3편이 오차 없이 같은 골격.** 이게 기준이다. |
| 서대문구(sdmstory) 1편 | 관공서형. 본문 463자 + **끝에 카드뉴스 8장을 통째로 첨부**. |

### 실측값 (레이아웃 목표치)

| | 김부장 | 피스마이너스원 | 유미의세포들 |
|---|---|---|---|
| 본문 글자수 | 2,011 | 2,343 | 2,077 |
| 줄당 평균 | 27자 | 29자 | 27자 |
| 30자 이하 짧은 줄 | 70% | 59% | 71% |
| 소제목 | 6개 | 6개 | 6개 |
| 본문 이미지 | 4장 | 1장 | 5장 |

### 확정된 형식

```
제목 두 줄(검색어 / 후킹) → ─── → 인용구 요약(> 3줄) → 도입
→ 📷1+캡션 → ## 소제목×4 (앞 3개 뒤에 📷2~4+캡션)
→ ## 그래도 망설여진다면 → 📷5+캡션 → ## 정리하면 → 📷6+캡션
→ 🔔 개요표 → 해시태그
```

- **목차를 넣지 않는다.** 레퍼런스 3편 모두 없다. 예전 구조에는 있었다 — 되살리지 말 것.
- **한 문장 = 한 줄, 문장 사이에 빈 줄** (`breathe()`). 문단을 덩어리로 붙이면 네이버에서 벽처럼 보인다.
  긴 나열 문장은 쉼표에서 한 번 더 쪼갠다(이때는 빈 줄 없이).
  이건 문체가 아니라 **화면에 보이는 모양**이라 유지한다.
- **📷 줄 바로 아래 `⤷ ` 캡션 한 줄이 붙는다.** 레퍼런스가 이미지마다 설명을 단다.
  캡션은 `buildDeck()` 의 카드에서 뽑는다 — 그래야 **블로그 이미지와 인스타 카드가 같은 문구**를 말한다.
  캡션은 **설명**이지 제목이 아니다. 표지만 카드 제목(후킹), 나머지는 카드 본문 첫 문장을 쓴다.
  (처음에 제목을 넣었더니 본문 소제목이 두 번 나왔다.)

### 검증 (블로그를 건드렸으면 다시 돌린다)

`validateDraft()` 의 블로그 검사는 세 가지다 — **캡션 6줄 · 목차 금지 · 줄 리듬**. 문체 검사는 없다.

> ⚠️ 줄 리듬 검사에서 **📷 줄을 제외**하는 것과 같은 이유로, 어떤 검사든 이미지 자리 표시
> ("…카드뉴스 N번을 여기에 넣으세요")를 본문으로 취급하면 안 된다. 한 번 정상 글 96건을 막았다.

마지막 실행 기준: 상품 4 × 톤 4 × variant 2 × 주제 3 × 채널 3 = **288조합 실패 0건**,
블로그 96건 전부 `validateDraft()` 통과.

---

## 8-4. 세 채널 내용 통일 (2026-08-03)

요청자 요구: **블로그·인스타·쓰레드의 내용이 같아야 하고, 이미지 한 벌을 세 채널에 나눠 쓴다.**

예전에는 채널마다 재료를 따로 골랐다(인스타 qa 3개 / 블로그 4개 / 쓰레드는 notes 별도 정렬).
같은 상품·주제인데도 **다루는 사실이 서로 달랐다.**

### `buildCore()` 하나가 모든 걸 정한다

```
buildCore(ctx) → { hook, summary[3], points[3], extra, objection, closing, cta, events }
        │
        ├── blogCopy(core)        소제목 3개 = points
        ├── instagramCopy(core)   ①②③ = points
        ├── threadsCopy(core)     points 에 대응하는 구어체 note
        └── deckFromCore(core)    카드 2·3·4 = points
```

- **`CORE_POINTS = 3`.** 카드뉴스 본문 3장과 1:1로 맞물린다. 여기를 바꾸면 덱도 같이 봐야 한다.
- ⚠️ **채널 생성기 안에서 `byTopic()`·`pick()` 으로 재료를 새로 고르지 말 것.** 그 순간 다시 갈라진다.
  생성기는 코어가 준 것만 쓴다.
- 달라지는 것은 **형식과 화법**뿐이다. 그게 '각 플랫폼에 맞게'의 의미다.
- 블로그만 문단마다 근거를 한 줄씩 덧댄다. **다루는 항목은 같고 설명만 길어지는 것**이라 통일이 깨지지 않는다.

### variant 를 채널별로 두면 안 된다

내용을 정하는 것이 variant 이므로 채널마다 따로 올리면 그 채널만 다른 사실을 다룬다.
`regenerateAll()` 이 **하나의 값을 공유**하고, 채널별 「다시 쓰기」도 전체 재생성으로 넘긴다
(`regenerateOne()` → `regenerateAll()`). 되돌리지 말 것.

### 이미지 배포 (`IMAGE_PLAN`)

카드는 **한 벌만** 만든다. 서대문구 레퍼런스가 카드뉴스 한 벌을 블로그 끝에 붙이고
같은 걸 인스타에 올린 방식 그대로다.

| 채널 | 쓰는 카드 |
|---|---|
| 블로그 | 1~6번을 📷 자리에 순서대로 |
| 인스타그램 | 1~6번 캐러셀 |
| 쓰레드 | 1번(표지)만 — 여러 장 붙이면 광고 톤이 된다 |

2단계 각 채널 탭 아래에 이 안내가 나온다.

### AI 경로도 같은 코어를 본다

규칙 기반은 코어 하나를 세 생성기가 나눠 쓰면 그만이지만, LLM 은 채널마다 따로 부른다.
그래서 `coreBlock()` 이 **코어가 고른 3가지를 프롬프트에 박아 넣는다.** 빼면 채널마다 다른 사실을 고른다.

### 검증

마지막 실행 기준 — 상품 4 × 톤 4 × variant 2 × 주제 3:

- 288건 생성, **실패 0건**
- 카드 2·3·4 ↔ 블로그 소제목 ↔ 인스타 ①②③ **288개 항목 전부 일치**
- 반론·마무리 문장도 블로그·인스타에서 동일
- ⚠️ 문자열 비교 전에 **공백을 정규화할 것.** `breathe()` 가 문장을 줄로 쪼개므로
  원문 그대로 `includes()` 하면 전건 실패한다.

> ⚠️ **쓰레드는 코어 3항목 중 평균 2.7개만 다룬다 (256/288, 89%).**
> `voice.threads.notes` 에 대응하는 구어체 문장이 없는 항목은 **일부러 뺀다**
> (`NOTE_MATCH_MIN`). 예전엔 가장 가까운 note 를 억지로 붙여서, 핵심이 '받은 다음 어디에 쓰나요'인데
> 쓰레드는 '방송사가 직접 여는 브랜드어워즈'를 말했다. 어긋난 걸 말하느니 빼는 게 낫다.
> 완전히 맞추려면 `data/products.js` 의 `threads.notes` 에 빠진 항목을 **승인받아** 채워야 한다.

---

## 8-5. 1단계 프로필 세팅 (2026-08-03)

인스타그램 계정 프로필 초안을 만든다. 요청자가 정한 순서 그대로다.
① 유형(+어워즈형은 브랜드) 선택 → ② 이름·이미지·소개 무작위 제작 → ③ litt.ly 링크.

### 레퍼런스에서 뽑은 구조 (요청자가 준 인스타 계정 5개)

| 유형 | 계정 | 구조 |
|---|---|---|
| 브랜드 어워즈형 | @kbsnawards_official · @kbsnbrand_production · @kcstawards_official | 이름 「브랜드명」 또는 「짧은이름 + 부제」 · 소개 『선언형 슬로건』 → `。` → 🏆 행사명 → ▫️ 사실 → 📩 문의 → 🔗 링크 |
| 마케터형 | @thecontents_lab · @soosangmarket | 이름 「이름 + 분야•분야」 · 소개 정체성 한 줄 → 공유하는 것 → 📨 연락처 → 🔗 링크 |

- **`。` 은 오타가 아니다.** 인스타가 빈 줄을 먹어버려서 여백 대신 넣는 글자다. 레퍼런스 3곳이 같은 방식을 쓴다.
- **litt.ly 링크는 소개 맨 마지막 줄**이다(요청자 지정). 인스타 링크 칸이 아니라 소개 안에 넣는다.
- 인스타 실제 제한을 지킨다 — 이름 30자, 소개 150자 (`LIMITS`).

### 어워즈형은 승인된 상품 4종에 붙는다

`data/products.js` 의 `PRODUCTS` 에서 이름과 사실을 가져온다.
**행사명을 지어내지 않는다** — 처음엔 무작위로 만들었는데 사실성 원칙에 걸린다.

litt.ly 주소는 브랜드마다 다르다 (`AWARD_SLUG`).

| 브랜드 | 주소 | 출처 |
|---|---|---|
| KBS N 브랜드어워즈 | `kbsn_awards` | 레퍼런스 계정 실제 주소 |
| KCST | `_kcst` | 레퍼런스 계정 실제 주소 |
| 중소기업 AI TV CF | `kbsn_ai` | 레퍼런스 계정 실제 주소 |
| 포브스 브랜드어워즈 | `forbes_awards` | ⚠️ **확인 안 된 추정값** |

### 이미지 프롬프트

**어워즈형은 심볼을 고정하고 테두리·색·마감만 바꾼다** (요청자 지시).
같은 시상 계열로 읽히면서 계정끼리는 구분되게 하려는 것이다.
심볼 7 × 테두리 8 × 색 6 × 마감 5 = 1,680가지.

**마케터형은 상패가 아니라 일하는 사람을 그린다.** 인물 8 × 배경 6 × 분위기 5 × 스타일 4 = 960가지.

4단계와 같은 원칙 — 영문, `no text`, 정사각형 아바타 기준.
생성 API 연결은 **다음 작업**이다(요청자 지시). 지금은 프롬프트까지만 만든다.

### ⚠️ 무작위 조합에서 세 번 헛디뎠다

1. **재료마다 같은 seed 로 뽑으면 조합이 맞물린다.** 80번 뽑아도 어워즈형 이름이 4종뿐이었다.
   보폭을 다르게 줘도 소용없다 — 재료 개수가 같으면 주기가 같이 돈다.
   이미지 프롬프트는 `combo()` 로 자릿수처럼(mixed-radix) 돌려 모든 조합을 훑는다.
2. **소개 문장은 `combo()` 로 묶으면 안 된다.** 뒷자리로 밀린 재료가 수십 번에 한 번씩만 바뀌어서,
   요청자가 "연차 숫자만 바뀌고 똑같다"고 지적했다.
   소개는 **재료 개수를 서로소로 맞추고 각자 `pick(pool, seed)`** 로 돌린다.
   그래야 한 번 누를 때마다 모든 줄이 바뀐다.
3. **개수를 바꿀 때 서로 나누어떨어지는지 본다.** slogan 6 · contact 4 였을 때 주기가 12로 짧아져
   200번 뽑아도 소개가 12종뿐이었다. 7 · 5 · 4 로 맞춰 455종이 됐다.

### 길이 맞추기 — `fitBio()`

150자를 넘으면 **뒤에서부터 자르면 안 된다.** 링크 줄이 먼저 날아간다.
`keep` 이 아닌 줄(여백 `。`·부가 사실)부터 덜어낸다.

### 검증 (마지막 실행 기준)

어워즈 4브랜드 × 200 + 마케터 200 = **실패 0건**.
소개 가짓수 어워즈 455종 / 마케터 200종, 이미지 프롬프트 각 200/200 전부 고유.
「다시 뽑기」 인접 seed 에서 모든 줄이 30/30 변경. 링크는 1,000건 전부 마지막 줄.

---

## 8-1. 다음 작업 — STEP 5 보관함

`pages/library.js`가 자리표시자다. 보관함 검색·필터·정렬을 만든다.
그다음이 PART 2(백엔드)다.

---

## 9. 검증 방법

브라우저 콘솔(`javascript_tool`)에서 돌린다. 매 단계 끝에 확인한다.

### 명도 대비 전수 감사 (실패 0건이어야 한다)

```js
(()=>{
const lum=c=>{const [r,g,b]=c.match(/\d+\.?\d*/g).slice(0,3).map(v=>{v/=255;return v<=0.03928?v/12.92:Math.pow((v+0.055)/1.055,2.4)});return 0.2126*r+0.7152*g+0.0722*b};
const bgOf=el=>{let n=el;while(n&&n!==document.documentElement){const b=getComputedStyle(n).backgroundColor;if(b&&!/rgba\(0, 0, 0, 0\)|transparent/.test(b))return b;n=n.parentElement}return 'rgb(255,255,255)'};
const out=[];
document.querySelectorAll('body *').forEach(el=>{
 const t=[...el.childNodes].filter(n=>n.nodeType===3&&n.textContent.trim()).map(n=>n.textContent.trim()).join('');
 if(!t)return;const cs=getComputedStyle(el);
 if(cs.visibility==='hidden'||cs.display==='none'||el.closest('.sr-only,.skip-link')||el.hidden)return;
 const size=parseFloat(cs.fontSize),w=parseInt(cs.fontWeight)||400;
 const large=size>=24||(size>=18.66&&w>=700);
 const L1=lum(cs.color),L2=lum(bgOf(el));
 const ratio=(Math.max(L1,L2)+0.05)/(Math.min(L1,L2)+0.05);
 if(ratio<(large?3:4.5))out.push(t.slice(0,20)+'|'+(el.className||el.tagName)+'|'+ratio.toFixed(2));});
return JSON.stringify({fails:out.length,list:out},null,1)})()
```

### 글귀 전수 검사 (금지 표현·길이·조사 오류)

상품 4 × 톤 4 × variant × 채널 3 조합을 전부 돌려 위반 0건인지 본다.
`generate()`와 `findBanned()`를 import해서 이중 루프로 검사한다. 과거 실행 기준 위반 0건이었다.

### 가로 스크롤

`resize_window`로 375px에서 `document.documentElement.scrollWidth === clientWidth`인지 확인한다.

### 카드 렌더 전수 검사 (4단계)

상품 4 × 톤 4 × variant 2 × 템플릿 3 × 카드 6 을 `renderCard()` 로 전부 그려 예외 0건인지 본다.
그다음 **글자가 캔버스 밖으로 잘리지 않는지**를 픽셀로 확인한다.

빈 문구로 한 번(배경만), 실제 문구로 한 번 그려 두 이미지를 뺀 차이가 곧 글자 잉크다.
그 잉크의 bounding box 가 가장자리에 닿으면 잘린 것이다.

마지막 실행 기준 (1080×1350):

| 템플릿 | top | bottom | left | right |
|---|---|---|---|---|
| magazine | 93 | 1268 | 87 | 980 |
| card | 164 | 1268 | 88 | 991 |
| note | 109 | 479 | 87 | 1012 |

`fit()`·`fitPair()`·`fitRich()` 의 `sizes`·`maxLines` 를 건드렸다면 반드시 다시 돌린다.

### 색 대비 계산 (캔버스 안은 DOM 감사가 못 잡는다)

canvas 로 그린 글자는 위의 DOM 전수 감사에 걸리지 않는다. 색을 바꿨다면 직접 계산할 것.

- 매거진형 강조색 후보: 하단 스크림 최악값 `#1A1A1A` 기준 4.5:1 이상
- 카드형 흰 글씨: `layout.brand` / `layout.surface` 기준 4.5:1 이상 (현재 4.7:1)
- 노트형: 제목 16.85 / 본문 9.42 / 부제 5.71 (종이 `#F2F2F0` 기준)

---

## 10. 버전 관리

- 원격: `https://github.com/yuki010726-eng/brand-sns-studio` (**Private**)
- 기기를 옮기기 전에 반드시 커밋·푸시하고, 시작할 때 `git pull` 한다.
- 자동 동기화가 아니다. 안 하면 양쪽이 갈라진다.
- 브라우저에 저장된 값(글귀·이미지·API 키)은 기기 간에 따라가지 않는다. README 참고.


---

## 11. 이메일 로그인 · 관리자 승인 · 기기 간 동기화 (2026-08-03)

요청자 요구: **집 PC 에서 쓴 걸 회사 PC 에서 이어서 보고 싶다.**
여태 코드만 git 으로 오갔고 작업 내용은 브라우저에 남아 기기를 따라가지 않았다.

설치 절차는 `SUPABASE_SETUP.md` 에 있다 — SQL, 이메일 로그인, 관리자 승인 설정이 적혀 있다.

### 왜 Supabase 인가

이 프로젝트에는 node/npm 이 없다. Supabase 는 **CDN 에서 ES 모듈로 바로 가져올 수 있어서**
빌드 도구 없이 지금 구조에 그대로 붙는다. Firebase 도 후보였지만 요청자가 Supabase 를 골랐다.

### 인증과 관리자 승인

현재 로그인은 보호 화면의 접근 관문이다. Supabase Auth는 이메일/비밀번호를 검증하고,
`public.users.status`는 관리자가 서비스 접근을 승인했는지 판단한다.

- `pending`, `approved`, `rejected`는 관리자 승인 상태에만 사용한다.
- `email_not_confirmed`, 프로필 조회 실패, RLS 오류를 `pending`으로 바꾸지 않는다.
- `SUPABASE.url` / `anonKey`가 없거나 SDK 로드가 실패하면 로그인할 수 없다고 명확히 표시한다.
- `initAuth()`가 기존 세션을 복구한 다음 라우터가 승인 상태에 따라 화면을 연다.

### 동기화 범위 — 글·문구만

`sync.js` 의 `SYNCED_KEYS` 22개. 요청자 결정으로 1차에서는 이미지를 뺐다.

> ⚠️ **`images` 를 SYNCED_KEYS 에 넣지 말 것.** 그건 '이 기기에 어떤 이미지가 있는지' 적어 둔 값이라,
> 실제 Blob 이 없는 기기로 옮기면 있지도 않은 이미지가 있다고 표시된다.
> 이미지까지 옮기려면 Supabase Storage 를 붙여야 한다 — 별도 작업이다.
> (게시물 하나에 6장 × 약 1MB 라 무료 용량을 금방 쓴다.)

API 키도 동기화하지 않는다. 기기마다 따로 넣는다.

### 되먹임을 막는 장치

`setState()` 가 바뀔 때마다 `scheduleSync()` 를 부른다. 그런데 서버에서 받아온 내용을 얹을 때도
`setState()` 를 쓰므로, 그대로 두면 **받자마자 도로 올리는** 순환이 생긴다.
`applyRemote()` 가 `applying` 플래그를 세우고 `scheduleSync()` 가 그때는 아무 일도 하지 않는다.

불러오기도 **로그인 상태가 잡힐 때 한 번만** 한다. `initAuth()` 안에서도 `onAuth` 가 울리므로
`app.js` 의 구독 한 곳에만 두고 `restored` 로 막는다 — 안 그러면 두 번 받는다.

### 충돌 — 마지막 저장이 이긴다

두 기기에서 동시에 고치는 일은 드물고, 자동 병합은 글을 뒤섞어 더 나쁜 결과를 만든다.
대신 `hasRemoteChange()` 로 다른 기기가 먼저 고쳤는지 확인할 수 있게 열어 뒀다.

### 남은 것

- **실사용하려면 배포가 필요하다.** localhost 만으로는 다른 PC 에서 접속할 주소가 없다.
- 관리자 승인은 `public.users.status`만 사용한다. Supabase Auth의 이메일 Confirm과 섞지 않는다.
  Confirm email을 사용한다면 미확인 상태는 별도 오류로 안내해야 한다.
- 이미지 동기화(Storage)
