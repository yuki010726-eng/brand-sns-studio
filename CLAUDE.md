# CLAUDE.md — 브랜드 SNS 스튜디오

Claude Code가 세션 시작 시 자동으로 읽는 프로젝트 지침이다.
**PART 1(STEP 5까지) 완료. API 키는 개인이 발급해 넣는다 — 8-7 참고(현재 방식).
⚠️ 배포하지 않는다. 관계자가 각자 로컬에서 띄워 쓴다 (요청자 결정 2026-08-10).
서버 프록시(8-6)와 `DEPLOY.md` 는 되돌릴 때를 위한 코드·문서일 뿐 쓰지 않는다.**

---

## 1. 이 프로젝트가 뭔가

브랜드 4개 상품의 SNS 게시물을 만드는 도구. 흐름은 4단계다.

```
1단계 프로필 세팅 → 2단계 상품·주제 선택 → 3단계 아이디어 문서화 → 4단계 카드뉴스 템플릿(이미지 포함)
```

**1단계 프로필 세팅은 건너뛸 수 있다.** 게시물마다 하는 일이 아니라 계정을 한 번 잡는 단계다.
`reachedStep()` 이 최소 2를 돌려주는 이유다 — 1을 돌려주면 프로필을 만들기 전에는 상품 선택으로 못 넘어간다.

**이미지 제작은 원래 별도 단계(`pages/image.js`)였다가 3단계 안으로 합쳤다.**
이미지가 필수가 아닌데 단계로 세워 두니 흐름을 막는 것처럼 보였기 때문이다.
지금은 문구 입력칸 반대쪽(미리보기 아래)에 이미지 패널로 붙어 있고, 원할 때만 쓰면 된다.

상품 4종: KBS N 브랜드어워즈 / 포브스 브랜드어워즈 / KCST 대한민국 고객만족도 신뢰도 대상 / 중소기업 AI TV CF

| 단계 | 파일 | 상태 |
|---|---|---|
| 1 | `pages/profile.js` + `lib/profile.js` | ✅ 완료 (8-5 참고) |
| 2 | `pages/home.js` | ✅ 완료 |
| 3 | `pages/copy.js` + `lib/copywriter.js` | ✅ 완료 (블로그 형식은 8-3 참고) |
| 4 | `pages/template.js` + `lib/templates.js` `lib/cardrender.js` `lib/concepts.js` | ✅ 완료 |
| 4의 이미지 | `components/imagepanel.js` + `lib/imageprompt.js` `lib/imagestore.js` | ✅ 완료 (프롬프트만 표시, API 생성 없음 — 2026-08-11) |
| 보관함 | `pages/library.js` + `lib/librarystore.js` | ✅ 완료 (8-1 참고) |
| 로그인·동기화 | `lib/supabase.js` `lib/auth.js` `lib/sync.js` | ✅ 코드 완료 · **설정 대기** (11 참고) |

---

## 2. 실행

빌드 도구를 쓰지 않는다. ES 모듈을 쓰므로 `file://`로 열면 동작하지 않는다. 반드시 서버로 띄운다.

```bash
python -m http.server 5610
```

`.claude/launch.json`에 등록돼 있으므로 `preview_start`로 실행하면 된다.
`python`이 없고 `py`만 있으면 `launch.json`의 `runtimeExecutable`을 `py`로 바꾼다.
`serve.cmd`를 더블클릭해도 된다 (python → py → python3 순으로 시도).

### 캐시 주의

`python -m http.server`는 캐시 헤더를 붙이지 않아, 코드를 고쳐도 브라우저가 옛 파일을 쓴다.
브라우저에서 검증하기 전에 반드시 캐시를 비운다.

```js
Promise.all([...document.styleSheets].filter(s=>s.href).map(s=>fetch(s.href,{cache:'reload'})))
  .then(()=>location.reload())
```

JS 모듈까지 바꿨다면 해당 파일도 `fetch(path,{cache:'reload'})` 한 뒤 reload 한다.
이걸 빼먹으면 "고쳤는데 반영이 안 된다"고 잘못 판단하게 된다.

---

## 3. 절대 임의로 바꾸지 말 것

### 디자인 토큰 (`styles/tokens.css`)

```
--primary #3182F6 / --primary-dark #1B64DA / --text #4E5968 / --sub #8B95A1
--bg #F2F4F6 / --surface #FFFFFF / --border #E5E8EB
```

버튼은 둥근 알약: `border-radius:999px; padding:12px 22px; font-weight:700`
폰트는 Noto Sans KR (400/500/700). 아이콘은 라인, 24x24, `stroke-width:1.5`, `fill:none`.

> ### ⚠️ 파랑은 세 곳에만 쓴다 (2026-08-12, 요청자 지시)
>
> "모든 UI가 너무 번잡해. 흰색 파란색이라서 눈에도 잘 안 보인다."
> **레시피 색 값은 하나도 바꾸지 않았다.** 바꾼 것은 **어디에 쓰느냐**다.
>
> | 파랑을 쓰는 곳 | 그 외 전부 |
> |---|---|
> | 주 버튼(`--cta-bg`) · 포커스 링 · 입력 포커스 테두리 | 중립색(`--ink` `--ink-soft` `--fill`) |
>
> - **작은 컨트롤의 선택 상태는 `--select-bg`(= `--ink`) 하나로 통일한다.** 칩·탭·스테퍼·프로필 pick 이
>   전부 같은 색이다. 한 곳만 파랑으로 되돌리면 화면에서 그것만 튄다.
>
> #### ⚠️ 큰 칸은 예외다 — `--select-panel-bg` (2026-08-14, 요청자 지시)
>
> "선택된 칸은 외곽선만 검은색으로 하니 잘 안 보이는 것 같아. 지금부터 선택된 건 **색깔로 칸을 칠해** 줘."
>
> 유형·상품·템플릿 카드는 안에 두세 줄의 글과 **또 다른 선택 칩**이 들어간다.
> 칩처럼 짙게 칠하면 그 안의 글과 칩이 배경에 묻히고, 중립 회색(`--fill`)으로 칠하면
> 흰 배경과 거의 같아서 "안 보인다"는 지적이 그대로 남는다.
> 그래서 **연한 파랑으로 칠하고 글자는 그대로 둔다.**
>
> | | 작은 컨트롤 | 큰 칸 |
> |---|---|---|
> | 배경 | `--select-bg` (#191F28) + 흰 글자 | `--select-panel-bg` (#E8F2FE) + 원래 글자 |
> | 테두리 | 같은 색 | `--select-panel-border` (#1B64DA) |
> | 쓰는 곳 | `.pick` `.tab` `.step` | `.channel` `.product-card__body` `.concept__body` |
>
> 측정값 — 선택된 칸 안 제목 9.72:1 / 설명 4.80:1. 되돌리려면 토큰 두 줄만 `--fill`·`--ink` 로 바꾼다.
> - 배지 기본값은 중립이다. 정말 튀어야 하는 하나에만 `.badge--accent`.
> - **그림자는 실제로 떠 있는 것(모달·토스트)에만.** `--shadow-sm/md` 는 `none` 이다.
>   카드마다 그림자를 깔면 화면이 붕 떠 보인다.
> - 되돌리려면 `--select-bg` 한 줄을 `--primary-weak` 로 바꾸면 된다.

### 접근성 (요청자 지정 규칙 — 디자인 값과 충돌하면 이쪽이 우선)

- 모든 버튼에 `aria-label`, 장식 아이콘은 `aria-hidden="true"`
- **명도 대비 4.5:1 이상**
  - `--sub`(#8B95A1)는 흰 배경 3.08:1이라 본문 텍스트에 쓰지 않는다. 아이콘·구분선·비활성만.
  - 작은 보조 텍스트는 `--sub-strong`(#5F6B7A) — 흰 배경 5.43:1 / 회색 배경 4.92:1
  - CTA 배경은 `--cta-bg`(=`--primary-dark`). `#3182F6`+흰 글자는 3.71:1로 미달이라
    **요청자 승인을 받아** 변경했다. 되돌리려면 `--cta-bg` 한 줄만 바꾸면 된다.
  - 카드뉴스 **카드형의 파랑**도 같은 이유로 낮췄다. 레퍼런스 `#3B93F7` 은 흰 글자 3.13:1 이라
    큰 글씨 기준(3:1)은 겨우 통과하지만 여유가 없다 → `#2673D2` (4.7:1).
    `lib/concepts.js` 의 `card.layout` 두 줄만 바꾸면 레퍼런스 색으로 되돌아간다.
- `:focus-visible` 3px 아웃라인. 시각적으로 숨긴 radio/checkbox도 label에 포커스링을 전달한다.
- 이미지 `alt` 필수. canvas는 `role="img"` + `aria-label`.

### 폼 컨트롤에 `autocomplete="off"`

새로고침 시 브라우저가 예전 선택을 복원하며 `change`를 발생시켜 **저장된 상태를 덮어쓴다.**
실제로 컨셉 선택이 초기화되는 버그가 있었다. 라디오·체크박스·textarea에 반드시 붙인다.

---

## 4. 콘텐츠 사실성 원칙 (위반 시 재작업)

상품 정보 출처는 **공식 제안서·브로셔 8종**이며(2026-08-13 전면 검증) `data/products.js`에 옮겨져 있다.
통합 팩트시트는 `바탕화면/card_news_gpt_package/04_KNOWLEDGE_브랜드_정보_v2_통합.md` 에 있다.
(예전 출처였던 `07_BRAND_INFORMATION.md`(2026-07-23)는 미검증 항목이 섞여 있어 대체됐다.)

- 매출 상승·광고 성과를 **보장하지 않는다**
- 공식 근거 없이 **최고·유일·1위·최고 권위** 단정 금지
- **종료된 행사**(`events[].status !== 'open'`)를 모집 중처럼 쓰지 않는다
- 마무리 문장은 반드시 `product.closings` 배열에서만 가져온다 (승인된 표현)
- KCST 심사 기준 비율 합계 100%를 정확히 유지한다
- 생성 결과는 `BANNED_PHRASES` 검사를 통과해야 한다 (`lib/copywriter.js`의 `findBanned`)

각 상품의 `cautions` 배열은 화면에 항상 노출한다.

### ⚠️ 비용 — **금액 공개** (2026-08-20 개정 · 3판, 요청자 결정)

> **이 규정은 세 번 바뀌었다.** ① 전면 금지 → ② 금액만 비공개(8/14) → ③ **금액 공개(8/20)**.
> 막는 것은 이제 금액이 아니라 **"싸다"는 주장**뿐이다.
>
> | 쓴다 | 안 쓴다 |
> |---|---|
> | `월 100만원(VAT 별도) · 6개월 계약 기준` / `기본 CPV 5원(15초 1회)` / `나레이션 별도 10만원` | `저렴한` · `가성비` · `부담 없는 **비용**` · 할인율 · 정가 비교 |
>
> ⚠️ **금액만 떼어 쓰지 않는다.** 조건(계약 기간·VAT)을 반드시 붙인다.
> ⚠️ **금액이 확정된 것은 AI TV CF 뿐이다.** 어워즈 3종 참가비는 **지어내면 폐기 사유**다.
>    대신 **문의로 넘긴다** — ① 왜 정해진 금액이 없는지 ② 무엇이 금액을 가르는지(부문·패키지·
>    송출 범위·시상식 참석) ③ 어디로 물어보면 되는지(1566-2347). ②가 물어볼 이유를 만든다.
>    막는 게 아니라 넘기는 구성이다 (요청자 지시 2026-08-20).
> ⚠️ 앱의 상품 자료(Supabase)에는 금액이 들어 있지 않다. 넣으려면 `products` 를 고쳐야 한다.
>    스킬(`brand-info.md` 0-7)에만 반영돼 있다.

<details><summary>이전 판 (2026-08-14, 폐기)</summary>

### ⚠️ 비용 — 금액만 비공개, 상담 안내는 허용 (2026-08-14 개정, 요청자 결정)

**막는 것은 '금액'이지 '비용이라는 주제'가 아니다.** 2026-08-13 판의 전면 금지는 폐기했다.

| ⭕ 쓴다 | ❌ 안 쓴다 |
|---|---|
| 비용은 상담 시 안내드립니다 / 부담 없이 상담 남겨보세요 / 조건은 담당자가 안내드립니다 | 구체 금액·단가 / 저렴한·가성비 / **부담 없는 비용**·부담 없는 가격 / 할인율·정가 비교 |

> ⚠️ **'부담 없는 상담'과 '부담 없는 비용'은 다르다.** 앞은 행동 안내, 뒤는 가격 주장이다.
> 가격을 주장하는 순간 표시광고 리스크가 붙고, 가격 경쟁 프레임으로 끌려간다. 이 한 칸이 규정의 경계다.
>
> **왜 열었나**: 비용을 아예 안 꺼내면 독자는 "말 안 하는 게 더 수상하다"고 읽는다.
> 게다가 콘텐츠의 목적지가 상담인데 상담을 안내할 문장 자체를 막고 있었다.
> 금액만 감추고 **다음 행동은 분명히 안내한다** — 이게 정상적인 리드 설계다.

**판단 기준 한 줄**(폐기): "얼마"는 말하지 않는다. "어디서 알 수 있는지"는 반드시 말한다.

</details>

> **새 판단 기준 한 줄: "얼마"는 말해도 된다. "싸다"는 말하지 않는다.**

> 코드 반영 위치 — `BANNED_PHRASES`(data/products.js)에서 '참가비'·'심사비'·'비용은 상담'을 빼고
> '저렴한'·'가성비'·'부담 없는 비용'을 넣었다. 상품별 `cautions` 문구도 같이 고쳤다.

### ⚠️ AI TV CF의 '300만원' — 체감은 세게, 단어만 정확하게 (2026-08-14 개정)

공식 표기는 **`300만원 상당 AI CF 무상지원`** 이다. 성격은 **지원 상당액**(현물 서비스의 가치)이며
사실이므로 세게 쓴다. 8-9 ①의 "무상 지원이라고 쓰게 만들면 안 된다"는 **더 이상 유효하지 않다.**

> **카피라이팅 목표**: 독자가 `300만원 지원받는다` / `300만원 할인받는다`와 **똑같은 크기로 체감**하게 만든다.
> 다만 그 두 단어만 쓰지 않는다. **규제하는 건 단어지 임팩트가 아니다.** 임팩트는 오히려 최대로 올린다.

| ⭕ 쓴다 | ❌ 안 쓴다 |
|---|---|
| 300만원 상당 AI CF 무상지원 / TV CF 제작비 0원 / **300만원 → 0원** / 300만원짜리 TV CF를 0원에 / 300만원 아끼고 시작하는 TV 광고 / 제작비는 오픈엑스가 부담합니다 / 300만원 상당 제작 지원 | 300만원 지원금 / 300만원 드립니다 / 300만원 할인 / 300만원 캐시백 / 정가 ○○ → 0원 / 정부지원·국가지원 |

> **"준다(給)"가 아니라 "안 받는다(無償)"로 쓴다.** 같은 임팩트, 다른 리스크.
> 헤드라인에서는 **숫자 300만원과 0원을 붙여 놓는 것**이 가장 강하다.
>
> - `지원금` — 상품명이 「중소기업지원 프로젝트」라 붙이면 **정부 지원금(현금)으로 읽힌다.**
>   법적 문제 이전에 잘못된 리드가 유입돼 상담 효율이 떨어진다.
> - `할인` — **정가가 존재한다는 전제**를 만든다. "그럼 원래 얼마인데요"로 대화가 끌려가고,
>   비공개인 집행 단가 질문이 상담 앞으로 나온다.
> - `제작 지원`·`무상 제작`은 사실이므로 **쓴다.**

### ⚠️ 성과 서술 — 자산이 먼저, 수치는 조건부 허용 (2026-08-20 개정 · 2판)

> **2026-08-20 요청자 결정** — 8/14 판의 성과 수치 전면 금지를 풀었다.
> **근거와 기준을 병기하는 조건으로** 쓸 수 있다. 다만 **본체는 여전히 자산**이다.
>
> | 쓸 수 있는 수치 | 병기할 것 |
> |---|---|
> | 상세페이지 구매 전환율 평균 **15~30% 상승** | `브랜드별 상이` |
> | **동종업계 부문 중 단 하나**만 선정 | 어느 시상식의 어느 부문 기준인지 |
>
> ⚠️ **기준 없이 숫자만 쓰면 그때부터 위반이다.**
> ⚠️ 여전히 금지: **개별 브랜드의 결과를 약속**하는 문장 ("수상하면 매출이 오릅니다").
> ✅ 전환율 15~30% 는 **확정된 값으로 쓴다** (요청자 확인 2026-08-20). `[확인 필요]` 로 두지 않는다.
> ⚠️ 앱의 `findRisky()` 는 여전히 "매출 상승" 류를 잡는다. 수치를 앱 글에도 쓰려면 그쪽을 함께 봐야 한다.

<details><summary>이전 판 (2026-08-14) — 원칙은 유효하다</summary>

### ⚠️ 성과가 아니라 자산을 판다 (2026-08-14, 요청자 결정)

4종은 전부 **마케팅 상품**이다. 매출·문의를 만들어내는 장치가 아니라 **활용할 자산을 만들어주는 장치**다.
성과는 그 자산을 어디에 얼마나 쓰느냐에서 갈린다. 잘 쓰는 곳은 추가 수익까지 가져간다.

- ❌ "수상하면 매출이 오릅니다" / "문의가 2배로"
- ⭕ "손에 남는 것: 엠블럼 5종 · 상장과 트로피 · 검색에 남는 기사 · 홈페이지 게재 · 인터뷰 영상"
- ⭕ "상세페이지 첫 화면에 올리는 곳과 명함에만 넣는 곳은 1년 뒤가 다릅니다"

> **수상 사례의 성과 수치가 없는 것은 결함이 아니다. 애초에 성과를 파는 상품이 아니다.**
> 그러니 "사례가 없어서 콘텐츠 품질이 떨어진다"는 진단은 폐기한다.
> 대신 **활용법이 콘텐츠의 본체**가 된다 — 엠블럼을 어디에 붙이는지, 수상 기사를 검색 자산으로 어떻게 쓰는지,
> 메탈 현판·X배너를 오프라인 어느 접점에 두는지. **"받는 법"보다 "쓰는 법"이 양질의 정보다.**
>
> 수치를 쓸 수 있는 영역은 따로 있다(사실이므로 적극 활용):
> 매체 규모 3,174만 가입가구(2025.12 기준) · 심사 배점 1,000점/100점 · 2023년 108개사 → 2024년 139개사 · 200여 개 제휴사.
>
> ⚠️ **IPTV 총계는 3,174만으로 확정했다** (요청자 결정 2026-08-20). 3사 숫자(KT 1,300·B tv 660·U+tv 540)를
>    **더해서 총계를 만들지 말 것** — 합은 2,500만이라 3,174만과 다르다(집계 시점·기준이 다르다).

</details>

### ⚠️ 주최·주관·후원은 행사마다 다르다

특히 포브스는 4개 행사가 **전부 다르다.** `events[].host` 를 그대로 쓰고 뭉뚱그리지 않는다.

- A 혁신대상 — 주최 중앙일보 / 주관 포브스코리아
- B·C — 주최·주관 중앙일보·포브스코리아 / 후원 JTBC
- D 사회공헌대상 — 주최 **대한상공회의소**·중앙일보 / 후원 **산업통상자원부·보건복지부**·JTBC

> KCST도 3개 기관(대한민국고객만족평가원 / 대한민국소비자신문 / 한국인터넷신문방송기자협회)을 함께 표기한다.
>
> ⚠️ **CEO 리더십 경영대상은 취급하지 않는다** (요청자 결정 2026-08-13). 브로셔는 있으나 상품 목록에서 뺐다.
> 되살리려면 `events` 에 `{ code:'E', date:'2026-06-26', host:'주최 월간중앙·중앙일보 / 주관 월간중앙 사무국 / 후원 포브스코리아' }` 를 넣는다.

---

## 5. 작업 규칙

1. **단계별 진행 + 검수.** 한 번에 다 만들지 말고, 한 단계 끝나면 요청자 검수를 받고 다음으로 간다.
2. **배치가 애매하면 임의로 정하지 말고 질문한다.**
3. **백엔드·API·배포가 필요한 항목은 먼저 물어본다.** 지금은 PART 1(프론트엔드)이다.
4. **검증 없이 "완료"라고 하지 않는다.** 브라우저로 실제 확인하고, 못 한 검증은 못 했다고 말한다.
5. 기존 구조·네이밍을 따르고, 요청 범위 밖 리팩토링은 하지 않는다.

---

## 6. 지금까지 내린 결정과 이유

이 맥락을 모르면 되돌리기 쉬운 것들이다.

| 결정 | 이유 |
|---|---|
| 빌드 도구 없는 정적 구조 | 작업 PC에 node/npm이 없다. Vite를 쓰면 실행·검증이 불가능하다. |
| 카드뉴스 **1080×1350 (4:5)** | 레퍼런스 세 계정이 모두 세로형이고, 인스타 피드에서 화면을 더 크게 차지한다. **원래는 1080×1080이었다** — 블로그 본문 재사용 때문이었는데, 레퍼런스 반영을 우선해 2026-07-28에 요청자 승인을 받아 바꿨다. 되돌리려면 `lib/cardrender.js` 의 `H` 한 줄이다. |
| 채널별 화법 완전 분리 | 처음엔 데이터를 불릿으로 덤프해서 "AI로 뽑은 것 같다"는 지적을 받았다. 지금은 `product.voice`의 문장 재료를 조합한다. |
| 쓰레드만 별도 화법(`voice.threads`) | 공지 톤이 아니라 "알게 된 걸 흘리는" 톤이 필요하다. **해시태그·계정·CTA를 넣지 않는다** — 하나라도 붙으면 광고 티가 나서 톤이 무너진다. |
| 블로그는 Q&A 구조 | 검색 유입용. 질문과 답을 `voice.qa`에 **쌍으로** 묶었다. 따로 뽑으면 소제목과 본문이 어긋난다. |
| 카드 장면(`shot`)도 qa 항목에 동봉 | 주제에 따라 순서가 바뀌어도 그림과 글이 어긋나지 않게. |
| 이미지 프롬프트는 **영문** | 이미지 모델이 영문 지시를 훨씬 정확히 따른다. |
| 프롬프트에 `no text, no letters` | 문구는 4단계에서 얹어야 수정 가능하고, 생성 이미지의 한글은 깨진다. |
| 이미지는 IndexedDB | localStorage(약 5MB)에 1024px PNG 6장이 안 들어간다. |
| 4단계에서 배경 이미지를 **선택 사항**으로 | API 비용 때문에 이미지 생성을 아직 못 쓴다. 이미지가 없어도 카드뉴스를 완성할 수 있어야 한다. 그래서 `reachedStep()`도 이미지가 아니라 **글귀**가 있으면 4단계를 연다. |
| 이미지 제작을 **별도 단계에서 빼서 템플릿 안으로** | 이미지는 선택 사항인데 단계로 세워 두니 반드시 거쳐야 하는 것처럼 보였다. 문구와 이미지를 오가며 보는 것도 번거로웠다. 없어진 `#/image` 경로는 `app.js`의 `MOVED`가 템플릿으로 넘긴다. |
| 블로그 글에 **이미지 자리 표시**(`📷 [이미지 N · 역할]`) | "생성된 이미지가 어디에 들어가는지" 알 수 없다는 지적. 카드 6장이 글의 어느 위치에 대응하는지 한 줄로 박아 둔다. 붙여넣고 지우기도 쉽다. |
| 인스타는 **질문(?)과 사실(습니다)을 번갈아** 배치 | 전부 '~습니다'로 끝나 리듬이 없고 읽히지 않는다는 지적. 사실 문장 자체는 못 고치므로(사실성 원칙) 사이에 다른 종결을 끼워 리듬을 만든다. |
| 쓰레드 맨 앞에 **후킹 한 줄**, 맨 뒤에 **질문** | "왜 읽어야 하는지 모르겠다"는 지적. 후킹 문장은 사실 주장이 아니라 화자의 경험·태도라서 사실성 원칙에 걸리지 않는다. |
| 카드 6장을 한 화면에 늘어놓지 않고 **탭으로 한 장씩** | 슬롯이 카드당 4개(라벨·제목·본문·핸들)라 6장을 동시에 펼치면 미리보기와 입력칸의 대응이 무너진다. 요청자가 제시한 레퍼런스 UI도 한 장씩 편집하는 구조였다. |
| OpenAI를 브라우저에서 직접 호출 | 백엔드가 없다. **요청자가 개인·내부용으로 쓰기로 합의한 방식이다.** |

---

## 7. API 키 취급 (중요)

AI 제공자는 **OpenAI 하나만** 쓴다(요청자 결정, 2026-08-11). 전에는 Gemini/OpenAI 둘 다
지원하는 제공자 선택 레이어(`lib/imagegen.js`, `lib/llm.js`의 provider 분기)가 있었는데
Gemini 를 완전히 뺐다 — `lib/gemini.js` · `lib/imagegen.js` 는 삭제됐다.
텍스트 생성은 `lib/openai.js` 하나가 담당하고, `lib/llm.js`는 그걸 그대로 다시 내보내는
얇은 통로다. **이미지는 API로 만들지 않는다** — 8절 "이미지 패널" 참고.

- OpenAI 텍스트 모델은 **무료 한도가 없다.** 게시물 1세트에 약 $0.03(모델에 따라 다름).
- 키는 화면(3단계 AI 설정)에서 요청자가 직접 입력하고 `localStorage`(`bboggl.openai-key`)에 저장된다.
- **키를 코드·저장소·문서에 절대 넣지 않는다.** 대화로 받지도 않는다.
  > 예외가 두 가지다.
  > 1. **Supabase `anon public` 키**는 Supabase 가 클라이언트에 심으라고 만든
  >    공개 키라서 `lib/supabase.js` 에 둔다. 실제 보호는 테이블의 RLS 가 한다.
  >    ⚠️ **`service_role` 키는 진짜 비밀키다.** 절대 파일에 넣지 말 것 — 모든 권한을 가진다.
  > 2. **`lib/apikeys.local.js`** — `localStorage`에 값이 없을 때 대체값으로 쓰는 로컬 전용
  >    파일이다. `.gitignore`에 있어 **절대 커밋되지 않는다.** 화면에 매번 입력하지 않고
  >    기기에 한 번 채워 두려는 용도(요청자 결정, 2026-08-11). 기기를 옮기면 이 파일은
  >    git으로 안 따라가니 새 기기에서는 다시 채워야 한다(11절 동기화 정책과 같은 맥락).
  >    파일이 없어도(신규 clone) `lib/openai.js`가 동적 import 실패를 잡아 빈 값으로 넘어가
  >    앱이 깨지지 않는다.
- 이 저장소는 Private이지만 그것과 무관하게 키는 파일에 두지 않는다.
- 커밋 전 `git grep -nI "sk-[A-Za-z0-9]\{16,\}"`로 확인하는 습관을 유지한다.

> ⚠️ 브라우저 직접 호출은 키가 노출되므로 **로컬·내부용 전용**이다.
> 외부 배포 시에는 서버 프록시로 옮기고 키를 서버에만 둔다 → PART 2.

---

## 8. 완료된 STEP 4 — 카드뉴스 템플릿 (구조 메모)

`pages/template.js` + `lib/templates.js` + `lib/cardrender.js`. 손댈 때 알아야 할 것만 적는다.

### 화면 구조

**템플릿 3종 선택 → 카드 탭(01~06) → 2열 편집.**
왼쪽은 스크롤을 따라오는 미리보기 canvas, 오른쪽은 슬롯별 입력칸이다.
카드 저장 버튼과 **이미지 패널**은 오른쪽 입력 폼 맨 아래에 둔다.
입력칸 라벨에 '카드 어디에 들어가는 글인지'를 반드시 함께 적는다
(계정 이름(상단 중앙) / 대주제(파란 박스) / 부제(중앙, 회색) …).
미리보기와 입력칸을 눈으로 잇는 유일한 단서라 라벨에서 위치 표현을 빼지 말 것.

시스템이 `buildDeck()` 결과로 문구를 미리 채우고, 사용자는 칸마다 고친다.
요청자가 명시한 요구사항이다 — "추천 글을 먼저 작성해주고 수정할 수 있도록".

### 템플릿 3종 (레퍼런스: 바탕화면 `브랜드 sns 스타일 레퍼런스 자료/`)

| 템플릿 | 레퍼런스 | 장별 배치 |
|---|---|---|
| `magazine` 매거진형 | ai.brief.kr | **모든 장 동일.** 상단 중앙 계정명 · 하단 검정→투명 그라데이션 · 카테고리 pill · 흰 제목 + **형광색 강조줄** · @아이디 |
| `card` 카드형 | soosangmarket | **표지** 배경 사진 + 하단 테마색 그라데이션 + 테두리 pill + **아주 큰 흰 제목** / **본문** 상단 테마색 알약(제목) + 하단 흰 박스(본문·출처) / **마무리** 테마색 단색 + 중앙 팔로우 문구 (이미지 안 씀) |
| `note` 노트형 | sslmo.lab | **표지** 종이 배경 + 중앙 제목·부제 + 일러스트 / **본문** 좌상단 실험실 심볼 + 테두리 알약 라벨 + **왼쪽 정렬** 아주 굵은 검은 제목 + 회색 본문 + 검정 박스·번호 박스 |

> ⚠️ **카드형 표지와 본문은 배치가 다르다.** 한 번 같은 배치로 합쳤다가 되돌렸다(2026-08-03).
> 요청자 지적: "카드형 표지는 기존 작업이 맞다." 레퍼런스 표지 3장이 전부 하단 큰 제목 구조다.

### ⚠️ 글자를 박스에 얹을 때는 `centerLift()` 를 쓴다

`draw()`·`drawRich()` 는 줄마다 **먼저 행간만큼 내려간 뒤** 그린다. 그래서 첫 줄 위에 행간이
통째로 여백으로 남고 마지막 줄 아래는 거의 붙는다. 그대로 두면 글이 박스 중앙보다 아래로 쏠린다.
요청자가 **두 번** 지적했다(흰 박스 → 고침, 그다음 알약 제목 → 또 지적).
새로 박스 안에 글을 얹을 때는 반드시 `y - centerLift(size, lh)` 로 그린다.

### 카드형 테마 색 · 노트형 종이

- **`state.cardTheme`** 한 곳이 카드형 전 장의 색을 정한다. 카드마다 두지 말 것 —
  요청자 요구가 "한번 바꾸면 나머지 페이지도 다 바뀌도록" 이다.
  후보 6색 전부 흰 글씨 대비 4.5:1 이상이다. **노랑은 밝은 노랑이 아니라 짙은 호박색**이다(밝으면 미달).
- **직접 입력(`#RRGGBB`)도 받는다** (2026-08-11, 요청자 요구 — 매거진형 강조 색과 같게).
  그래서 `state.cardTheme` 는 **프리셋 id 이거나 hex** 다. 비교할 때 `getCardTheme()` 를 거칠 것.
  ⚠️ 직접 입력 색에는 `light` 가 없어서 `lighten()` 이 만들어 준다. 안 만들면 표지 그라데이션이
  폴백 파랑으로 떨어져 **고른 색과 표지가 따로 논다.**
  ⚠️ 직접 입력은 대비를 보장하지 않는다. 카드형은 전부 흰 글씨라 밝은 색을 넣으면 글이 안 읽힌다.
  `contrastWithWhite()` 로 **경고만 하고 막지는 않는다** — 요청자 지시(직접 입력을 막지 말 것).
- **우상단 마크에서 이모지 4종(🔥 💡 ✅ 👉)을 뺐다** (2026-08-11, 요청자 지시). 도형 5종 + 없음만 남는다.
  ⚠️ 없앤 id 가 localStorage 에 남아 있을 수 있다. 화면은 반드시 `getMark()` 를 거쳐 선택 상태를
  정할 것 — 저장값을 그대로 비교하면 어느 견본도 안 켜지는데 렌더러는 별표를 그려 **화면과 그림이 어긋난다.**
- **`state.notePaper` / `state.noteGrain`** — 종이 색 3종(흰색·회색·블루 그레이)과 결 4단계.
  기존 `#F2F2F0` 은 누런 계열이라 뺐다. 결의 점 위치는 소수 곱셈으로 만들어 **매번 같은 무늬**가 나온다
  (렌더가 결정적이어야 한다 — 검증에서 두 번 그려 0px 차이를 확인한다).
- **`state.noteSymbol`** — 실험실 무드 심볼 9종. 레퍼런스 로고가 실험실 아이콘이라 같은 결로 만들었다.
  **실제 로고 이미지가 있으면 그쪽이 우선**이고 심볼은 그려지지 않는다.

### 카드 장수는 1~6장

2단계에서 고른다(`state.cardCount`). API 비용을 줄이려는 요청자 요구다.
`DECK_PLAN` 이 장수별 구성을 정하고, **장수를 줄여도 글은 기승전결을 그대로 쓴다** — 줄어드는 건 이미지뿐이다.
1장이면 표지가 마무리 문장까지 안고 간다.

### 제목은 900(Black)이다

레퍼런스의 제목은 700으로 그리면 눈에 띄게 얇다. `styles/tokens.css` 의 폰트 import 에
900 을 넣어 두었고 `ensureFonts()` 가 900 을 먼저 로드한다. **둘 중 하나라도 빠지면
조용히 700 으로 그려진다** — 굵기를 바꿀 때 두 곳을 같이 본다.

### 슬롯 정의는 `lib/templates.js` 한곳에

템플릿마다, 그리고 표지/본문/마무리마다 들어가는 글의 자리가 다르다.
`roleOf()` 가 kind → role 을 정하고, `slotsFor()` 가 입력칸을, `defaultsFor()` 가 초기값을 준다.
**폼과 렌더러가 같은 정의를 보게 해야 한다** — 한쪽만 고치면 조용히 어긋난다.

매거진형 기본 문구는 `headline()` → `splitHeadline()` 을 거친다.
카드 문구를 그대로 넣으면 줄이 늘어 글자가 작아지고 행간이 무너진다("글이 넘치는 느낌").
**핵심 한 문장만** 쓰고 자연스러운 어절 경계에서 두 줄로 나눈다 — 뒷줄이 형광색이다.
어절 중간에서는 자르지 않는다. 마무리 장은 승인된 문장이라 자르지도 쪼개지도 않는다.

### 렌더러 (`lib/cardrender.js`)

`renderCard(canvas, texts, { conceptId, kind, image, accent })` 한 곳이 진입점이다.

- **이미지는 선택 사항이다.** 없으면 `fallbackBg()` 가 템플릿별 배경을 그린다.
  이 덕분에 OpenAI 키 없이도 4단계를 끝까지 쓸 수 있다.
- 노트형에서 이미지는 **배경이 아니라 아이콘**이다 (`drawContain`). 나머지는 배경(`drawCover`).
- `fit()` 은 `weight`·`lh` 를 결과에 담아 돌려준다. 여러 블록을 **먼저 재고 나중에 그리는**
  바닥 정렬 배치라 `draw()` 가 자기 폰트를 스스로 복구해야 하기 때문이다.
- `fitPair()` 는 매거진형 전용 — 흰 제목과 형광 강조줄은 한 덩어리로 읽혀야 해서
  **크기가 달라지면 안 된다.** 따로 `fit()` 하면 안 된다.
- 노트형 본문은 서식을 지원한다: `**강조**` → 굵고 진하게, 줄 앞 `1.` `2.` → 번호 박스, 이모지 그대로.
  `parseBody` → `fitRich` → `drawRich` 순서다.

### 문구 상태 — 낡는 이유가 두 가지다 (자주 틀리는 곳)

`state.card = { key, concept, texts[], base[] }`

| 무엇이 바뀌었나 | 어떻게 다루나 |
|---|---|
| 상품·주제·톤 (`key`) | 내용만 낡는다. **편집본을 지키고 안내만** 띄운다. |
| 템플릿 (`concept`) | **슬롯 구성 자체가 달라진다.** 반드시 다시 세운다 — `mergeTexts()` 가 직접 고친 값만 살리고 나머지는 새 기본값으로 채운다. |

둘을 한 지문으로 합치면 안 된다. 실제로 그렇게 만들었다가, 템플릿을 바꿔도 이전 슬롯이
그대로 남아 **새 템플릿에 없는 칸이 빈 채로 보이는** 버그가 났다.

### 이미지 패널 (`components/imagepanel.js`)

**AI가 여기서 이미지를 직접 만들지 않는다** (요청자 결정, 2026-08-11). 전에는 "AI로 생성"·
"전체 생성" 버튼이 Gemini/OpenAI 이미지 API를 브라우저에서 바로 불렀는데, 그 버튼과 관련
키 설정 UI를 뺐다. 지금은 한 카드 기준으로 **프롬프트 보기/복사 · 파일 올리기 · 지우기**만 담는다.
프롬프트를 복사해 원하는 이미지 도구에서 만든 뒤 「파일 올리기」로 넣는 흐름이다.

- 카드형 마무리 장은 단색 배경 고정이라 패널이 안내문만 남기고 비활성된다(`usesImage()`).
- 이미지가 바뀌면 `applyImage()` 한 곳을 통해 IndexedDB·상태·비트맵·미리보기·안내문을 함께 맞춘다.
  개별로 갱신하면 어느 하나가 빠져서 화면이 어긋난다.
- `lib/gemini.js` · `lib/imagegen.js` 는 삭제됐다. `lib/openai.js`의 이미지 생성 코드(`generateImage`)도
  같이 뺐다 — 아무 데서도 부르지 않는 죽은 코드였다. 다시 API 생성 버튼을 붙이려면 git 이력에서
  복구하거나(`git log -- lib/gemini.js lib/imagegen.js`) 새로 짜야 한다.

### 선택 요소

- **강조 색상 (매거진형)** — `ACCENTS` 6종 + hex 직접 입력. `state.accent`.
  ⚠️ 입력칸은 **견본과 같은 줄**에 둔다(요청자 지시 2026-08-11). 「직접 입력」 라벨은 뺐다 —
  다시 붙이면 줄이 나뉘어 두 단이 된다. `.input` 이 `width:100%` 라 `.accent__hex` 에
  `flex: 0 0 88px` 를 못박아야 알약 옆에 붙는다. 그냥 두면 혼자 다음 줄로 밀린다(실제로 그랬다).
  견본이 한 줄을 거의 채우므로 **폭을 키우면 바로 다시 밀린다** — 키울 때 실제로 재 볼 것.
  후보 색은 전부 하단 스크림(최악 #1A1A1A) 기준 4.5:1 을 넘긴다 — 색을 추가하면 다시 계산할 것.
- **우상단 마크 (카드형)** — `MARKS` 10종. `state.mark`.
  `draw` 는 캔버스 도형(브랜드 색을 따른다), `glyph` 는 시스템 이모지 폰트로 찍는다.
- **본문 서식 넣기 (노트형)** — 폼의 칩 버튼이 커서 자리에 `**강조**` · `> ` · `1. ` · 이모지를 꽂는다.

### 잊지 말 것

- 1080×1350 고정. 전체 저장은 브라우저가 연속 다운로드를 막으므로 350ms 간격을 둔다.
- 입력 중에는 캔버스만 다시 그린다(120ms 디바운스). 폼을 다시 그리면 캐럿이 튄다.
- 이미지 업로드는 3단계와 **같은 키**(`imageKey`)를 쓴다. 두 화면이 같은 이미지를 본다.
- 카드형 파랑은 레퍼런스(#3B93F7)보다 한 단계 낮은 **#2673D2** 다. 이유는 아래 접근성 절 참고.

---

## 8-2. AI 글귀 생성 (`lib/copyai.js`)

2단계에서 「AI로 쓰기」를 누르면 규칙 기반 글 대신 LLM 이 쓴 글로 바꾼다.
규칙 기반 생성은 그대로 남아 있다 — **키가 없어도 앱은 완전히 동작한다.**

**OpenAI 텍스트 모델에는 무료 한도가 없다.** 게시물 1세트에 약 $0.03(모델에 따라 다름).

### ⚠️ 주제가 중심이다 — 프롬프트 순서를 바꾸지 말 것

처음엔 상품 자료를 먼저 넣고 주제를 한 줄로 덧붙였다. 그랬더니 모델이 **주제를 제쳐두고
자료를 순서대로 요약**했다. 요청자 지적: "기존 내용만 가져오면 어떡하냐."

지금은 이렇게 한다. 셋 다 필요하다.

1. **주제를 맨 앞에 세운다.** 자료는 그 뒤에 '재료'로 넣는다.
2. 자료 항목 이름에 `[참고 재료]`, `문장을 그대로 베끼지 말 것`을 박아 둔다.
3. 프롬프트 **맨 끝에서 주제를 다시 한 번** 못박는다.

그리고 `offTopic()` 이 주제 낱말이 글에 충분히 나오는지 확인한다(기준 35%). 안 나오면 재시도한다.

같은 상품에 주제만 바꿔 두 번 돌려 **소제목이 실제로 달라지는지** 확인할 것.
(예: '슈퍼자막 패키지가 뭔가요' vs '수상 이력을 홍보에 활용하는 법')

### AI는 버튼을 눌러야 쓴다 (2026-08-11, 요청자 지시로 변경)

2026-08-03엔 "키가 있으면 3단계에 들어오자마자 자동으로 AI가 세 채널을 다시 썼다.
**지금은 자동 호출을 없앴다** — 3단계에 들어오면 2단계에서 고른 상품·주제·톤(ctxbar)과
규칙 기반 초안만 보여주고, 「AI 생성」버튼을 직접 눌러야 그때 글이 나온다.

> ⚠️ **버튼은 한동안(2026-08-10~11) 하나로 합쳐져 있었다** — 키가 있으면 라벨이
> 「AI 시작」으로 바뀌는 식이었다. 그런데 키를 넣어도 `config.local.js` 가 없으면
> 버튼이 계속 규칙 기반으로만 도는 등 "왜 AI 로 안 써지나"가 헷갈렸고, 요청자가
> **항상 보이는 별도 「AI 생성」 버튼**을 요구해 2026-08-11 에 다시 나눴다(`pages/copy.js`
> 의 `#regen-all`·`#ai-all`). 그 뒤 요청자가 **「전체 재생성」버튼 자체를 없애 달라**고 해서
> 같은 날 다시 뗐다 — 지금은 `#ai-all` 하나만 남는다. 이후 2단계에서 조건을 바꾸고
> 3단계로 이동할 때 뜨던 조건 변경 안내와 규칙 기반 재생성 버튼도 제거했다. 조건이
> 달라졌다면 이전 조건의 글을 이어서 보여주지 않고 빈 초안 상태로 시작한다. 단,
> 기존 `AI 1`·`AI 2` 생성 기록과 아웃라인은 초기화하지 않는다.

- **프롬프트가 실제로 달라진 채널만** 부른다 (`promptKeyOf`, 아래 8-2-1 참고) — 버튼을 눌러
  전체를 다시 쓸 때도 이 지문으로 AI가 실제로 쓴 채널만 기록한다.
- 편집본이 있으면 자동으로 덮어쓰지 않는다(버튼을 눌러도 확인을 한 번 더 받는다).
- **규칙 기반 생성기를 없애면 안 된다.** 키가 없거나 한도를 넘겼을 때 앱이 멈춘다.

## 8-2-1. 비용 통제 (2026-08-10)

요청자 지시: **시키지 않았는데 토큰을 쓰는 곳을 없앤다.**
이미지 프롬프트(`lib/imageprompt.js`)는 규칙으로 조립하므로 API 를 타지 않는다 — 비용은 전부 글귀다.

### 채널별 프롬프트 지문 (`promptKeyOf`)

**측정 결과: 카드 장수를 6↔3 으로 바꿔도 인스타·쓰레드 프롬프트는 글자 하나 안 달라진다.**
장수는 블로그의 📷 줄 수에만 들어간다. 그런데 예전에는 `draftKeyOf()`(장수 포함) 한 덩어리로
판단해서 **장수만 바꿔도 세 채널을 다 다시 생성했다** — 호출 2번이 같은 글을 받고 버려졌다.

지금은 채널마다 지문을 따로 만든다.

| 채널 | 지문에 들어가는 것 |
|---|---|
| 블로그 | 상품 · 주제 · 톤 · **AI 라운드** · **카드 장수** |
| 인스타 · 쓰레드 | 상품 · 주제 · 톤 |

⚠️ `promptKeyOf()` 는 `buildPrompt()` 바로 옆에 둔다. **프롬프트에 새 입력을 넣으면 이 함수도
같이 고쳐야 한다.** 안 고치면 입력이 바뀌어도 낡은 글을 그대로 들고 있게 된다.

`state.aiKey` 는 문자열에서 `{blog, instagram, threads}` 객체가 됐다.
옛 문자열은 `load()` 가 채널별로 편다 — 그냥 비우면 업그레이드하자마자 3회를 다시 부른다.

`aiOne()` 도 지문을 남긴다. 안 남기면 사용자가 방금 AI 로 쓴 채널을 자동 실행이 또 덮어썼다.

### 출력 상한 (`maxTokensFor`)

**출력 단가가 입력의 6배라 상한이 곧 비용 상한이다.** 채널 글자 상한 + 1,500 을 준다.
여유분은 형식 토큰과 추론 토큰이 같이 먹는다. 너무 빡빡하면 글이 잘려 검수에 걸리고
재시도로 오히려 더 든다.

### 추론 강도 (`REASONING_EFFORT = 'low'`)

**추론 토큰은 출력 요금으로 과금된다.** 안 보내면 모델 기본값이 걸리는데 그 기본이 medium 이면
요청하지도 않은 비용이 매 호출 붙는다. 정해진 형식에 맞춰 쓰는 일이라 긴 추론이 필요 없다.

### 지원하지 않는 파라미터는 빼고 다시 보낸다

모델마다 지원 범위가 다르다 — 추론 모델은 `temperature` 를 안 받는 경우가 있다.
파라미터 하나로 글쓰기 전체가 죽으면 안 되므로, 400 이 나면 그것만 빼고 한 번 더 보내고
모델별로 기억해 둔다(`unsupported`). 두 번째 호출부터는 처음부터 빼고 보낸다.

### 실측값 (2026-08-10, fetch 를 가로채 전송 본문을 직접 측정)

게시물 1건 = 3채널 = 3회 호출.

| 채널 | 시스템 | 입력 | 출력 상한 |
|---|---|---|---|
| 블로그 | 945자 | 3,983~4,351자 | 4,500 |
| 인스타 | 945자 | 2,706~3,048자 | 3,700 |
| 쓰레드 | 945자 | 2,605~2,947자 | 2,000 |

입력 합계 12,100~13,200자. 한글 1자를 1토큰으로 넉넉히 잡고 출력 4,200토큰 기준
1건당 Luna $0.008 / Terra $0.076 / Sol $0.191.

## 8-8. 주제 아웃라인 · 카드 연동 · 글자 상한 (2026-08-10)

요청자 지적 세 가지를 한 번에 고쳤다. 앞의 둘은 **뿌리가 같았다.**

### ⚠️ 왜 "기존 팩트에서만 돌려 막는" 느낌이었나

`buildCore()` 는 핵심 3가지를 `products.js` 의 **미리 써 둔 `voice.qa` 에서 고르기만** 했다.
주제와 2-gram 이 겹치는 순으로 정렬할 뿐 새로 만들지 않는다. 게다가 프롬프트가 그 3개를 넣고
"이 범위를 벗어나지 마세요" 라고 못박았다. **주제를 바꿔도 같은 이야기가 돌아 나왔다.**

이제 `lib/outline.js` 가 **모델에게 주제를 쪼개게** 한다. 상품 자료는 근거로만 쓴다.

```
아웃라인 1회 → 블로그·인스타·쓰레드 3회 (같은 뼈대) → 카드뉴스 덱도 같은 뼈대
```

- ⚠️ **왜 별도 호출인가** — 세 채널은 각각 따로 호출된다. 채널마다 알아서 쪼개게 두면
  셋이 다른 이야기를 한다(요청자 요구: 세 채널 통일). 뼈대를 한 번 만들어 셋에 나눠 준다.
  호출이 3 → 4 회로 늘지만 아웃라인은 출력이 짧아 세트당 비용은 10% 안팎만 는다.
- ⚠️ **사실성 원칙은 그대로다.** 뼈대를 모델이 만들어도 사실은 자료 안에서만 쓴다.
  마무리 문장은 `product.closings` 에 있어야 하고, `validate()` 와 `mergeCore()` 가
  **두 겹으로** 막는다(검증이 closing 을 안 보던 버그가 있었다).
- ⚠️ `mergeCore()` 는 통째로 갈아치우지 않는다. `shot`(이미지 프롬프트용 영문 장면)은
  상품 데이터에만 있다. **글은 모델이, 장면은 데이터가** 맡는다.
- 실패하면 규칙 기반(`buildCore`)으로 떨어진다. 키가 없어도 앱은 끝까지 동작해야 한다.

### ⚠️ 카드뉴스가 글귀를 따라가지 않던 문제

`pages/template.js` 가 `buildDeck()` 로 카드를 만드는데 이것도 같은 `voice.qa` 에서 나왔다.
**AI 가 글을 새로 써도 카드 문구는 그대로였다.** 이제 `buildDeck(ctx)` 가 `ctx.core` 를 받고,
2단계가 만든 `state.outline` 을 4단계가 그대로 쓴다.

⚠️ 아웃라인은 `state.outline = { key, core }` 에 담고 `outlineKeyOf()`(상품·주제·톤)로
낡았는지 본다. 같은 조건으로 다시 들어가면 **다시 사지 않는다.**

### 카드 글자 수 상한 — 실측으로 정했다

`lib/templates.js` 의 슬롯마다 `max` 가 붙었다. 자세한 표와 근거는 그 파일 머리말에 있다.

- 기준은 '잘리기 직전'이 아니라 **'글자 크기가 한 번도 줄지 않는 지점'** 이다.
  잘리지만 않으면 된다고 잡으면 글자가 최소 크기까지 작아져 행간이 무너진다.
- `max` 는 **생성 기본값에만** 적용된다. 직접 더 길게 쓰는 것은 막지 않는다(요청자 지시).
- ⚠️ 노트형 본문만 유독 낮다(220). 하이라이트 바·번호 박스가 세로 공간을 훨씬 많이 먹는다.
  340 이면 160장 중 64장에서 글자가 작아졌고 220 에서 0장이 됐다.

### 넘치면 화면이 짚어 준다

렌더러는 넘치면 조용히 `…` 로 자르거나 줄을 버렸다 — **글이 사라진 걸 아무도 몰랐다.**
이제 `fit()`·`fitPair()`·`fitRich()` 가 잘릴 때 기록하고 `lastClipped()` 로 내보낸다.
`paint()` 가 그린 **직후에** 읽어(매번 초기화된다) 어느 칸이 잘렸는지 이름으로 알려 준다.

`lastSizes()` 는 슬롯별로 실제 쓰인 글자 크기를 준다. 상한을 다시 잴 때 쓰는 도구다.

### 검증 (2026-08-10)

- 상품 4 × 톤 4 × 장수 6 × 템플릿 3 = **1,008장 렌더 — 잘림 0 · 글자 축소 0**
- ⚠️ 렌더러의 `sizes`·`maxLines` 를 건드렸다면 이 수치를 **반드시 다시 재야 한다.**

---

## 8-9. 주제 해석 · AI 2 다양화 · 읽는 모양 · 말투 (2026-08-12)

요청자 지적 네 가지를 한 번에 고쳤다. **네 가지 다 원인이 코드에 있었다.**

### ① 주제를 글자 그대로 받던 문제

> ⚠️ **이 절은 8-11(2026-08-13)에서 절반이 뒤집혔다. 먼저 8-11 을 읽을 것.**
> 여기서 넣은 "글자를 그대로 따르지 마세요"가 너무 멀리 가서, 모델이 **물음 자체를 갈아치웠다.**
> 지금 규칙은 "표현은 다듬되 **대상과 물음은 바꾸지 않는다**"이다.
> 아래 내용(intent·angle 도입, 검증을 소제목에서 해석까지 넓힌 것)은 그대로 유효하다.

요청자 예: 「중소기업 AI TV CF」에 *"ai영상 무료로 받아볼수 있는방법"* 이라고 적으면
"중소기업 지원 개념으로 제작을 지원받는다" 쪽으로 유연하게 풀려야 하는데 원문만 되뇌었다.

원인이 **두 군데**였다.

| 어디 | 무엇이 막고 있었나 |
|---|---|
| `lib/outline.js` `validate()` | 소제목 3개에 **주제 낱말이 그대로** 있어야 통과였다. 유연하게 바꾸면 반려되니 통과하려면 주제를 베낀 소제목을 쓸 수밖에 없었다. |
| 프롬프트 | "주제 하나만 다룹니다", "주제와 상관없는 내용은 넣지 마세요" 로 글자를 못박고 있었다. |

이제 아웃라인이 **주제를 의도로 먼저 읽는다.** JSON 에 `intent`(알고 싶은 것)와
`angle`(그래서 답할 각도)이 추가됐고, 채널 프롬프트는 원문이 아니라 **`angle` 을 중심**으로 쓴다.

- ⚠️ **사실성 원칙은 하나도 안 풀었다.** 주제에 자료로 뒷받침되지 않는 단어가 있으면
  그 단어를 주장하지 말고 **자료 안에서 가장 가까운 사실**로 각도를 바꾸라고 프롬프트에 박았다.
  > ✅ **2026-08-13 갱신** — 당시 예시로 들었던 "무료"는 **이제 근거가 있다.**
  > 공식 제안서에 `300만원 상당 AI CF 무상지원` 이 명시되어 `data/products.js` 에 반영됐다.
  > 따라서 AI TV CF 에서 "무상 제작"은 **써도 된다**(4절 참고). 다만 "지원금·드립니다"는 여전히 금지다.
  > 근거 없는 단어를 주장하지 않는다는 **원칙 자체는 그대로**다 — 바뀐 것은 이 한 사실뿐이다.
- `validate()` 의 주제 확인은 소제목만이 아니라 `intent`·`angle`·`hook` 까지 함께 본다.
  `offTopic()`(copyai.js)도 `angle` 낱말을 인정한다. 어디에도 안 걸릴 때만 진짜 이탈로 본다.
- 해석은 화면에 **반드시 노출한다** (`angleHTML()`, 3단계 「주제를 이렇게 읽었습니다」).
  글자 그대로 안 따르게 만들었으므로, 안 보이면 왜 이런 글이 나왔는지 알 수 없다.

### ② AI 1 과 AI 2 가 거의 같던 문제

**온도 문제가 아니었다.** `aiRuns` 는 벌을 두 개까지 쌓지만, 두 벌의 상품·주제·톤이 같아
`ensureOutline()` 이 **캐시된 뼈대를 그대로** 돌려줬다. 핵심 3가지·후킹·마무리가 통째로 같은 채로
다시 쓰니 말만 조금 바뀐 같은 글이 나온다.

이제 **라운드**(= 지금까지 만든 벌 수)가 뼈대까지 내려간다.

```
「AI 생성」 → round = aiRunsFor(s).length   (AI 1 이면 0, AI 2 면 1)
   ├── ensureOutline({ round })  뼈대를 다시 짠다 + 직전 소제목을 avoid 로 넘긴다
   ├── ROUND_ANGLE   (outline.js)  다루는 항목 자체를 바꾼다 (상황 / 오해 / 체크리스트)
   └── ROUND_OPENING (copyai.js)   첫 단락 여는 방식을 바꾼다
```

- ⚠️ **`outlineKeyOf()` 에 라운드를 넣으면 안 된다.** 그 지문은 `state.aiRuns.key` 로도 쓰여
  "이 주제로 몇 벌 만들었나"를 세는 기준이다. 넣으면 AI 2 를 만드는 순간 AI 1 이 목록에서 사라진다.
  라운드는 **`state.outline.round`** 에 따로 담고 `ensureOutline()` 이 지문과 **함께** 비교한다.
- 같은 조건으로 화면에 다시 들어오는 것만으로는 라운드가 오르지 않는다 — 비용 통제(8-2-1)는 그대로다.

### ③ 마크다운이 그대로 찍히고 단락이 다 떨어져 있던 문제

**붙여넣을 곳 어디에도 마크다운이 렌더링되지 않는다** — 네이버 블로그·인스타그램·쓰레드는
전부 순수 텍스트다. `**강조**` 는 별표가, `## 소제목` 은 샵이 글자로 찍힌다.

| | 처리 |
|---|---|
| 소제목 | `##` → **`■`** (`HEAD_MARK`, `lib/copywriter.js`). 프롬프트·규칙 기반 양쪽 같은 표시 |
| 굵게·기울임·백틱 | `stripMarkdown()` 이 기호만 벗기고 글자는 남긴다 |
| 불릿 `-` `*` | `·` 로 바꾼다 |
| 인용 `> ` | **그대로 둔다.** 확정된 형식이고(8-3) 인용 표시로 읽힌다 |
| 빈 줄 | 2~3문장 묶음마다만 넣는다 (문장마다 → 묶음마다) |

#### ⚠️ 쉼표 분할은 길이로 판단하지 않는다 — `isEvenList()`

요청자 지적: "어떤 기준으로 심사하나요?는 나눠도 편한데, 무엇을 받게 되나요 같은 건 안 읽힌다."

```
쪼갠다   고객만족도 30%, / 브랜드 신뢰도 25%, / 품질 20%,        조각 7~11자로 고르다
안 쪼갠다 상장과 상패, / 인증서에 더해 네이버 플레이스 배너, / X배너,  6자와 18자가 섞인 산문
```

**마지막 조각을 뺀 모든 조각이 12자 이하일 때만** 쪼갠다. 하나라도 길면 산문으로 보고 한 줄로 둔다.
프롬프트(`channelRules`)에도 같은 기준을 좋은 예·나쁜 예로 넣었다 — **AI 글은 프롬프트가 결정하므로
둘 다 고쳐야 한다.**

#### 읽기 모드 (`previewHTML()`, `pages/copy.js`)

기본이 읽기 모드고, 「고치기」를 눌러야 편집칸이 나온다. 소제목·인용·이미지 자리·개요표·해시태그를
화면에서만 입힌다.

- ⚠️ **원문은 절대 바꾸지 않는다.** 복사되는 건 언제나 `drafts` 에 든 텍스트다.
- ⚠️ **여기에 서식 문법을 새로 만들지 말 것.** 화면에서만 통하는 규칙을 늘리면
  붙여넣은 결과와 미리보기가 어긋난다. 이미 글에 들어 있는 표시만 해석한다.
- ⚠️ `bindPanel()` 은 편집칸이 없어도 **아래 버튼들을 반드시 묶어야 한다.**
  예전 구조는 `if (!ta) return` 이었다 — 그대로 뒀으면 읽기 모드에서 복사가 죽는다.
- 읽기 모드 여부는 모듈 지역 변수다. 스토어에 넣으면 기기 간 동기화까지 따라간다.

#### ⚠️ 미리보기 여백은 요소 이름으로 초기화하면 안 된다

요청자 지적: 회색 인용 박스와 파란 소제목 **바로 아래** 글이 붙어 있어 불편하다.

`.preview p { margin: 0 }` 처럼 요소 이름으로 초기화하면 특정도(0,1,1)가
흐름 규칙 `.preview > * + *`(0,1,0)를 **이겨서 문단 사이가 0px 이 된다.** 실제로 그렇게 났다.
`.preview > * { margin: 0 }` → `.preview > * + *` 순서로만 준다.
소제목 위 여백도 `h3.preview__head` 로 특정도를 한 단계 올려야 `.preview__slot + *` 에 안 밀린다.

측정값: 문단 16 · 회색박스 아래 30 · 소제목 위 40 / 아래 30 · 이미지 자리 24.
**여백을 건드렸으면 다시 재라** — CSS 특정도 때문에 조용히 0 이 되는 자리다.

### ④ 말투가 딱딱하던 문제

요청자 지시: "블로그에서 가장 친절하고 정보 전달을 잘하는 블로그의 말투를 그대로 사용해줘."

- **화자를 바꿨다.** '카피라이터' → '먼저 알아본 사람이 옆에서 정리해 주는 블로그 운영자'.
- `SYSTEM` 의 「말투」 절을 **10개 항목으로 명세화**했다. "친절하게 써라"로는 안 된다 —
  모델이 늘 쓰던 홍보문체로 돌아간다. 그래서 **어미·문장 길이·금지어**로 적는다.
  - 기본 어미 `~예요 / ~해요 / ~거든요 / ~더라고요 / ~죠`, 못 박을 때만 `~습니다`
  - 읽는 사람의 질문을 먼저 대신 말해 주고 답하기
  - 설명 앞 한 박자 ("이 부분이 좀 헷갈리는데요,")
  - 어려운 말은 바로 풀어 주기, 숫자는 의미까지 붙이기
  - 금지어: "귀사", "바야흐로", "~의 시대", "주목받고 있습니다", "다양한", "본 상은", "당사" …
- `TONE_GUIDE`(copyai.js)는 톤마다 4~5줄로 늘리고, `TONE_ANGLE`(outline.js)로 **뼈대에도 톤을 넣었다.**
  ⚠️ 예전엔 아웃라인 프롬프트에 톤이 **아예 없었다** — 후킹과 소제목을 정하는 게 거긴데.
  "톤을 나눠도 차이가 없다"는 지적의 절반이 여기서 나왔다. **둘은 짝이라 같이 고친다.**
- ⚠️ 네 톤 모두 **바탕은 친절한 블로그 말투**다. 톤은 그 위에 얹는 강약이지 말투를 갈아치우는 게 아니다.
  `TONE_GUIDE` 에 "딱딱하게", "격식 있게" 를 다시 넣지 말 것.

> ⚠️ **문체를 강제하는 검사 장치는 여전히 만들지 않는다** (8-3 참고). 프롬프트로만 유도한다.
> 검사로 막았더니 정상 문장이 계속 반려됐던 자리다.

### 검증 (2026-08-12 실행 기준)

| 무엇 | 결과 |
|---|---|
| 규칙 기반 전수 — 상품 4 × 톤 4 × variant 2 × 주제 3 × 채널 3 | **288건 · 마크다운 누출 0 · 금지 0 · 위험 0 · 잘못 쪼갠 나열 0** |
| 쉼표 분할 | 심사 기준(고른 나열)은 쪼개고, 특전(들쭉날쭉)은 한 줄 유지 — 둘 다 확인 |
| `stripMarkdown()` 두 번 적용 | 결과 동일 (idempotent) |
| 라운드 분리 | `outlineKeyOf` 는 AI 1·AI 2 가 같고(묶음 유지), `promptKeyOf` 는 `r0`/`r1` 로 갈림 |
| 미리보기 여백 | 문단 16 / 회색박스 아래 30 / 소제목 위 40·아래 30 / 이미지 자리 24 |
| 읽기 ↔ 고치기 토글 | 양방향 정상 · 읽기 모드에서 복사·AI 생성 버튼 살아 있음 |
| 명도 대비 전수 감사 | **실패 0** |

> ⚠️ **실제 AI 호출로는 검증하지 못했다.** 이 PC 에 `config.local.js` 가 없어 키가 없다.
> 위 검증은 전부 모듈을 직접 불러 돌린 것이다.
> **키가 있는 환경에서 한 번 돌려 확인할 것** — ①의 각도 해석, ②의 AI 2 가 AI 1 과 다른지,
> ④의 말투가 실제로 부드러워졌는지.

> ⚠️ **375px 가로 스크롤 20px** 이 남아 있다. 원인은 헤더의 `.authbox`(사용자 이름 + 로그아웃)이고
> 홈·3단계·프로필 **모든 화면**에서 난다. 미리보기와 무관한 별도 건이라 이번에 손대지 않았다.

---

## 8-10. 소제목 고정 탈출 · UI 간결화 (2026-08-12)

### ① 모든 글이 "그래도 망설여진다면 → 정리하면" 으로 끝나던 문제

요청자 지적: "마지막이 무조건 그래도 망설여진다면으로 끝나더라. AI면 다양해야지."

원인은 단순했다. **문구가 세 곳에 하드코딩돼 있었다** — `channelRules()` 의 블로그 구조 6·7번,
`blogCopy()`, 그리고 카드뉴스 반론 장(`deckFromCore`). 모델은 시키는 대로 그 문구를 썼을 뿐이다.

이제 뼈대가 **주제마다 소제목을 새로 짓는다.** 아웃라인 JSON 에 `objectionHead` · `closingHead` 추가.

```
outline → objectionHead / closingHead
   ├── channelRules()  블로그 6·7번 소제목에 그대로 꽂는다
   ├── blogCopy()      규칙 기반도 같은 문구를 쓴다
   └── deckFromCore()  카드뉴스 반론 장 제목도 같은 문구 (한 벌로 읽혀야 한다)
```

- ⚠️ **프롬프트로만 시키면 모델이 익숙한 문구로 돌아온다.** 그래서 `BOILERPLATE_HEADS`
  ("그래도 망설여진다면", "정리하면", "마치며", "결론" …)를 **코드로 반려**한다.
  ⚠️ 정확히 일치할 때만 잡는다 — "작은 매장이라 망설여진다면"처럼 주제가 붙어 구체화된 건 통과해야 한다.
- ⚠️ **한계 단락과 승인된 마무리 문장 자체는 없애지 않았다.** 사실성 원칙 4번이라 그대로다.
  바뀐 건 **소제목뿐**이다. 단락을 빼자는 뜻으로 읽지 말 것.
- `generate()` 도 `ctx.core` 를 쓰게 고쳤다. 예전엔 코어를 늘 새로 만들어 AI 가 지은 소제목을
  무시했다 — 카드와 블로그의 소제목이 서로 달라지는 자리였다(`buildDeck()` 과 규칙을 맞췄다).

### ② UI 간결화

색 규칙은 3절에 정리했다. 여기에는 **구조**를 적는다.

- **3단계에 박스가 네 개(조건 바 · AI 벌 · 해석 · 진행 상황) 쌓여 있었다.**
  「주제를 이렇게 읽었습니다」를 별도 안내 박스에서 빼고 **주제 바로 아래 한 줄**로 붙였다.
  ⚠️ 다시 박스로 만들지 말 것 — 무엇을 어떻게 읽었는지는 붙어 있어야 읽기도 쉽다.
- 패널 아래 안내가 **세 줄**(글자 수 · 저장 · 이미지 배포)이었다. 한 줄로 합쳤다.
  ⚠️ 안내가 본문보다 눈에 들어오면 안 된다. 줄을 더 늘리지 말 것.
- `--radius-lg` 20 → 16, `--radius-md` 14 → 12. 모서리가 과하게 둥글면 요소가 커 보인다.

### ③ 375px 가로 스크롤 20px (같이 고쳤다)

헤더가 원인이었다. 브랜드(92) + 메뉴(116) + 계정(120) + 간격(48) = 376px 인데 쓸 수 있는 폭은 335px.
이름만 감춰서는 모자라 **간격·안쪽 여백·로고 글자**까지 줄여야 들어갔다.

- 로고 글자를 `.brand__text` 로 감싸 좁은 화면에서 감춘다. 마크는 남으므로 홈 버튼은 유지된다.
  `<a>` 의 `aria-label` 이 이름을 갖고 있어 스크린리더에는 그대로 읽힌다.
- ⚠️ **헤더에 항목을 더 넣으면 375px 에서 `scrollWidth === clientWidth` 를 다시 재라.**

### 검증 (2026-08-12 실행 기준)

| 무엇 | 결과 |
|---|---|
| 규칙 기반 전수 288건 | 마크다운 0 · 금지 0 · 위험 0 · 잘못 쪼갠 나열 0 |
| 소제목 전달 | 블로그·카드가 모두 새 소제목을 쓴다 / 옛 고정 문구 사라짐 |
| 폴백 | 뼈대가 없으면 옛 문구로 되돌아간다 (앱이 멈추지 않는다) |
| 명도 대비 — 홈·3단계·프로필·보관함·4단계 | **전부 실패 0** |
| 파란 배경 요소 | 주 버튼뿐 (배지·탭·스테퍼·안내 박스에서 전부 사라짐) |
| 375px 가로 스크롤 | 5개 화면 전부 없음 (375 = 375) |

> ⚠️ **실제 AI 호출로는 여전히 미검증이다.** 이 PC 에 키가 없다.
> 특히 `BOILERPLATE_HEADS` 반려가 통과율을 떨어뜨리지 않는지 실제 호출로 확인할 것 —
> 반려되면 아웃라인을 한 번 더 부르므로(최대 2회) 비용이 조금 는다.

---

## 8-11. 주제에 답하기 · 표지 후킹 한 줄 (2026-08-13)

요청자 지적 다섯 가지를 한 번에 고쳤다. **앞의 넷은 뿌리가 하나였다** — 각도가 주제를 떠났다.

> "『IPTV 지역 타겟팅이란』이라고 했으면 일단 지역 타겟팅에 대한 설명을 하고 해야지.
> 내가 IPTV가 뭐냐고 주제를 입력하면 그거에 대한 이야기를 해줘."
> "한 글에 모든 상품에 대한 내용이 들어가는 게 아닌 주제에 대한 핵심만."
> "IPTV를 예로 들었다고 이것만 수정하지 말고 **모든 상품·주제**에 해당되게."

### ⚠️ 「의도로 읽어라」가 너무 멀리 갔다

8-9 ①에서 "주제를 글자가 아니라 의도로 읽으라"고 넣었다. **표현이 어설픈 메모를 다듬으라**는
뜻이었는데, 프롬프트가 "글자를 그대로 따르지 마세요"라고만 말해서 모델이 **물음 자체를
갈아치우는** 데까지 갔다.

```
주제 『IPTV 지역 타겟팅이란』  (무엇인지 묻는다)
 → angle 『지역 범위와 시청 조건을 조합하는 IPTV 광고 방법』  (방법으로 답했다)
 → 글의 첫 줄이 "우리 동네에만 알릴 수 있나요?" — 정의는 한 줄도 없다
```

`validate()` 도 못 잡았다. 주제 낱말 하나만 어디든(intent·hook·소제목 포함) 스치면 통과였다.

### 무엇을 바꿨나

**주제를 코드가 먼저 읽는다** (`lib/outline.js` 의 `topicKind()` · `topicSubject()`).

| 물음 | 잡는 말 | answer 가 무엇이어야 하나 |
|---|---|---|
| `define` | ~이란 / ~란 / 뭔가요 / 뜻 / 의미 / 개념 | **그 대상의 정의.** points[0] 도 "무엇이고 어디까지인가" |
| `compare` | 차이 / 비교 / 다른 점 / vs | 둘의 차이 한 줄 |
| `whether` | 되나요 / 가능한가 / 자격 / 조건 | 되는지 안 되는지 먼저 |
| `why` | 왜 / 이유 | 이유 한 줄 |
| `howto` | 방법 / 어떻게 / ~하는 법 / 절차 / 신청 | 무엇을 하면 되는지 |
| `general` | (그 외) | 가장 먼저 알아야 할 것 한 줄 |

- ⚠️ **`why` 를 `howto` 보다 먼저 본다.** 『왜 지금 신청해야 하나』에 '신청'이 들어 있어서
  순서를 반대로 두면 방법을 묻는 글로 잡힌다(실측으로 확인하고 순서를 바꿨다).
- ⚠️ 이건 IPTV 전용 예외가 아니다. **상품·주제와 무관하게 같은 규칙**이 돈다(요청자 지시 5번).

**아웃라인 JSON 에 `answer` 가 생겼다** — 주제가 물은 것에 대한 직접적인 답 한 줄.
이게 **글의 첫 단락**이 된다. 흐르는 경로는 이렇다.

```
outline.answer → mergeCore → core.answer
   ├── coreBlock()      (copyai) 세 채널 프롬프트에 "첫 단락은 이걸 말하는 데서 시작"
   ├── blogCopy()       규칙 기반 도입 둘째 줄
   └── instagramCopy()  '더보기' 앞 둘째 줄  (예전엔 "'주제', 헷갈리는 것만 골랐어요." 안내 문구였다)
```

**검증을 두 겹으로 조였다** (`validate()`).

1. `angle` **하나만** 본다. 주제 낱말이 각도에 없으면 반려 — 다른 칸에 있어도 통과시키지 않는다.
2. **정의형 주제**는 각도에 **대상의 중심 낱말**(마지막 어절)이 반드시 있어야 한다.
   한국어는 뒤가 중심이라 『IPTV 지역 타겟팅』의 중심은 '타겟팅'이다.
3. `answer` 가 비었으면 반려.

- ⚠️ **중심 낱말 검사는 정의형에만 건다.** 모든 주제에 걸면 8-9 의 실패로 돌아간다 —
  『수상 후 언론 노출 활용법』에 "기사가 나가면 어디에 먼저 붙이나"라는 **좋은 각도**가
  '노출'이 없다는 이유로 반려된다. 실제로 그 예가 이 문서에 좋은 예로 적혀 있다.
- 프롬프트에도 나쁜 예(실제로 나갔던 각도)와 좋은 예를 나란히 넣었다. 코드와 프롬프트 **둘 다** 고쳐야 한다.

### 표지·본문은 후킹 한 줄로 (요청자 지적 2번)

> "후킹 멘트가 2줄이 되니깐 행렬이 디자인적으로 이쁘지가 않아."

매거진형 표지가 **세 줄에 말줄임까지** 붙어 있었다. 원인이 세 개였다.

| 어디 | 무엇이 문제였나 | 어떻게 고쳤나 |
|---|---|---|
| `outline.js` | `hook` 상한이 60자 | **26자**(`HOOK_MAX`) · 어절 중간에서 안 자르는 `trimWords()` |
| `templates.js` | `splitHeadline()` 이 쉼표만 보고 잘라 5자+22자로 갈림 | **두 줄 다 상한에 들어가는 자리**를 고른다 · 슬롯 상한 22 → **16**(`MAG_LINE_MAX`) |
| `cardrender.js` | `maxLines: 4` 라 가장 큰 글자로 네 줄까지 감았다 | `tightLines: 2` — **두 줄에 들어가는 크기를 먼저 찾는다** |

- ⚠️ **`maxLines` 를 2로 낮추지 말 것.** 낮추면 사용자가 길게 고쳐 쓴 제목이 잘려 나간다.
  `tightLines` 는 먼저 시도만 하고, 안 되면 기존 4줄 탐색으로 **그대로 떨어진다.**
  짧은 기본 문구는 두 줄로 크게, 긴 편집본은 예전처럼 감긴다.
- ⚠️ **`MAG_LINE_MAX = 16` 은 실측값이다** (MAXW 904px, Noto Sans KR 900).
  한글 한 자 폭 — 82px: 75.4 / 74px: 68.1 / 66px: 60.7 / 58px: 53.4.
  가장 작은 후보 58px 에서 904/53.4 ≈ 16자다. **올리기 전에 다시 잴 것.**
- `card` 표지의 흰 박스에서 **`product.summary` 를 뺐다.** 주제가 무엇이든 표지 절반이
  늘 같은 상품 소개였다 — 요청자 지적 4번에 정면으로 걸린다.

### 검증 (2026-08-13 실행 기준)

| 무엇 | 결과 |
|---|---|
| 카드 렌더 전수 — 상품 4 × 톤 4 × 주제 3 × 장수 6 × 템플릿 3 = **3,024장** | 예외 0 · **잘림 0** |
| 글자 축소 | 36건 (전부 매거진 표지 82→74) — **의도한 동작**이다. 세 줄로 감기는 대신 한 단계 줄인다 |
| 실패하던 그 문구 「지역 광고, 시 단위가 아니라 구 단위까지 좁힐 수 있어요」 | 3줄+말줄임 → **2줄(16자/15자)·말줄임 없음** |
| 규칙 기반 전수 — 상품 4 × 톤 4 × 주제 3 × variant 2 × 장수 3 × 채널 3 = **864건** | 금지 0 · 위험 0 · 마크다운 0 · `answer` 누락 0 |
| `validate()` 게이트 (fetch 를 가로채 canned JSON 으로 실제 경로 통과) | 실제로 나갔던 각도 **반려** · 대상을 지킨 각도 통과 · answer 누락 반려 · 무관한 각도 반려 · 방법형 주제는 과잉 반려 없음 · 긴 후킹 23자로 어절 보존 절단 |
| 앱 부팅 | 전 모듈 200 · 콘솔 오류 없음 (`config.local.js`·`/api/health` 404 는 8-7 의 정상 동작) |

> ⚠️ **실제 AI 호출로는 검증하지 못했다.** 이 PC 에 키가 없다(8-9·8-10 과 같은 상황).
> 위 게이트 검증은 `fetch` 를 가로채 정해 둔 JSON 을 흘려 **실제 코드 경로**를 통과시킨 것이다.
> 키가 있는 환경에서 반드시 확인할 것 — ① 정의형 주제에서 첫 단락이 정의로 시작하는지,
> ② 통과율이 떨어지지 않는지(반려되면 아웃라인을 한 번 더 부른다, 최대 2회).

---

## 8-12. 처음 듣는 사람 기준으로 쓰기 · 선택 칸 · 폼 배치 (2026-08-14)

요청자 지적 5가지. 셋은 화면, 둘은 글이다.

### ① 선택된 칸을 칠한다 · 브랜드 칩을 칸 안으로

색 규칙은 3절에 표로 정리했다. 여기에는 **구조**만 적는다.

브랜드 칩 묶음이 어워즈 카드 **아래에** 따로 떠 있어서 어느 유형에 딸린 것인지 흐렸다.

- ⚠️ **칩을 `<label class="channel">` 안으로 넣을 수 없다.** 칩을 눌러도 라벨이 클릭을 먼저 먹어
  유형 라디오가 다시 선택된다(2026-08-13 에 실제로 겪은 자리다).
  그래서 **테두리를 `<li class="channel-cell--group">` 로 올려** 라벨과 칩이 한 칸에 든 것처럼 보이게 했다.
  `li:has(.channel__input:checked)` 로 칸 전체가 선택 색을 입는다.
- **「어느 브랜드인가요?」 라벨은 뺐다.** 칩이 칸 안에 있으니 무엇을 고르는 자리인지 칸이 이미 말해 준다.
  ⚠️ 라벨을 없앴다고 `role="radiogroup"` 까지 없애면 안 된다 — `aria-label` 로 이름을 남겼다.
- 실제 클릭으로 확인했다: 칩을 눌러도 유형은 그대로(awards 유지, 브랜드만 kbsn→forbes),
  유형을 바꾸면 칩 묶음이 사라진다.

### ② 템플릿 카드의 「참고 · 계정명」 제거

`concept__ref` 를 화면에서 걷어냈다. 남의 인스타 계정을 우리 화면에 적어 둘 이유가 없다 —
프로필 유형 카드에서 `refs` 를 뺀 것(2026-08-13)과 같은 판단이다.

⚠️ `CONCEPTS[].ref` **값 자체는 남겼다.** 어느 계정을 보고 만든 배치인지 적어 둔 기록이다.
   화면에 되살리지 말 것.

### ③ 자리 설명을 라벨 옆으로

라벨 → 입력칸 → 설명 순이라 「계정 이름」과 「카드 맨 위 가운데」 사이를 입력칸이 갈라놓고 있었다.

- `fieldHTML()` **한 곳**만 고치면 세 템플릿이 다 바뀐다. 템플릿별로 만들지 말 것.
- 글자 수 카운터는 오른쪽 끝에 그대로 두고, 라벨+설명을 `.tpl-field__title` 로 묶어 왼쪽에 붙인다.
  ⚠️ `min-width: 0` 이 없으면 긴 설명이 카운터를 칸 밖으로 밀어낸다.
- 노트형 본문(설명 106자)처럼 긴 것은 **라벨 아래로 감긴다.** 그건 정상이다 — 칸을 넘지는 않는다.

### ④ 쓰레드 500자 → 300자

> "쓰레드는 500자에서 300자로 줄이자. 글이 너무 많다."

⚠️ **숫자가 세 곳에 있다. 하나만 고치면 경로마다 다른 길이가 나온다.**

| 어디 | 무엇 |
|---|---|
| `data/products.js` `CHANNELS` | `limit` · `limitLabel` · `hint` |
| `lib/copywriter.js` `threadsCopy()` | `clampToLimit(draft, 300)` |
| `lib/copyai.js` `channelRules()` | 프롬프트의 "300자 이내" 지시 (머리와 꼬리 두 군데) |

### ⑤ ⚠️ 읽는 사람은 **이 상품을 오늘 처음 듣는 사람**이다

요청자 지적이 정확했다. 8-11 에서 "첫 단락은 답에서 시작한다"를 넣었더니 이렇게 나왔다.

```
"영상부터 고르면 되나요?" 접수 순서가 먼저예요
15초 AI 홍보영상은 먼저 참여신청서, 사업자등록증, 수상기사 요청서를 내고 심사를 받는 과정부터 시작해요.
```

> "갑자기 서론도 없이 (…) 처음 이 상품을 듣는 사람에게는 어렵게 들려.
> 이 글을 처음 본 사람은 이 상품들에 대해 모르는 사람들이야."

**답으로 곧장 들어가는 것과, 발판 없이 세부 절차를 던지는 것은 다르다.** 앞의 것만 시키고
뒤의 것을 막지 않아서 서류 이름 셋이 첫 줄에 튀어나왔다. 왜 영상 이야기에 서류가 나오는지,
셋이 뭔지, 무엇을 심사하는지 한 줄도 없었다.

두 겹으로 막는다.

| 어디 | 무엇을 시키나 |
|---|---|
| `outline.js` `SYSTEM` | `answer` 는 **사전 지식 없이 읽어도 이해되는 문장**이어야 한다. `points[0]` 은 발판을 놓는 자리다 |
| `copyai.js` `SYSTEM` | 낯선 이름(상품·행사·패키지·**서류**)이 처음 나오면 그 자리에서 한 줄로 푼다. 나열했으면 **왜 필요한지**를 붙인다 |
| `copyai.js` 블로그 규칙 2) | 도입은 **3~4줄** — 첫 줄에 발판, 이어서 답 |

- ⚠️ **발판은 한 줄이다.** 업계 상황·시장 배경을 쌓아 올리라는 뜻이 아니다.
  그렇게 읽히면 8-9 이전의 "도입이 길다"로 되돌아간다. 프롬프트에 그 경계를 명시해 뒀다.
- ⚠️ 8-11 의 "배경부터 쌓지 마세요"를 **지우지 않았다.** 둘은 짝이다 — 발판 한 줄 + 바로 답.
  한쪽만 남기면 다시 한쪽으로 기운다.

### 검증 (2026-08-14 실행 기준)

| 무엇 | 결과 |
|---|---|
| 규칙 기반 전수 864건 (상품4 × 톤4 × 주제3 × variant2 × 장수3 × 채널3) | 금지 0 · 위험 0 · **채널 상한 초과 0** |
| 쓰레드 길이 | 196 ~ 299자 (상한 300) — 전건 통과 |
| 브랜드 칩 | 3개가 어워즈 칸 **경계 안**에 · 「어느 브랜드인가요?」 제거 · `aria-label` 유지 |
| 칩 클릭 (실제 click) | 유형 awards 유지, 브랜드만 kbsn→forbes / 유형 변경 시 칩 묶음 사라짐 |
| 선택 칸 색 | `rgb(232,242,254)` 배경 + `rgb(27,100,218)` 테두리, 선택 안 된 칸은 투명 |
| 자리 설명 | 매거진 5칸 · 카드형 3칸 · 노트형 2칸 **전부 라벨 옆** · 입력칸 아래 잔재 0 |
| 긴 설명(노트 본문 106자 · 카드 본문 75자) | 라벨 아래로 감김 · 카운터 칸 밖으로 안 밀림 |
| `참고 · 계정명` | `.concept__ref` 0개 · 화면 텍스트에 "참고 ·" 0건 |
| 명도 대비 전수 — 프로필·홈·4단계 | **전부 실패 0** (선택 칸 안 제목 9.72 / 설명 4.80) |
| 375px 가로 스크롤 | 3개 화면 전부 375/375 |
| 앱 부팅 | 전 모듈 200 · `config.local.js`·`/api/health` 404 만 (8-7 정상 동작) |

> ⚠️ **AI 경로는 이번에도 실제 호출로 검증하지 못했다** (이 PC 에 키 없음).
> ⑤ 는 프롬프트 변경이라 **실제 호출로만 확인된다.** 키가 있는 환경에서
> 『ai 홍보 영상 신청 방법』으로 돌려 도입에 발판 한 줄이 서는지 반드시 볼 것.
>
> ⚠️ 화면은 **눈으로 보지 못했다.** 로그인 게이트가 있어 페이지 모듈을 떼어 내 그린 뒤
> 좌표·계산값으로 확인했다. 실제 화면에서 한 번 볼 것.

---

## 8-13. 해시태그·마무리 문장 확장 · 제목 정리 (2026-08-14)

요청자 목표: **"남들이 이 화면에 들어와서 SNS에 활용하기 좋은 글을 잘 얻어갔으면 좋겠다."**
그 기준으로 보니 세 가지가 걸렸다. 승인 목록 확장은 요청자가 위임했다("네가 알아서 승인 더 해줄 테니").

### ① 인스타 해시태그가 사실상 고정이었다

태그가 상품당 **5개인데 인스타가 5개를 쓴다.** 그래서 순서만 돌 뿐 **늘 같은 태그**가 나갔다
(`tagsFor` 의 "매번 다르게 고른다"는 주석과 실제가 달랐다). 게다가 전부 브랜드 이름이라
**이 브랜드를 이미 아는 사람에게만 닿았다** — 새 유입 경로가 0이었다.

세 곳을 함께 고쳐야 실제로 갈린다.

| 어디 | 무엇 |
|---|---|
| `data/products.js` | 5 → **14개**. 앞쪽은 브랜드, 뒤쪽은 **탐색 태그**(브랜드마케팅·자영업자·광고제작 …) |
| `tagsFor()` | **대표 태그(첫 번째)는 늘 넣고 나머지만 돌린다.** 브랜드로 찾아오는 길을 끊지 않는다 |
| `tagSeed()` | 씨앗에 **주제**를 넣는다. 톤+variant 만 쓰면 값이 0~4 다섯 가지뿐이라 뒤쪽 태그가 한 번도 안 나간다 |

- ⚠️ **태그도 사실성 원칙 안에 있다.** 비용·성과·최고/1위를 암시하는 태그를 넣지 말 것.
  AI TV CF 에는 특히 **'무료·지원금' 계열 태그를 넣지 않는다** — 정부 지원으로 오인된다(4절).
- ⚠️ 씨앗은 **결정적**이어야 한다. 같은 주제·톤이면 늘 같은 태그가 나와야 다시 열었을 때 글이 안 바뀐다.
- 실측: 풀 14 → **세트 13종 · 14개 태그 전부 등장 · 안 나온 태그 0** (고치기 전에는 세트 3종).

### ② 모든 글이 두 문장 중 하나로 끝났다

`closings` 가 상품당 2개뿐이었다. 소제목은 매번 새로 짓게 고쳐 놨는데(8-10) 정작
**마지막 문장은 둘 중 하나**라, 글을 열 편 뽑으면 다섯 편씩 똑같이 끝났다.

2 → **5개**로 늘렸다. 각도를 나눠 뒀다 — 신뢰 근거 / 상황별 활용 / 특전 활용 / 접수 시점 / 판단 순서.

- ⚠️ **늘릴 때 지키는 선은 그대로다.** 성과·매출을 보장하지 않고, 비용을 꺼내지 않고,
  자료에 있는 사실만 근거로 쓴다. 새로 쓴 문장도 `findBanned`·`findRisky`·비용 표현 검사를
  **원문 그대로** 통과시켰다(상품 원문 제외 없이 날것으로 검사).
- ⚠️ `buildCore()` 의 씨앗도 함께 고쳐야 한다. `pick(p.closings, variant)` 는 variant 가
  0·1 뿐이라 **목록을 늘려도 앞의 두 개만 계속 썼다.** 후킹과 같은 `TONE_HOOK[tone] + variant` 로 맞췄다.
  (AI 경로는 목록 전체를 프롬프트에 주므로 모델이 직접 고른다 — 이 줄과 무관하다.)

### ③ 블로그 제목이 그냥 이어 붙인 형태였다

`${p.short} ${topic}` 한 줄이라 두 가지가 어설펐다.

```
주제 『ai 홍보 영상 신청 방법』      → "KBS N ai 홍보 영상 신청 방법"      (소문자가 그대로)
주제 『KBS N 브랜드어워즈 특전』     → "KBS N KBS N 브랜드어워즈 특전"     (상품명이 두 번)
```

`blogTitle()` 이 **중복 제거**와 **아는 약어의 대소문자**만 만진다.

- ⚠️ **담당자가 쓴 말을 고쳐 쓰지 않는다.** 손대는 것은 표기뿐이고, 주제를 다시 해석하는 일은
  `lib/outline.js` 가 한다. 약어도 `ACRONYMS` 목록에 있는 것만 올린다.
- ⚠️ 낱말 속은 건드리지 않는다 — `aircraft`·`native` 는 그대로다(실측 확인).
- AI 경로는 모델이 제목을 직접 쓰므로 이 함수를 타지 않는다. **규칙 기반 전용 손질**이다.

### 검증 (2026-08-14 실행 기준)

| 무엇 | 결과 |
|---|---|
| 규칙 기반 전수 **1,536건** (상품4 × 톤4 × 주제8 × variant2 × 장수2 × 채널3) | 금지 0 · 위험 0 · 채널 상한 초과 0 |
| 새 마무리 문장 20개 · 새 태그 56개를 **원문 그대로** 검사 | 금지 0 · 위험 0 · 비용 표현 0 · 위험 태그 0 |
| 해시태그 | 상품마다 세트 **13종**, 태그 **14/14 전부 등장**, 미등장 0 |
| 마무리 문장 | 상품마다 **5개 전부 사용**됨 (고치기 전 2개) |
| 같은 입력 → 같은 출력 | 3회 생성 결과 완전 일치 (결정적) |
| 제목 | 소문자 약어 교정 · 상품명 중복 제거 · 낱말 속 오작동 0 |
| 앱 부팅 | 전 모듈 200/304 · `config.local.js`·`/api/health` 404 만 (8-7 정상 동작) |

> ⚠️ **남은 가장 큰 것은 접근성이다.** "남들이 이 화면에 들어와서"가 지금은 불가능하다 —
> 배포하지 않기로 했고(8-7) 키도 각자 발급이라, 쓰려면 clone → Python → 서버 실행 → 키 발급을
> 거쳐야 한다. 로그인·승인 코드(11절)와 `vercel.json`·`api/`(8-6)는 **이미 다 만들어져 있고 안 쓴다.**
> 남들이 쓰게 하려면 그 두 결정을 다시 여는 것이 먼저다 — 글 다듬기보다 앞선다.

---

## 8-14. 분량 회계 · 문단마다 구체 하나 · 마케팅 관점 (2026-08-14)

요청자 판단: **"글 양도 나쁘지 않아. 중요한 건 주제를 넣었을 때 그 주제에 해당되는 내용과
결과값이 잘 나오냐다. 자료가 있는데 안 쓴다니, 전문 블로거라고 생각하고 주제에 대해
이야기한 다음 마케팅적으로 잘 이야기해봐."**

### ⚠️ ① 분량 숫자가 본문을 가리키지 않았다

`BLOG_PLAN` 의 `min`·`max` 는 글 **전체** 글자 수였다. 그런데 실측해 보니 절반이 본문이 아니었다.

```
1,138자짜리 글 = 본문 570(50%) + 📷 199(17%) + ⤷ 캡션 157(14%) + 개요표·태그·소제목 212(19%)
```

📷 줄과 캡션은 **붙여넣고 나면 이미지로 바뀌거나 지워지는** 줄이다. 그걸 글자 수에 넣고
"1,200~1,600자 안에 쓰라"고 시키니 모델이 그만큼 본문을 스스로 깎았다.
게다가 📷 줄은 장수에 비례해 길어져서 **장수가 늘수록 본문 예산이 조용히 줄었다.**

이제 `min`·`max` 는 **본문 기준**이고, 프롬프트에도 "📷·⤷·🔔·해시태그는 세지 않는다"를 못박았다.

- ⚠️ **글을 늘리려고 바꾼 것이 아니다.** 요청자가 지금 분량이 괜찮다고 했다.
  값은 지금 실제로 나오는 본문 길이에 맞춰 잡았다 — 화면에 보이는 총 길이는 그대로다.
  바뀐 것은 **숫자가 무엇을 가리키는지**뿐이고, 그래야 장수를 바꿔도 본문이 흔들리지 않는다.
- `bodyLength()`(copywriter.js) 하나가 그 정의를 갖는다. **계획·프롬프트·화면 카운터가 같은 정의를 본다.**
- ⚠️ `bodyLength()` 는 **빈 줄을 남긴다.** 빈 줄까지 걷어내면 뺄 것이 하나도 없는 글에서도
  본문과 전체가 달라져서, "둘이 같으면 한 숫자만" 하는 판단이 영영 안 맞는다(실제로 그렇게 났다).

### ② 자료가 있는데 안 썼다 — 문단마다 구체 하나

상품마다 사실이 12~16개인데 글에는 1~7개만 나갔다(반영률 약 30%). 남은 문단은 이랬다.

> "지역 단위 타겟팅이 가능합니다."
> "AI 기반으로 CF를 제작하고 그대로 IPTV 송출까지 연결됩니다."

**기능 설명이지 정보가 아니다.** 읽고 나서 새로 아는 게 없다.

원인은 세 겹으로 조여 있었기 때문이다 — `coreBlock` 의 "한 항목당 3~5줄", `A_MAX = 160`,
그리고 8-8 에서 뺀 근거 문장. 셋 다 "글이 길다"는 지적에 대한 대응이었는데,
**잘려 나간 것이 군더더기가 아니라 근거였다.**

| 어디 | 무엇을 시키나 |
|---|---|
| `outline.js` | `a` 는 두 부분 — ① 자료의 구체 하나(숫자·기간·조건·대상·절차·범위) ② 그래서 무엇을 정하면 되는지 |
| `copyai.js` `SYSTEM` | 「밀도」 절 — 구체 없는 문단은 실패. "가능합니다/연결됩니다"만 있는 문단은 기능 설명이다 |
| `copywriter.js` `blogCopy()` | 규칙 기반도 문단마다 근거 한 줄을 **되살렸다**(아래 참고) |

### ③ 마케팅 관점 — 사실 다음에 '그래서 무엇을 정하면 되는지'

화자를 **"이 분야를 오래 다뤄 온 전문 블로거"** 로 올리고, 문단의 진행을 못박았다.

```
사실 → 브랜드 입장에서 무슨 의미인지 → 그래서 무엇을 정하면 되는지
```

프롬프트에 좋은 예/나쁜 예를 나란히 넣었다(지역 타겟팅 예시).

- ⚠️ **성과를 약속하는 것과는 전혀 다르다.** "매출이 오릅니다 / 효과가 있습니다 / 문의가 늘어납니다"
  는 절대 규칙 2번 위반이다. 해도 되는 것은 **선택지와 판단 기준을 알려 주는 것까지**다.
  이 경계를 프롬프트 세 곳에 모두 적어 뒀다 — 안 적으면 모델이 바로 성과 약속으로 넘어간다.

### ④ 규칙 기반의 근거 한 줄 — 8-13 의 결정을 조건부로 되돌렸다

> ⚠️ 8-8 의 "근거 문장을 덧대지 않는다"(2026-08-13)는 **이 절로 대체됐다.**

예전처럼 무조건 붙이지 않는다. `concreteFor()` 가 **구체(숫자·기간·조건)가 든 문장만,
문단당 하나만, 이미 말한 것과 겹치지 않을 때만** 고른다. 문단은 한 줄 늘고 알맹이가 생긴다.

⚠️ `CONCRETE_MARK` 에 형용사("우수한", "다양한")를 넣지 말 것 — 그건 구체가 아니라 수식이다.

### ⑤ 화면 카운터를 나눴다

```
블로그        본문 811자 · 전체 1,268 / 2,000자
인스타·쓰레드  480 / 700자          (한 숫자 그대로)
```

- ⚠️ **블로그에서만 나눈다.** 사라지는 줄이 있는 채널이 블로그뿐이다.
  인스타의 해시태그는 붙여넣으면 그대로 남는 글이라 빼면 오히려 헷갈린다.
- ⚠️ 상한(`c.limit`) 판정은 **전체 기준 그대로**다. 붙여넣기 전 글 전체의 안전망이라 바꾸지 않았다.

### 검증 (2026-08-14 실행 기준)

| 무엇 | 결과 |
|---|---|
| 규칙 기반 전수 **2,304건** (상품4 × 톤4 × 주제8 × variant2 × 장수3 × 채널3) | 금지 0 · 위험 0 · 채널 상한 초과 0 |
| 본문 밀도 | 6장 본문 **570 → 777자**, 전체 1,138 → 1,303자 (요청자가 괜찮다고 한 1,290 수준) |
| 자료 숫자 반영률 | **약 30% → 70%** |
| 구체 없는 소제목 문단 | **0건** (전수 확인) |
| 계획 상한 안에 드는가 | 768/768 |
| 카운터 | 블로그만 본문·전체 분리 · 뺄 것 없는 글은 본문 == 전체(검산 통과) |
| 앱 부팅 | 전 모듈 200/304 · `config.local.js`·`/api/health` 404 만 (8-7 정상 동작) |

> ⚠️ **②·③ 은 프롬프트 변경이라 실제 AI 호출로만 확인된다.** 이 PC 에 키가 없다.
> 위 수치는 규칙 기반 경로 실측이다. 키가 있는 환경에서 확인할 것 —
> ① 문단마다 구체가 들어가는지, ② "그래서 무엇을 정하면 되는지"까지 가는지,
> ③ 그러면서 성과 약속으로 넘어가지 않는지(`findRisky` 가 잡지만 통과율이 떨어질 수 있다).

---

## 8-15. 블로그를 카드에서 떼어냈다 (2026-08-20)

요청자 지적: **"블로그 내용이 부족하다."**

### ⚠️ 이 자리는 두 번 뒤집혔다 — 순서를 알고 손댈 것

| 시점 | 규칙 | 그때 나온 지적 |
|---|---|---|
| ~2026-08-13 | 장수와 무관하게 1,800~2,400자 | "한 섹션 내용이 너무 많아 안 읽힌다" |
| 2026-08-13 (8-14) | 장수에 맞춰 축소 — 6장 800~1,100자·소제목 3개 | **"블로그 내용이 부족하다"** |
| **2026-08-20 (지금)** | **총량은 되돌리되, 섹션은 오히려 더 잘게** | — |

**두 지적은 상충하지 않는다.** 앞의 것은 *한 덩어리가 길다*는 말이고 뒤의 것은 *전체가 얇다*는 말이다.
그래서 총량을 늘리면서 **소제목을 3 → 4개로 쪼개고** 소제목당 문단 2~3개 × 3~5줄로 묶었다.
글은 길어지되 **한 번에 읽는 덩어리는 오히려 짧아진다.**

### 무엇이 원인이었나 — 블로그가 카드에 종속돼 있었다

`BLOG_PLAN` 이 **카드 장수를 키로** 분량과 소제목 수를 정했다. 6장을 골라도 본문 1,100자·소제목 3개다.
소제목당 250자면 사실을 나열하다 끝난다. 논증이 들어갈 자리가 없다.

**순서를 뒤집었다. 블로그가 원문이고 카드가 그 원문을 잘라 나르는 것이다**
(팩트시트 `0-11` 원문 우선 규정, 카드뉴스 스킬과 같은 규정).

| | 예전 | 지금 |
|---|---|---|
| 블로그 분량 | 장수를 따라감 (600~1,100) | **고정 1,400~2,000자** |
| 소제목 수 | 장수를 따라감 (2~3) | **고정 4개** |
| 카드 장수가 정하는 것 | 분량·소제목·이미지 자리 | **📷 이미지 자리 개수만** |

### 고친 곳 — ⚠️ 다섯 군데를 함께 봐야 한다

| 어디 | 무엇 |
|---|---|
| `lib/copywriter.js` `BLOG_PLAN` | 전 장수 `{ points: 4, min: 1400, max: 2000 }` |
| `lib/copyai.js` 블로그 규칙 4) | 소제목당 **문단 2~3개** — 한 문단으로 끝내면 사실 나열이 된다 |
| `lib/copyai.js` 이미지 절 | "카드가 적으면 글도 짧아야 합니다" **삭제** — 상충 지시였다 |
| `lib/copyai.js` 분량 절 | "짧게 쓰는 것이 목표" → "인스타·쓰레드가 잘라 쓸 원문이다" + **자료가 모자라면 늘리지 말고 짧게 끝낸다** |
| `data/channels.js` | `limit` 2000 → **3600** (전체 기준 안전망), 라벨도 함께 |

⚠️ **`promptKeyOf()` 의 블로그 버전을 `v7` → `v8` 로 올렸다.** 안 올리면 저장된 글이 옛 구조 그대로 남는다.

⚠️ **`A_MAX`(160)·`Q_MAX`(28)는 건드리지 않았다.** 그건 **카드 슬롯 상한**이고
`deckFromCore()` 가 `points[].q/a` 를 카드 제목·본문으로 그대로 쓴다. 늘리면 카드가 넘친다.
블로그를 더 두껍게 하려면 아웃라인에 **블로그 전용 필드를 따로** 만들어야 한다 — 여기를 늘리는 게 아니다.

⚠️ **비용**: 카드를 1장만 골라도 블로그 토큰은 그대로 든다. 장수로 비용을 아끼던 효과가
이미지 쪽에만 남는다. 문제가 되면 `BLOG_PLAN` 의 `min`/`max` **한 곳만** 낮춘다.

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 모듈 로드 (`copywriter`·`copyai`·`channels`) | 전부 ok · 앱 부팅 전 모듈 200 |
| 콘솔 오류 | `config.local.js`·`/api/health` 404 **둘뿐** (8-7 정상 동작) |
| `blogPlanFor(1~6)` | 전부 `points 4 / 1400~2000` |
| `buildDeck()` 카드 구성 (장수 1~6) | 예전과 동일 — 본문 카드는 `points[0..2]` 만 쓴다. **4번째 항목은 블로그에만 간다** |
| 채널 라벨 | 「본문 기준 1,400~2,000자 · 카드 장수와 무관」 |

> ⚠️ **규칙 기반 전수 검사와 실제 AI 호출은 못 했다.**
> 상품이 `data/products.js` → **Supabase 로 옮겨져** 로그인해야 불러와진다(`lib/products.js`).
> 로그인 없이는 `PRODUCTS` 가 빈 배열이라 8-14 까지 하던 2,304건 전수 검사를 돌릴 수 없다.
> 위 검증은 합성 코어로 카드 경로만 확인한 것이다.
> **로그인한 환경에서 반드시 확인할 것** — ① 본문이 실제로 1,400자를 넘는지,
> ② 소제목 4개가 각각 2~3문단으로 서는지, ③ 문단마다 구체가 남아 있는지(8-14 ②가 안 깨졌는지).

---

## 8-16. 카드가 본문을 요약하게 · 다시 생성이 같은 글을 내지 않게 (2026-08-20)

요청자 지적 두 가지. **둘 다 원인이 코드에 있었다.**

### ① "템플릿에 들어가는 내용이 본문 요약이 아니라 다른 내용 같다"

원인이 **두 겹**이었다.

**첫째 — 카드가 블로그가 아니라 뼈대를 보고 있었다.**

```
예전:  카드 제목 = core.points[].q        (아웃라인 뼈대)
       카드 본문 = core.points[].a 인데 첫 문장만 블로그 캡션으로 교체  ← 섞임
```

블로그는 뼈대를 받아 **AI 가 자기 문장으로 다시 쓴다.** 그러니 화면의 글과 카드의 글이 서로
다른 말을 했고, 캡션 + 뼈대 나머지가 붙은 자리는 앞뒤가 안 맞았다.

이제 `blogCardSource()`(`pages/template.js`)가 **완성된 블로그에서 직접** 뽑는다.
대응 규칙은 앱이 이미 정해 둔 것 그대로다 — 블로그 프롬프트가 `📷` 줄을
"그 카드가 대응하는 단락 **바로 뒤**"에 놓게 하므로, `📷 [이미지 N]` 기준으로
**바로 위 `■ 소제목`** 과 **바로 위 문단**이 곧 N번 카드의 재료다.

| 카드 | 제목 | 본문 |
|---|---|---|
| 표지 | 첫 `⤷` 캡션 | 건드리지 않음(주제) |
| 본문·반론 | 그 자리의 `■ 소제목` | `📷` 바로 위 문단 |
| 마무리 | 건드리지 않음 | 건드리지 않음 — **승인된 마무리 문장**이다(사실성 원칙 4번) |

- ⚠️ **빈 줄에서 문단을 버리면 안 된다.** `📷` 줄 앞에는 빈 줄이 하나 들어간다.
  버리면 카드가 늘 빈 문단을 집는다(만들면서 실제로 그렇게 났다). `lastPara` 로 직전 문단을 들고 있는다.
- ⚠️ 목차·개요표·해시태그·구분선·인용 요약(`[테이블`·`🔔`·`#`·`─`·`>`·`목차`·`1. `)은 문단에 넣지 않는다.
- `📷` 줄이 없는 글(규칙 기반·옛 보관본)은 **원래 카드를 그대로 둔다.** 앱이 멈추면 안 된다.

**둘째 — 글을 다시 뽑아도 카드가 옛 문구를 들고 있었다.**

`draftKeyOf()` 는 **상품·주제·톤·장수**만 본다. AI 2 를 뽑아도 그 넷은 그대로라
`ensureTexts()` 가 `card.key === key` 로 판단해 `base` 만 갈고 `texts` 는 그대로 뒀다.

이제 `state.card.source` 에 **블로그 지문**(`blogFingerprint()`)을 함께 남긴다.
지문이 다르면 `mergeTexts()` 로 다시 맞춘다 — **직접 고친 칸은 지키고, 손대지 않은 칸만**
새 글의 문구로 바뀐다(템플릿 교체와 같은 규칙).

### ② "AI로 다시 생성했는데 같은 내용이 나온다"

원인이 **두 겹**이었다.

**첫째 — 라운드가 4에서 한 바퀴 돌아 "지시 없음"으로 되돌아왔다.**

`ROUND_OPENING`(copyai) 과 `ROUND_ANGLE`(outline) 은 **0번 칸이 빈 문자열**이고
`list[round % list.length]` 로 골랐다. 길이가 4라 **round 4 = 0번 칸**이다.
5번째 글의 프롬프트가 첫 글과 **글자 하나 안 달랐다.** 같은 글이 나오는 게 당연하다.

```js
// 0번은 첫 글 전용. round >= 1 은 1번 칸 이후에서만 돈다.
const roundPick = (list, round) => (round > 0 ? list[1 + ((round - 1) % (list.length - 1))] : list[0]);
```

두 배열에 각도를 3개씩 더 넣어 **한 바퀴가 6라운드**가 됐다. ⚠️ **0번 칸을 지우지 말 것** —
첫 글은 각도 지시 없이 주제에 곧장 답해야 한다.

**둘째 — 피해야 할 소제목을 직전 한 라운드만 넘겼다.**

`avoid` 가 지금 들고 있는 뼈대의 소제목뿐이라, AI 4 는 AI 3 만 피하면 됐고
AI 1 과 같은 구성으로 되돌아가도 막을 게 없었다.
이제 `state.outline.pastHeads` 에 **그 주제로 만든 모든 라운드의 소제목**을 쌓아 넘긴다.

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 모듈 로드(copywriter·copyai·outline·template·copy) | 전부 ok |
| `blogCardSource()` — 실제 서빙 소스를 떼어내 샘플 블로그 파싱 | 카드 3장 전부 **소제목·문단·캡션 정확히 대응** · 목차/해시태그/인용 혼입 0 |
| 라운드 매핑 round 0~9 | round ≥ 1 에서 **0번 칸으로 돌아오는 경우 0건** (예전 규칙은 round 4·8 에서 0번) |
| 옛 `[round % length]` 잔존 | 주석 1곳뿐, 코드 경로 0 |

> ⚠️ **실제 AI 호출로는 검증하지 못했다.** 상품이 Supabase 로 옮겨져 로그인해야 불러와진다.
> 로그인한 환경에서 확인할 것 — ① 카드 제목이 블로그 소제목과 같은지,
> ② 「AI 생성」을 5번 눌렀을 때 1번째와 5번째가 다른지, ③ 손으로 고친 카드 문구가 재생성 후에도 남는지.

---

## 8-17. 인용구 메타 문장 · 이미지 위치 · 노트형 글씨 색 (2026-08-20)

요청자 지적 3건.

### ① "인용구 안의 말이 무슨 말인지 모르겠다"

나온 글: `"그거 대기업만 되는 거 아니에요?"라는 질문부터 답합니다.`

인용구 둘째 줄(후킹)에 **글이 자기 이야기를 하는 문장**이 들어갔다. 읽는 사람은 두 줄만 보고
계속 읽을지 정하는데, 글이 앞으로 무엇을 할 예정인지는 알 바가 아니다. 읽어도 새로 아는 게 없다.

프롬프트 1)번에 좋은 예/나쁜 예를 넣었다. 금지 꼬리: `~부터 답합니다` `~를 알아보겠습니다`
`~를 다룹니다` `~를 정리했습니다` `이 글에서는`.
⚠️ 큰따옴표로 남의 말을 옮길 거면 **그 말만 쓰고 끝낸다.** 뒤에 진행 설명을 붙이지 않는다.

### ② "이미지가 그 내용 뒤에 붙는다"

`이미지 2 · 본문` 캡션이 "중소기업도 심사에 참여할 수 있어요"인데 그 내용을 **다 말한 뒤**에
이미지가 놓였다. 그러면 이미지가 다음 소제목을 설명하는 것처럼 읽힌다.

**이미지는 그 자리의 이야기를 여는 그림이다.** 배치를 뒤집었다.

```
전:  ■ 소제목 → 본문 문단 → 📷 → ⤷ 캡션
후:  ■ 소제목 → 📷 → ⤷ 캡션 → 본문 문단
```

⚠️ **`lib/copyai.js` 의 배치 규칙과 `pages/template.js` 의 파서를 같이 고쳐야 한다.**
프롬프트만 고치면 파서가 여전히 위 문단을 집어 카드 본문이 어긋난다.
`blogCardSource()` 는 이제 **아래 문단을 먼저** 보고, 없으면 위 문단으로 떨어진다
(옛 글·보관본은 이미지가 문단 뒤에 있어서 그때는 위가 맞다). 두 배치 모두 실측으로 확인했다.

### ③ 노트형 글씨 색을 고를 수 있게

종이 색은 고를 수 있는데 글씨는 검정 고정이라 짝이 안 맞았다.

`NOTE_INKS` 6종(검정·차콜·네이비·브라운·딥그린·와인) + `#RRGGBB` 직접 선택.
`state.noteInk` 한 곳이 모든 장에 걸린다(카드형 테마 색과 같은 규칙).

⚠️ **고르는 건 잉크 하나뿐이다.** 제목·본문·부제·강조 박스를 따로 고르게 하면 조합이 무너진다.
`noteLayout()` 이 하나에서 파생시킨다 — 제목·강조는 잉크 그대로, 본문은 `lighten(0.18)`,
부제는 `lighten(0.28)`.

⚠️ **비율은 실측으로 정했다.** 잉크 6 × 종이 3 = **18조합의 최악값** 기준이다.
처음 잡은 0.22/0.38 은 검정 외 부제가 3.84~4.10 으로 미달했다 — 시작 색이 `#111111` 보다
밝으면 같은 비율이라도 더 밝게 떨어지기 때문이다.

| 비율 | 18조합 최악 대비 |
|---|---|
| 0.18 (본문) | 6.40:1 |
| 0.28 (부제) | 4.65:1 |
| 0.30 | 4.38:1 ← 기준 미달 |

**올리기 전에 18조합을 다시 잴 것.** 직접 선택 색은 보장하지 않는다 — `contrastOn()` 으로
경고만 하고 막지 않는다(요청자 지시). 대비 기준이 **흰색이 아니라 지금 고른 종이 색**이다.

`contrastWithWhite()` 는 `luminance()` 를 떼어내 `contrastOn(fg, bg)` 과 나눠 쓴다.

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 모듈 로드 7개 · 새로고침 후 404 | 전부 ok · `config.local.js`·`/api/health` 둘뿐 |
| 노트형 잉크 6 × 종이 3 = 18조합 명도 대비 | **실패 0** (최악 제목 11.35 / 본문 6.40 / 부제 4.65) |
| 렌더 실측 — 가장 어두운 픽셀 | black `rgb(17,17,17)` · wine `rgb(74,29,40)` · navy `rgb(27,42,74)` · 직접 입력 `#B00020` 반영 확인 |
| `blogCardSource()` 새 배치 | 카드마다 **소제목 + 아래 문단** 정확히 대응 |
| `blogCardSource()` 옛 배치 | 위 문단으로 폴백 — 옛 보관본도 안 깨진다 |

> ⚠️ **①②는 프롬프트 변경이라 실제 AI 호출로만 확인된다.** 상품이 Supabase 로 옮겨져
> 로그인해야 불러와진다. 로그인한 환경에서 볼 것 — ① 인용구 둘째 줄이 사실 문장인지,
> ② `📷` 줄이 소제목 바로 아래에 오는지.

---

## 8-18. 블로그 품질이 안 오르던 진짜 이유 — 서로 싸우는 지시 (2026-08-20)

요청자: **"글자 수를 늘려줬는데도 내용이 너무 별로다. 돈 내고 쓰는데 이런 글 보려고 하는 게 아니다."**

코드를 다시 훑어 원인을 넷 찾았다. **첫 번째가 결정적이다.**

### ① 분량을 두 곳에서, 서로 반대로 말하고 있었다 ⭐

8-15 에서 `BLOG_PLAN` 을 1,400~2,000자로 올렸다. 그런데 `coreBlock()`(copyai.js)에는 이게 남아 있었다.

```
"핵심만 짧게 씁니다. 한 항목당 3~5줄이면 충분합니다."
"배경 설명·부연으로 분량을 늘리지 마세요."
"다른 항목을 새로 만들거나 순서를 바꾸지 마세요 — 카드뉴스 2·3·4번과 1:1로 맞물립니다."
```

`SYSTEM` 의 「깊이」 절에도 같은 말이 있었다 — "길게 쓰라는 뜻이 아닙니다. 한 항목당 3~5줄이면 충분합니다."

**4항목 × 5줄이면 800자도 안 된다.** 모델은 "1,400~2,000자를 쓰되 항목당 3~5줄만 쓰고
늘리지 말고 항목도 추가하지 말라"는 **모순된 지시**를 받았다. 분량을 채우려면 규칙을 어겨야 하고,
안 어기려면 물을 타야 한다. 그래서 **길이만 늘고 내용은 그대로**였다.

**분량은 `channelRules()` 의 「분량」 절 한 곳에서만 말한다.** `coreBlock` 과 `SYSTEM` 에서 걷어냈고,
항목을 "목차"가 아니라 **"재료"** 로 격하해 항목 안에서 얼마나 깊이 팔지는 모델이 정하게 했다.

⚠️ **다시 넣지 말 것.** 분량 지시를 두 곳에 두면 반드시 한쪽이 이기고, 그때부터 다른 쪽 숫자는 거짓말이 된다.

### ② 지시가 1.3만 자인데 추론은 `low` 였다

| 무엇 | 크기 |
|---|---|
| `SYSTEM` | **4,595자 / 111줄** (8-2-1 실측 당시 945자 → 5배로 자랐다) |
| `channelRules()` 블로그 파트 | **6,550자** |
| `factSheet()` | 상품마다 수천 자 |
| 합계 | **1.3만~1.5만 자 · 지켜야 할 제약 50개 이상** |

여기에 `reasoning: { effort: 'low' }` 가 걸려 있었다(전 채널 공통, 비용 통제 8-2-1).
**모델의 여력이 '좋은 글'이 아니라 '규칙 준수'에 전부 쓰인다.**

`REASONING_BY_CHANNEL = { blog: 'medium' }` 으로 **블로그만** 올렸다.
인스타·쓰레드는 형식이 단순하고 짧아 `low` 로 충분하다.
⚠️ 추론 토큰은 출력 요금이다. 대신 반려·재시도(최대 3회)가 줄어 실제 차이는 이보다 작다.

### ③ 새 정보가 시스템에 들어올 경로가 없다 (미해결)

사실은 Supabase 상품 자료 안에서만 쓸 수 있다(사실성 원칙 1번). 상품당 사실이 12~16개다.
2단계 「스타일 수집」은 남의 블로그를 읽지만 **"제품·주제·사실·예시는 가져오지 마세요"** 로
내용 반입을 명시적으로 막는다 — 문체만 가져온다.

**즉 같은 상품이면 같은 15개 사실을 영원히 돌려쓴다.** 좋은 블로거는 재료가 많아서 좋은 글을 쓴다.
프롬프트를 아무리 고쳐도 이 천장은 안 뚫린다. 뚫으려면 둘 중 하나다.

- `product_proofs` 에 사실을 계속 추가한다 (운영 작업, 코드 아님)
- 스타일 수집에서 **사실이 아니라 "사람들이 실제로 묻는 질문"만** 가져오게 연다 (사실은 여전히 자료에서)

### ④ 고쳐 쓰는 단계가 없다 (미해결)

`validateDraft()` 는 금지어·주제 이탈·길이·형식만 본다. **글이 좋은지는 아무도 안 본다.**
통과하면 그대로 화면에 나간다. 초안 → 자기 검수 → 고쳐 쓰기 1회를 넣으면 품질이 가장 크게 오르지만
**호출이 2배**라 요청자 결정이 필요하다.

### 함께 고친 것

**후킹 줄이 딱딱했던 이유** — 인용구 둘째 줄이 "제목"이라는 이유로 「말투」 규칙 밖에 있었다.
그래서 `"...아니에요?"라는 질문부터 답합니다.` 같은 문어체가 나왔다. 요청자 지적:
"질문부터 답합니다 라는 말이 이상하지 않아? AI 티 나고 블로거스럽지 않다."
둘째 줄에도 말투 규칙을 적용하고, 좋은 예/나쁜 예를 넣었다.

**이미지 순서** — `📷 → ⤷ 캡션 → ## 소제목 → 본문 문단` 으로 확정(요청자 지시).
8-17 에서 `소제목 → 📷 → 본문` 으로 넣었던 것을 바로잡았다.

> ### ⚠️ `HEAD_MARK` 는 `■` 가 아니라 **`##`** 이다
> 화면(`previewHTML()`)에서만 ■ 로 보인다. 실제 원고에는 `## 소제목` 으로 들어간다.
> `blogCardSource()` 의 잡줄 필터가 해시태그를 잡으려고 `#` 로 시작하는 줄을 버렸는데,
> **소제목까지 함께 삼켰다.** 카드 제목이 영영 안 잡히고 늘 폴백으로 떨어졌다.
> 필터는 반드시 `!isHead(t) && ...` 로 소제목을 먼저 빼고 판단할 것.

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 모듈 로드 5개 | 전부 ok |
| `blogCardSource()` 새 배치(📷→캡션→소제목→문단) | 카드마다 **아래 소제목 + 그 첫 문단** 정확히 대응 |
| `blogCardSource()` 옛 배치 | 위 소제목·위 문단으로 폴백 — 보관본이 안 깨진다 |
| `HEAD_MARK` 실측 | `##` (코드포인트 23 23) · 해시태그 필터에 걸리던 것 해소 |

> ⚠️ ①②의 효과는 **실제 AI 호출로만 확인된다.** 로그인이 필요해 이 PC 에서는 못 돌렸다.
> 돌려 보고도 부족하면 다음 손댈 곳은 ③(재료)과 ④(고쳐 쓰기)다. **프롬프트가 아니다.**

---

## 8-19. 고쳐 쓰기 1회 · 도입 규칙 폐기 (2026-08-20)

요청자 지적이 정확했다. **이 절의 전제는 이것이다.**

> "그냥 AI한테 글 써 달라고 하면 핵심만 딱딱 말해주는데, 이렇게 제약했다고 못 쓴다는 것도 말이 안 되는 것 같아."

맞다. 모델 능력이 모자란 게 아니다. **1.3만 자짜리 규칙이 글을 눌러 놓은 것**이다.
8-18 ②에서 "추론이 low라서"라고 쓴 것은 절반만 맞고 절반은 변명이었다.
규칙을 더 붙여서 풀 문제가 아니라 **덜어내야 풀리는 문제**다.

### ① '고리타분한 도입'의 출처를 찾아 폐기했다

`channelRules()` 블로그 2)번이 **매번 같은 도입을 만들라고 명시적으로 시키고 있었다.**

```
첫 문단은 일상의 선택이나 상황에 빗대어 문을 엽니다.
둘째 문단은 사람·브랜드·상황마다 맞는 답이 다르다는 점을 보여 줍니다.
셋째 문단은 이 글에서 비교·설명·정리할 범위를 분명히 말합니다.   ← 여기가 "질문부터 답합니다"의 출처
```

셋 다 지웠다. **2~4줄로 줄이고, 묻는 것을 받아 바로 답하게** 했다.
금지 목록에 비유 도입·"상황마다 다릅니다"·글이 자기 진행을 설명하는 문장을 명시했다.

⚠️ 8-17 ①에서 인용구 둘째 줄만 고쳤던 것은 **증상 처치였다.** 원인은 이 2)번 규칙이었다.

### ② 소제목이 독자의 질문 순서를 따라가게

요청자가 원하는 모양을 그대로 넣었다.

```
주제 『A가 뭐야』 → A가 뭘까요 → A는 이겁니다 → A 할 때 이건 주의하세요 → A 하는 자세한 방법
```

한 소제목을 읽고 나면 다음이 궁금해져야 한다. 사전식 항목 나열(`브랜드 어워즈란` `참가 방법`)은 실패다.

### ③ 고쳐 쓰기 1회 — `reviseDraft()` ⭐

초고가 검수를 통과하면 **한 번 더 다듬는다.** 초고는 규칙을 지키느라 딱딱해지므로,
**규칙을 거의 주지 않고** 다시 읽힌다.

| | 초고 | 고쳐 쓰기 |
|---|---|---|
| 프롬프트 | `SYSTEM` **4,738자** + 채널 규칙 6,550자 | `REVISE_SYSTEM` **867자** |
| 시키는 것 | 지켜야 할 것 50개 | 군더더기 걷어내기 하나 |

⚠️ **`REVISE_SYSTEM` 을 늘리지 말 것.** 여기에 규칙을 붙이기 시작하면 초고와 같은 병에 걸린다.
지금 거는 제약은 넷뿐이다 — 사실 추가 금지 / 형식 줄 보존 / 군더더기 제거 / 짧아져도 됨.

**안전장치 셋** (다듬다가 더 나빠지는 일은 없어야 한다):
1. 결과가 `validateDraft()` 를 통과 못 하면 **초고를 그대로** 쓴다
2. 본문이 초고의 **절반 이하로 줄면** 문단을 통째로 날린 것이므로 초고를 쓴다
3. 호출이 실패하면(키 없음·네트워크) 초고를 쓴다. 고쳐 쓰기는 덤이다

⚠️ **블로그에만 건다** (`REVISE_CHANNELS`). 인스타·쓰레드는 짧아서 초고가 이미 덜 눌린다.
호출이 채널당 1회 늘어난다 — 세트당 4회 → 5회.

### ④ 분량을 다시 넓혔다 — 1,300~2,600자

요청자: "쓸데없는 이야기를 안 적을 거면 글자를 좀 더 자유롭게 해도 된다."

**글자 수를 조였던 것은 짧은 글을 원해서가 아니라 군더더기가 늘어나서였다.**
이제 군더더기는 ①(도입 규칙)과 ③(고쳐 쓰기)이 걷어내므로 상한으로 막을 이유가 줄었다.
`CHANNELS.blog.limit` 도 3,600 → 4,400 으로 함께 올렸다(전체 기준 안전망).

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 모듈 로드 5개 · 새로고침 후 404 | 전부 ok · `config.local.js`·`/api/health` 둘뿐 (71개 리소스) |
| `REVISE_SYSTEM` 길이 | **867자** (초고 `SYSTEM` 4,738자의 1/5) |
| 고쳐 쓰기 연결 | 검수 통과 경로에서 호출됨 · 실패 시 초고 폴백 확인 |
| `blogPlanFor(6)` | `points 4 / 1300~2600` |

> ⚠️ **효과는 실제 AI 호출로만 확인된다.** 로그인이 필요해 이 PC 에서는 못 돌렸다.
> 돌려 보고 확인할 것 — ① 도입이 2~4줄로 바로 답하는지, ② 소제목이 질문 흐름인지,
> ③ 고쳐 쓴 글이 초고보다 나은지(나빠지면 폴백이 도는지), ④ 세트당 호출이 5회인지(비용).

---

## 8-20. 이미지가 전부 같은 사진이던 이유 (2026-08-20)

요청자: **"주제와 관련이 없고 느낌만 있는 이미지가 나온다. 하나의 폼을 조금만 다르게 출력한 것에 지나지 않는다."**

받은 3장이 전부 「한국인 남성 + 제품 병 + 사무실 + 도시 전망 + 생각하는 포즈」였다. 원인이 셋이다.

### ① 장면 프롬프트에 예시가 **딱 하나** 있었다 ⭐

`lib/outline.js` 3단계에 이 한 줄이 있었다.

```
예) "a Korean small business owner in a modern Seoul office, reviewing documents at a desk"
```

바로 아래에 "항목마다 다른 장면이어야 합니다"라고 적어 뒀지만 소용없다.
**예시가 하나면 모델은 그 예시를 변주한다.** 6개 항목이 전부 이 문장의 사촌으로 나왔다.

이제 예시 대신 **촬영 방식 메뉴 7종**을 주고 항목마다 다른 것을 고르게 한다.

```
1) 사물 클로즈업   2) 손만 나오는 컷   3) 위에서 내려다본 배치(flat lay)
4) 넓은 공간       5) 어깨 너머        6) 질감 매크로
7) 인물 정면 — 6개 중 최대 1개
```

⚠️ **"인물은 최대 1개" 를 빼지 말 것.** 안 걸면 다시 전부 사람 사진이 된다.
⚠️ 좋은 예/나쁜 예도 항목 내용에 붙여 적었다 — 「엠블럼을 어디에 붙이나」면 상세페이지 배지 클로즈업이지
   생각하는 대표가 아니다. **사람이 생각하는 장면은 아무것도 설명하지 못한다.**

### ② `LOCALE` 이 모든 그림에 사무실을 불러왔다

`lib/imageprompt.js` 에 이 상수가 **무조건** 붙고 있었다.

```
all people are Korean, East Asian faces, Korean business setting in South Korea
```

장면에 사람이 없어도 붙었다. `Korean business setting` 이 들어가는 순간 모델은 사무실과 사람을 그려 넣는다.
사물·질감 컷을 아무리 잘 써도 이 한 줄이 도로 사무실로 끌고 갔다.

이제 **장면에 사람이 있을 때만** 인물 국적을 못박는다(`PEOPLE` 정규식으로 판별).
없으면 `shot in South Korea, no people in frame`.

⚠️ **`business setting` 을 기본값으로 되살리지 말 것.** 이 한 단어가 원인의 절반이다.

### ③ 프레이밍 안전망 — `FRAMING`

아웃라인이 촬영 방식을 고르게 시켜도 모델이 안 지킬 수 있다. 그래서 카드 번호로
거리감을 돌린다(wide / macro / flat lay / over-the-shoulder / still-life / low-angle).

⚠️ **결정적이어야 한다.** 난수를 쓰면 같은 카드를 다시 뽑을 때 프레이밍이 바뀌어 앞뒤가 안 맞는다.
`buildPrompt(card, conceptId, { index })` 로 카드 번호를 넘긴다 — `pages/template.js` 의 `promptFor(i)`.

### ④ 프레이밍을 돌린 것만으로는 부족했다 — **내용**을 못박았다

요청자 재지적: **"항목마다 다른 걸 고르게 하는 건 알겠는데, 주제에 관련된 내용을 만들라고."**

맞다. ①~③은 전부 **형식**의 다양성이다. 그림에 **무엇이 찍히는지**는 여전히 비어 있었다.
장면을 "어울리는 사진"이라고만 시키면 모델은 분위기로 답한다. 그래서 절차로 바꿨다.

```
1) 그 항목의 a(설명)에서 눈에 보이는 것 하나를 고른다
   상장·트로피·엠블럼·현판·배너·명함·상세페이지 화면·검색 결과·채점표·접수 양식·매장 문·TV 화면 …
   ⚠️ 항목에 숫자가 있으면 그 숫자가 '적혀 있는 물건'을 고른다
2) 그것이 어떤 상태인지 적는다 — "책상 위에 있다" 말고 "붙이는 중 / 막 도착한 / 이미 낡은"
3) 되읽는다. 이 장면을 다른 항목에 붙여도 말이 되면 실패다. 다시 고른다
```

무드 단어를 금지 목록으로 박았다 — `thoughtful, considering, thinking, professional, modern,
sleek, minimal, atmosphere, business meeting, office desk, laptop, discussing, planning`.

구체 예시도 항목 내용에 붙여 5개 넣었다.

| 항목 | 장면 |
|---|---|
| 엠블럼을 어디에 붙이나 | 상세페이지 첫 화면 속 배지 클로즈업 / 매장 유리문에 붙이는 중인 스티커 |
| 심사 배점 100점 | 항목별 점수가 적힌 채점표, 고객만족도 칸의 숫자 |
| 200여 개 제휴사 송출 | 여러 매체 기사 목록이 뜬 검색 결과 화면 |
| 비대면 시상 가능 | 택배 상자에서 막 꺼낸 상패와 완충재 |
| 연중 상시 접수 | 날짜가 비어 있는 접수 양식과 펜 |

**캡션도 함께 고쳤다.** 블로그의 `⤷` 캡션이 "그 이미지가 무슨 장면인지"를 쓰게 되어 있었다.
캡션은 **카드에 그대로 얹히는 문구**인데 이미지 설명을 쓰고 있었으니, 카드에도 "~하는 장면"이 찍히고
이미지 프롬프트도 그 설명을 다시 받아 **자기 꼬리를 무는 구조**였다.
이제 캡션은 **그 대목의 핵심 한 줄**이다. `❌ 배점표를 확인하는 장면` → `⭕ 심사의 절반이 고객 쪽 평가입니다`

`imageprompt.js` 도 캡션을 맨 앞에 세우고 **"그 문장이 가리키는 구체적인 사물이나 행동을 찍어라,
분위기가 아니라"** 로 바꿨다. 부정 지시(`avoid generic stock-photo scenes…`)도 붙였다 —
지시가 추상적이면 이미지 모델은 자기가 아는 가장 안전한 그림으로 돌아간다.

⚠️ **구체적으로 적게 하는 것(outline)과 기본값을 막는 것(imageprompt)은 짝이다. 둘 다 있어야 한다.**

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 모듈 로드 | 전부 ok |
| 사람 판별 — 장면 6종(사물 4·사람 2) | **6/6 정확** — 사물 컷은 `no people in frame` |
| ⚠️ `customer` 오탐 | `customer satisfaction`(고객만족도)이 채점표 컷을 인물 사진으로 끌고 갔다 → 목록에서 제거 |
| 프레이밍 — 카드 6장 | **6개 전부 다름** |
| 장면 절차·금지어·구체 예시 5종 | 프롬프트에 반영 확인 |
| 옛 단일 예시 | 제거 확인 |

> ⚠️ 실제 이미지는 뽑아 보지 못했다(프롬프트만 만드는 단계라 API 를 타지 않는다).
> 뽑아 보고도 비슷하면 다음은 `concept.style` 이다 — 매거진형의
> `cinematic editorial news photograph, dramatic directional lighting` 이 톤을 강하게 묶는다.
> 다만 그건 **템플릿의 정체성**이라 바꾸기 전에 요청자 확인이 필요하다.

---

## 8-21. 프롬프트 중복 정리 (2026-08-20)

같은 지시가 네 덩어리에 흩어져 있었다. **규칙을 없앤 게 아니라 중복만 지웠다.**

| 지시 | 몇 곳에 있었나 |
|---|---|
| 첫 단락·도입을 어떻게 열지 | **4곳** — SYSTEM · channelRules · coreBlock · buildPrompt |
| 주제에서 벗어나지 마라 | 3곳 |
| 문단마다 구체를 넣어라 | 3곳 |
| 인사말 금지 / 마크다운 금지 / 처음 듣는 사람 | 각 2곳 |

### 지운 것 — 전부 다른 곳에 같은 말이 남아 있다

| 지운 것 | 어디에 남아 있나 |
|---|---|
| SYSTEM 「첫 단락」 | 블로그 2) · 인스타 1)2) · 쓰레드 "첫 줄에서 왜 읽어야 하는지" · 말투 0) |
| SYSTEM 「범위」 | buildPrompt 꼬리 "상품 전반을 소개하는 문단을 끼워 넣으면 실패" (전 채널 공통) |
| SYSTEM 「출력 형식」 3줄 → 1줄 | buildPrompt 꼬리 "다른 마크다운 금지 / 소제목만 ##" |
| 블로그 2) 꼬리 경고 2개 | SYSTEM 「가장 중요한 것」·「읽는 사람」 |
| 「깊이」+「밀도」+「마케팅 관점」 3개 절 | **하나로 합침** — 셋 다 "사실 → 의미 → 판단" 이야기였다 |

`SYSTEM` **4,627자 → 3,844자 (-17%)**. 전체 지시 약 15,400 → 14,800자.

⚠️ **여기서 더 자를 때는 반드시 "다른 곳에 남아 있는가"를 먼저 확인할 것.**
이 규칙들은 하나하나가 과거 지적에서 나왔다(8-9 · 8-11 · 8-12 · 8-14).
유일한 규칙을 지우면 그때 고쳤던 문제가 다시 나오고, 원인 찾기가 어려워진다.

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 모듈 로드 | ok |
| 남아 있어야 할 규칙 6종 | 문단마다 구체 · 성과 약속 금지 · 말투 어미 · 절대 규칙 · 처음 듣는 사람 · 도입 금지 목록 **전부 확인** |
| 지운 규칙의 대체 위치 | 마크다운·소제목 형식·상품 전반 금지·주제 이탈 → buildPrompt 에 있음 확인 |
| 채널별 도입 규칙 | 블로그·인스타·쓰레드 **셋 다 자기 규칙을 갖고 있음** 확인 |

---

## 8-22. 이미지 자리를 코드가 놓는다 (2026-08-20)

요청자 지적이 **세 번** 반복됐다. "이미지가 그 소제목 앞으로 가야지 왜 뒤에 붙냐."
프롬프트에 배치 규칙을 넣고 좋은 예/나쁜 예까지 그려 줬는데도 계속 틀렸다.

### 원인 — 고치는 쪽이 더 망가뜨리고 있었다

```
① 모델이 형식 지시 50개를 지키다 보면 배치 하나쯤은 흘린다
② 흘린 자리를 repairBlog() 가 "글 끝에 몰아서" 채웠다   ← 여기가 진짜 원인
```

②는 8-2-2 에 "본문 사이 배치가 더 좋지만 문단 경계를 기계가 못 알아서 끝에 모은다"고
적어 둔 판단이었다. **그 판단이 틀렸다.** 문단 경계는 몰라도 **소제목 위치는 정확히 안다**(`## `).

### 이제 모델이 어디에 놓든 상관하지 않는다

`repairBlog()` 가 📷·⤷ 를 **통째로 걷어내고 코드가 다시 놓는다.**

| 카드 | 자리 |
|---|---|
| 표지 | **시작하는 글과 목차 사이** (목차 바로 위) |
| 본문 N | **N번째 핵심 소제목** 바로 앞 |
| 반론 | 한계 소제목 앞 |
| 마무리 | 정리 소제목 앞 |

> ⚠️ **표지를 첫 소제목 앞에 두면 본문 카드가 통째로 한 칸씩 밀린다.**
> 본문 1번은 핵심 1을 말하는데 소제목 2 앞에 서고, 캡션까지 어긋난다(실제로 그랬다).
> 표지는 소제목 자리를 차지하지 않는다 — 블로그 대표 이미지가 원래 목차 위다.

> ⚠️ **소제목이 카드보다 많으면 마지막 핵심에는 이미지가 없다.** 그게 정상이다.
> (핵심 4개 · 본문 카드 3장 — `BLOG_PLAN.points` 와 `DECK_PLAN` 이 다르기 때문)
> 억지로 채우면 다시 밀린다.

### 캡션은 조건부로만 믿는다

모델이 이미지를 **글 끝에 몰아 썼으면 캡션 번호도 믿을 수 없다.** 캡션은 모델이 생각한 순서로
붙어 있는데 자리는 코드가 다시 잡으므로, 그대로 옮기면 **영업 자료 캡션이 포럼 소제목에 붙는다**(실제로 그랬다).

- 마지막 소제목보다 **앞에** 이미지가 하나라도 있으면 → 모델 캡션을 **살린다**
- 전부 뒤에만 있으면 → 모델 캡션을 **버리고** 뼈대에서 뽑은 캡션(`captionOf(deck[n-1])`)을 쓴다.
  그쪽은 소제목 순서와 맞물려 있어 어긋나지 않는다.

### ⚠️ 세 곳이 같은 배치를 봐야 한다

배치를 바꾸면 **반드시 셋을 함께** 고친다. 하나만 고치면 조용히 어긋난다.

| 어디 | 무엇 |
|---|---|
| `lib/copyai.js` `repairBlog()` | 실제로 놓는 곳 |
| `lib/copyai.js` `channelRules()` 이미지 절 | 모델에게 시키는 곳 — 코드와 다르면 모델이 쓴 캡션이 밀린다 |
| `pages/template.js` `blogCardSource()` | 읽는 곳 — 카드 제목은 📷 **아래** 소제목에서 온다 |

### 검증 (2026-08-20 실행 기준)

| 초고 상태 | 결과 |
|---|---|
| 모델이 **끝에 몰아 쓴** 초고 (화면에서 본 그 형태) | 6장 전부 자기 소제목 앞으로 이동 · 캡션은 뼈대 것으로 교체 |
| 모델이 **제자리에 쓴** 초고 | 6/6 그대로 유지 · 모델 캡션 보존 · 소제목과 1:1 일치 |
| 장수 3장 | 표지·본문1·마무리만 배치 · 나머지 소제목은 이미지 없음(정상) |

---

## 8-23. 주제를 과대해석하던 문제 — 열거형 주제 (2026-08-20)

요청자가 같은 주제로 **GPT 결과와 앱 결과를 나란히** 보여줬다. 차이가 명확했다.

| | 주제 『특전 7종 정리, 현판과 배너는 어디에 둘까』 |
|---|---|
| GPT | ① 공식 상패 ② 공식 상장 ③ 수상 인증 엠블럼 ④ 매장용 현판 ⑤ 수상 홍보 배너 ⑥ 언론 기사 배포 ⑦ 홍보 콘텐츠 활용 — **7종을 하나씩 셌다** |
| 우리 앱 | 「어디에 둘까」 「자리를 나눠보세요」 「7종을 다 둘 필요는 없습니다」 — **7종을 한 번도 안 셌다** |

> "주제가 특전 7종 정리면 7종 정리만 해. 딴소리 하지 말고.
>  왜 내 맘대로 과대해석하고 궁금하지 않은 이야기를 계속하는 거야."

### 원인 셋

**① `TOPIC_KINDS` 에 열거형이 없었다.**
`define / compare / whether / why / howto` 다섯뿐이라 「7종 정리」가 **`general` 로 떨어졌다.**
general 은 "가장 먼저 알아야 할 것 한 줄"이라 모델이 **마음대로 각도를 잡는다.**
목록을 달라는 주제에 각도를 잡으면 그 순간 다른 글이 된다.

**② 항목 개수가 카드 장수에 묶여 있었다.**
`pointsOf()` 가 `BLOG_PLAN.points`(4) 고정이었다. 7종을 4칸에 넣으라니
모델은 **묶거나 버릴 수밖에 없다.** 그래서 「매장용·온라인용·홍보용」 같은 **없는 분류**를 지어냈다.

**③ 내가 넣은 「질문 흐름」 규칙이 열거를 밀어냈다.**
8-19 에서 "소제목은 독자의 질문 순서를 따라간다"를 넣었는데, 열거형에는 **정반대**다.
「① 공식 상패」가 「상패는 어디에 둘까」보다 낫다.

### 고친 것

- **`list` 유형 신설** — `정리 / 총정리 / 종류 / 목록 / 리스트 / 한눈에 / 모아 / 전부 / N종·N가지·N개`
  ⚠️ **`define` 보다 먼저 본다.** 「특전 7종이란」처럼 둘 다 걸리면 **열거 요구가 더 강하다.**
- **`topicCount()` 신설** — 주제가 개수를 말하면 그게 항목 수다. 2~10 만 받는다
  (「200여 개 제휴사」·「139개사」 같은 다른 수치를 항목 수로 오해하지 않도록 `(?!\s*사)`).
- **`channelRules()` 도 같은 개수를 본다** — `const points = topicCount(topic) || plan.points`
  ⚠️ 여기를 안 맞추면 아웃라인은 7개를 만들고 본문 규칙은 4개를 시키는 **엇갈린 지시**가 된다.
- **열거형이면 「질문 흐름」 규칙이 물러난다** — 소제목은 항목 이름 그대로.
  활용법은 각 항목 **안에서** 한 줄씩. 활용법이 목록을 대신하면 실패.
- 곁가지 물음이 붙은 주제(「7종 정리, 현판은 어디에 둘까」)는 **개수를 말한 쪽이 뼈대**다.

### 모든 주제에 걸리는 상위 규칙도 넣었다

요청자: "모든 주제에 지금 이 문제가 크게 자리하고 있어."

`lib/outline.js` `SYSTEM` 맨 앞에 넣었다 — **주제가 요구한 것을 먼저 다 채운다. 해석은 그다음.**
> **각도(angle)는 주제를 대신하는 게 아니라 주제에 답하는 방식이다.**
> 주제가 요구한 것을 안 하고 새 이야기를 꺼내면, 그건 각도가 아니라 다른 글이다.

⚠️ 이 규칙과 8-8(「기존 팩트에서만 돌려막지 마라」)은 **상충하지 않는다.**
8-8 은 *자료를 요약하지 마라*는 뜻이고, 이건 *주제를 갈아치우지 마라*는 뜻이다.
둘 중 하나만 남기면 반대쪽 실패로 넘어간다 — 자료 요약문이 되거나, 딴 글이 되거나.

### 함께 고친 것

`whether` 판정 구멍 — 「중소기업도 받을 수 있나요」가 `general` 로 빠졌다.
`할\s*수\s*있나` → `수\s*있나` 로 넓혀 잡는다.

### 검증 (2026-08-20 실행 기준)

| 주제 | 판정 |
|---|---|
| 특전 7종 정리, 현판과 배너는 어디에 둘까 | **list (7개)** |
| 수상 특전 7가지 / KCST 심사 기준 5개 항목 정리 | list (7개) / list (5개) |
| IPTV 지역 타겟팅이란 | define |
| 수상 후 언론 노출 활용법 | howto |
| 중소기업도 받을 수 있나요 | **whether** (고치기 전 general) |
| 왜 지금 신청해야 하나 | why |

모듈 로드 ok(순환 import `copyai ↔ outline` 정상) · 새로고침 후 404 는 둘뿐 ·
`plan.points` 잔존 0(선언부 제외) · 열거형 분기·좋은 예·"활용법이 목록을 대신하면 실패" 반영 확인.

> ⚠️ **실제 AI 호출로는 미검증.** 로그인이 필요하다. 『특전 7종 정리』로 돌려
> 소제목이 7개로 서는지, 7종이 이름으로 다 나오는지 확인할 것.

---

## 8-24. 왜 API 를 부르는데 GPT 만 못했나 — 코드가 모델 대신 읽고 있었다 (2026-08-20)

요청자 지적이 8-23 의 처방을 정면으로 반박했다.

> "열거형 주제를 인식하는 게 없다면 다른 주제들은 이런 문제를 만나면 어쩔래?
>  API 를 불러와서 하는 건데 왜 안 돼?"

**맞는 지적이다.** 8-23 은 유형을 하나 더 추가한 **땜질**이었다. 주제의 모양은 무한한데
정규식을 늘리는 걸로는 끝이 없다. 진짜 질문은 이것이다 —
**같은 모델을 부르는데 왜 GPT 는 되고 우리는 안 되나.**

### 답: 우리는 모델에게 주제를 읽히지 않았다. **코드가 먼저 읽고, 모델은 빈칸만 채웠다.**

모델이 주제를 보기도 전에 **코드가 정해 버린 것 셋**이 있었다.

| 무엇 | 어디 | 무슨 일이 벌어졌나 |
|---|---|---|
| **항목 개수** | `pointsOf()` → JSON 스펙이 배열 길이를 못박고, `validate()` 가 `length !== POINTS` 면 **반려** | 모델이 "7종이면 7개"라고 판단해도 **반려된다.** 재시도해도 같은 이유로 또 막혀 결국 4개로 욱여넣는다 |
| **주제 유형** | `topicKind()` 정규식 5종 | 정규식에 없으면 `general` → **"가장 먼저 알아야 할 것 한 줄"** = 사실상 *"알아서 각도를 잡아라"*. **분류 실패가 곧 과대해석 지시**였다 |
| **각도 강제** | `validate()` 의 angle 검사 | 목록을 달라는 주제에도 **반드시 각도를 만들어야** 통과한다 |

여기에 `q` 28자 / `a` 160자(카드 슬롯 상한)까지 걸려, 모델은 **글을 쓰기 전에 주제를 고정된 틀에 압축**해야 했다.

> **GPT 가 잘한 이유는 능력 차이가 아니다. 아무것도 미리 정하지 않았기 때문이다.**
> 주제를 읽고 스스로 "7개"라고 판단하고 7개를 썼다.

### 고친 것 — 개수와 구성의 주인을 모델에게 넘겼다

- **JSON 스펙에서 배열 길이 고정을 뺐다.** 대신 「항목 수」 절을 두고 **모델이 정하게** 한다.
  주제에 개수가 있으면 그 개수 / 목록형이면 항목 하나가 소제목 하나 / 그 외엔 필요한 만큼.
- **`validate()` 는 이제 두 가지만 본다** — ① 주제가 말한 개수를 어겼는가 ② 2~8 범위를 벗어났는가.
  **"내가 정한 숫자와 다르다"로는 반려하지 않는다.**
- **`pointsReason` 필드 신설** — 왜 그 개수인지 한 줄 적게 한다. 적으면서 다시 생각하게 된다.
- **`channelRules()` 의 소제목 개수 = `core.points.length`** — 아웃라인이 정한 값을 그대로 따른다.
  따로 계산하면 아웃라인은 7개를 만들고 본문 규칙은 4개를 시키는 **엇갈린 지시**가 된다.

### `general` 기본값을 바꿨다 — 분류에 실패해도 안전하게

정규식은 언제나 뚫린다. 그러니 **뚫렸을 때 무슨 일이 벌어지는지**가 더 중요하다.

```
예전: "그 대상에 대해 가장 먼저 알아야 할 것 한 줄"   → 각도를 지어내라는 말로 읽힌다
지금: "주제 문장을 동사 단위로 읽으세요. 「정리」면 정리하고, 「비교」면 비교합니다.
       모르겠으면 해석하지 말고 이행하세요."
```

> **잘못 해석한 글은 잘 쓴 딴 글이고, 독자에게는 안 쓴 것과 같다.**

⚠️ **`topicKind` 는 이제 '도움말'이지 '판정'이 아니다.** 유형이 안 걸려도 주제를 그대로 이행하면
결과가 크게 나빠지지 않는다. 새 유형을 추가하고 싶어지면 **먼저 general 로 돌려 보고**
정말 부족한지 확인할 것 — 유형을 늘리는 건 마지막 수단이다.

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 모듈 로드 4개 | 전부 ok · 404 는 `config.local.js`·`/api/health` 둘뿐 |
| 개수 고정 반려 제거 | `length !== POINTS` 검사 사라짐 확인 |
| 새 검사 | 주제 개수 불일치 / 2~8 범위 — 둘 다 반영 확인 |
| `pointsReason` · 「항목 수」 절 | 스펙에 들어감 확인 |
| `general` 기본값 | "모르겠으면 해석하지 말고 이행하세요" 반영 확인 |
| 채널 규칙 개수 출처 | `core.points.length` 를 먼저 본다 확인 |

> ⚠️ **실제 AI 호출로는 미검증.** 로그인이 필요하다. 확인할 것 —
> ① 『특전 7종 정리』에서 항목이 7개로 나오는지 ② `pointsReason` 이 납득되는지
> ③ 유형에 안 걸리는 주제(예: 『수상 후 6개월 동안 할 일』)에서 각도를 지어내지 않는지.

---

## 8-25. 마무리 카드가 잘린 문장이던 이유 (2026-08-20)

요청자 지적: 마무리 카드 제목이 **「중앙일보 연합광고와 포브스…」**. 뜻이 끊긴 조각이었다.
"마무리 내용이었으면 문의를 남기든지 해야지."

### 원인 — 40~55자 문장을 16자 칸에 넣고 있었다

```
deckFromCore()  →  outro.title = closing          (승인된 마무리 문장, 31~55자)
defaultsFor()   →  clampSlot(title, 16)           (매거진형 제목 칸 MAG_LINE_MAX)
                →  "중앙일보 연합광고와 포브스…"
```

`templates.js` 에는 **"마무리 장은 예외: 승인된 마무리 문장이라 자르거나 쪼개지 않고 통째로 쓴다"**
는 주석이 있었다. **그 주석이 사실이 아니었다.** 쪼개지만 않았을 뿐, 바로 아래 `pick()` 의
`clampSlot()` 이 16자에서 그대로 잘랐다. 예외가 오히려 글을 버리고 있었다 —
본문 카드는 두 줄로 쪼개져 32자를 쓰는데, 마무리만 한 줄 16자만 남았다.

### 고친 것

**① 마무리 카드 제목은 이제 '행동 유도'다**

마무리 카드에서 독자가 알아야 할 것은 **다음에 뭘 하면 되는지**다.

| | 예전 | 지금 |
|---|---|---|
| 제목 | 승인된 마무리 문장 (31~55자 → 잘림) | **승인된 CTA** (23~32자, 두 줄에 들어감) |
| 본문 | 접수 방식 안내 | **마무리 문장 + 접수 방식** |

CTA 는 `core.cta`(AI 뼈대) → 없으면 `product.voice.ctas`(승인 목록)에서 고른다.
예: `행사별 접수 일정은 프로필 링크에서 확인하실 수 있습니다.` / `현판·배너 실물이 궁금하시면 댓글 남겨주세요.`

⚠️ **승인된 마무리 문장은 사라지지 않는다.** 본문 칸이 있는 템플릿(카드형·노트형)에서 본문으로 나온다.
   **매거진형에는 본문 칸이 없다** — 거기서는 CTA 만 남는 게 맞다. 잘린 문장보다 낫다.

**② 매거진형 마무리 예외를 없앴다**

`card.kind === 'outro' ? [card.title, ''] : splitHeadline(...)` → 항상 `splitHeadline`.
두 줄로 쪼개면 32자까지 들어간다.

> ⚠️ **"자르지 않는다"는 주석을 다시 믿지 말 것.** 슬롯에 `max` 가 걸려 있으면
> `pick()` 이 무조건 자른다. 자르고 싶지 않으면 **글자 수를 슬롯에 맞춰 넣어야 한다.**

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 상품 4종 × 템플릿 3종 = 12조합 마무리 카드 렌더 | **잘림 0 · `…` 0** |
| 매거진형 마무리 | `행사별 접수 일정은 프로필` / `링크에서 확인하실 수 있습니다` — 두 줄로 온전히 |
| 노트형 마무리 | 제목 `정리하면` · 본문에 **마무리 문장 + 접수 방식 + CTA(하이라이트 바)** 전부 |
| 카드형 마무리 | 팔로우 유도 고정 문구 (설계대로 — 8절 참고) |
| 모듈 로드 | 전부 ok |

### 배지가 마지막 장만 브랜드명이던 것도 고쳤다

요청자: "그러게 왜 04/04 가 아니고 포브스야?"

`deckFromCore()` 가 **표지와 마무리만** `eyebrow: p.short` 였다. 그래서 배지가
`01/06 → 02/06 …` 로 가다가 **마지막만 「포브스」로 튀었다.**
게다가 매거진형은 상단 중앙에 계정명(`brand`)이 따로 있어 **같은 글자가 한 카드에 두 번** 보였다.

이제 **전 장에 번호를 붙인다.** 한 장짜리(장수 1)일 때만 번호가 뜻이 없어 브랜드명을 쓴다.

| 장수 | 배지 |
|---|---|
| 6 | 01/06 · 02/06 · 03/06 · 04/06 · 05/06 · **06/06** |
| 4 | 01/04 · 02/04 · 03/04 · **04/04** |
| 1 | 브랜드명 (번호가 뜻이 없다) |

재검증: 매거진·카드·노트 × 6장 = **18장 렌더 잘림 0.**

---

## 8-26. 카드만 봐도 말이 되게 · 호출을 5회 → 4회로 (2026-08-20)

요청자: **"인스타는 카드뉴스만 사용하는데, 6장을 안 만들면 내용이 자연스럽지 않다."**

### 원인 — 카드는 요약이 아니라 **앞에서 자른 것**이었다

`deckFromCore()` 가 `points[0..k-1]` 로 **앞에서부터** 집어 왔다. 실측:

| 블로그 소제목 | 카드 | 카드에 나온 것 | 빠진 것 |
|---|---|---|---|
| 4개 | 3장 | 소제목1 | **2·3·4** |
| 4개 | 6장 | 소제목1·2·3 | **4** |
| 7개 | 6장 | 소제목1·2·3 | **4·5·6·7** |

**6장을 다 써도 뒤가 빠진다.** 게다가 인스타 캡션은 항목을 **전부** 말하므로,
캡션엔 7개가 있는데 카드엔 3개만 있는 상태가 된다.

### 고친 것 — 파생 1회 (`derivePosts`)

**완성된 블로그를 원문으로 삼아 인스타·쓰레드·카드를 한 번에** 뽑는다.
카드가 블로그의 부분집합이 아니라 **자기 기승전결**을 갖는다.

```
예전 5회: 아웃라인 · 블로그 · 고쳐쓰기 · 인스타 · 쓰레드
순진하게 카드 추가하면 6회
지금 4회: 아웃라인 · 블로그 · 고쳐쓰기 · 파생(인스타+쓰레드+카드)
```

**셋을 합칠 수 있는 이유는 셋이 같은 일이기 때문이다** — 전부 *완성된 글을 요약*한다.
원문이 같은데 따로 부를 이유가 없다. 호출을 줄이려고 합친 게 아니라, 합치는 게 맞아서 합쳤다.

### 실행 순서가 바뀌었다

예전에는 세 채널을 **아웃라인 위에서 나란히** 불렀다(`Promise.allSettled`).
그래서 인스타·쓰레드가 완성된 블로그를 못 보고 뼈대만 보고 썼다.

```
지금:  블로그(+고쳐쓰기) → 그 결과를 원문으로 파생 1회 → 빠진 채널만 개별 생성
```

⚠️ **파생은 최적화지 필수 경로가 아니다.** 실패하거나 검수에 떨어진 채널은 예전처럼
개별 생성(`runOne`)으로 채운다. 파생이 죽어도 결과물은 나온다.
⚠️ 그래서 **최악의 경우 호출 수가 예전과 같아진다**(파생 1 + 개별 2 = 6회). 정상 경로는 4회다.

### 카드 문구의 우선순위

```
state.cardCopy (파생 1회)  →  blogCardSource() (블로그 소제목)  →  뼈대(outline)
```

`pages/template.js` 의 `deckFromBlog()` 가 `cardCopy.key === draftKeyOf(s)` 일 때만 쓴다.
⚠️ **마무리 카드는 어느 경로에서도 안 건드린다** — 승인된 CTA·마무리 문장 자리다(8-25).

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 모듈 로드 6개 | 전부 ok · 404 는 둘뿐 |
| `derivePosts` 실호출 (fetch 가로채 canned JSON) | 인스타 통과 · **카드 6장 통과** · 쓰레드는 검수 반려 |
| 쓰레드 반려 사유 확인 | `글이 너무 짧습니다`(119자) — **테스트 문장이 짧아서다.** 같은 글을 130자로 늘리면 통과. 코드 문제 아님 |
| 폴백 경로 | 반려된 채널은 `missing` 으로 넘어가 개별 생성됨을 코드로 확인 |
| 카드 우선순위 | `cardCopy` → `blogCardSource` 순서, 마무리 카드 보존 확인 |

> ⚠️ **실제 AI 호출로는 미검증.** 로그인이 필요하다. 확인할 것 —
> ① 카드 3~4장으로 뽑아 **카드만 넘겨봐도 말이 되는지** ② 캡션이 카드와 겹치지 않는지
> ③ 세트당 호출이 4회인지(비용).

### 다음에 더 줄이려면 — 아웃라인을 블로그에 흡수

아웃라인은 원래 **세 채널이 같은 뼈대를 보게** 하려고 만들었다(8-8).
그런데 이제 인스타·쓰레드·카드가 전부 **완성된 블로그**에서 나온다 — 공통 원본은 블로그다.
즉 아웃라인은 **블로그 하나만을 위해** 존재한다. 블로그 호출에 흡수하면 **3회**가 된다.

⚠️ 다만 `core` 는 카드 덱·이미지 `shot`·`objectionHead`·`angle` 검증까지 물려 있어
**수술 범위가 크다.** 지금 손대지 않았다. 손댈 때는 규칙 기반 폴백(`buildCore`)이
살아 있는지부터 확인할 것 — 키가 없어도 앱은 끝까지 동작해야 한다.

---

## 8-27. 스타일 수집을 단계에서 설정으로 옮겼다 (2026-08-20)

요청자: **"할 때마다 스타일 수집이 너무 번거로워서 프로필 세팅처럼 하는 게 좋겠다."**

### 왜 번거로웠나 — 주제에 묶여 있었다

8-20 에서 `/research` 를 **2단계**로 넣었다. 그런데 저장 키가 `상품|주제` 였다.
**주제를 바꾸면 수집분이 통째로 날아갔다.** 그래서 게시물마다 검색·수집·분석을 다시 해야 했다.

**문체는 주제와 무관하다.** 한 번 모아 두고 골라 쓰면 되는 것이었다 — 프로필과 같은 성격이다.

### 바뀐 구조

```
예전:  1 상품·주제 → 2 스타일 수집 → 3 아이디어 문서화 → 4 카드뉴스
지금:  1 상품·주제 → 2 아이디어 문서화 → 3 카드뉴스      (단계 3개로 복귀)
       + 헤더 「문체 스타일」 = 언제든 들어가는 설정 (프로필 옆)
```

| 무엇 | 어디 |
|---|---|
| 스타일을 **모은다** | `/research` — 검색 → 수집 → 분석 → **이름 붙여 저장** (최대 12개) |
| 스타일을 **고른다** | 2단계 아이디어 문서화 상단의 칩 (`stylePickHTML`) |
| 저장 위치 | `state.styles[]` + `state.styleId` · **기기 간 동기화됨**(`SYNCED_KEYS`) |

- **추천 키워드 7종**을 검색창 옆에 뒀다 — 브랜드 어워즈 / 기업 인증 대상 / 소상공인 마케팅 …
  ⚠️ **상품명 자체는 넣지 않는다.** 우리 브랜드 글이 검색되면 우리 문체를 다시 배우는 셈이다.
- `guard()` 를 없앴다 — 상품·주제 없이도 들어갈 수 있다. 설정이니까.
- **옛 저장분은 자동으로 옮긴다** (`migrateStyle()` in `store.js`) — `researchStyle` 이 있으면
  「이전에 수집한 스타일」이라는 이름으로 목록에 올린다. `aiCtx` 도 고른 스타일이 없을 때
  옛 값을 폴백으로 계속 읽는다.

⚠️ **고른 스타일은 문체만 프롬프트에 들어간다.** 사실·주제는 상품 자료와 담당자 주제가 이긴다
(`lib/copyai.js` 의 researchStyle 블록). 이 경계를 풀면 남의 글 내용이 우리 글에 섞인다.

### 검증 (2026-08-20 실행 기준)

| 무엇 | 결과 |
|---|---|
| 모듈 로드 6개 | 전부 ok · 404 는 둘뿐 |
| 단계 | **3개**로 복귀 확인 |
| `/research` 진입 | `guard()` 통과 (상품·주제 없이도 들어감) |
| 빈 상태 | 안내 문구 · 추천 키워드 **7개** · 이름 입력칸 · **스테퍼 없음** |
| 추천 키워드 클릭 | 검색어가 「브랜드 어워즈」로 채워짐 |
| 스타일 2개 저장 상태 | 목록 카드 2개 · 「저장된 스타일 2개」 · 미리보기 노출 |
| 삭제 | 1개로 줄고 **선택값도 함께 해제**됨 |
| 2단계 칩 | 「사용 안 함」 + 스타일 2개 = 3개 · 선택 표시 정확 · 관리 링크 |
| 동기화 | `styles`·`styleId`·`cardCopy` 가 `pickSynced()` 에 포함 |

> ⚠️ **실제 수집·분석은 미검증.** `/api/research/*` 가 Node 서버를 요구하는데 이 PC 엔 Node 가 없다(8-15).
> Node 가 있는 환경에서 확인할 것 — 검색 → 수집 → 「분석해서 저장」 → 목록에 쌓이는지.

---

## 8-2-2. 모델 비교 벤치마크 (`tools/bench.html`)

Luna 와 Sol 이 25배 차이라 감으로 고를 수 없다. **어느 모델이 최소 사양인지 숫자로 정하는 도구다.**

```
http://localhost:5610/tools/bench.html
```

### 어떻게 재나

같은 주제를 모델별로 여러 번 돌려 `validateDraft()` 의 **1회 통과율**과 **실제 지불액**을 잰다.
앱의 3단계 「AI 설정」에 저장된 키를 그대로 쓴다 — 키 입력칸을 따로 두지 않는다(키는 한 곳에만).

- ⚠️ **생성 로직을 복제하지 않는다.** `generateWithAI()` 를 그대로 부르고 `onAttempt` 훅으로
  관측만 한다. 벤치마크가 실제 경로와 달라지는 순간 측정값이 의미를 잃는다.
  같은 이유로 `onAttempt` 안에서 생성 흐름을 바꾸면 안 된다.
- 토큰은 응답의 `usage` 를 쓴다(`onUsage` 훅). **추론 토큰까지 정확히 세는 유일한 경로다.**
  사용량이 안 오면 글자 수로 어림하고 표의 「정확」 열에 그 사실을 표시한다.
- 돌리기 전에 예상 비용을 최소~최대로 보여주고 확인을 받는다. 최대는 전부 3회 재시도한 경우다.
- 제공자·모델을 잠시 바꾸므로 `finally` 에서 **반드시 되돌린다.** 벤치마크가 앱 설정을
  바꿔 놓고 끝나면 안 된다.

### ⚠️ 추천은 가격만 보고 정하지 않는다

**싼 모델이 정말 싼 게 아니다.** 반려되면 최대 3회까지 다시 부르므로 호출 수가 늘고,
3회를 다 쓰고도 실패하면 규칙 기반 글로 떨어져 품질이 무너진다.
그래서 **1회 통과율 70% 이상 · 폴백 0건**을 통과한 것 중에서 가장 싼 것을 고른다.

처음 만들었을 때는 세트당 비용만 보고 골라서 **통과율 33% 짜리를 「최선」으로 표시했다.**
이 도구를 만든 이유와 정반대라 기준을 넣었다. 기준을 낮추거나 없애지 말 것.

기준을 넘긴 모델이 없으면 그렇게 말하고, 반려 사유가 「주제 이탈」에 몰려 있으면
모델이 아니라 프롬프트 문제일 수 있다고 안내한다.

### 이 파일의 본체는 생성이 아니라 검증이다

규칙 기반은 승인된 문장만 조합해서 사실 위반이 **구조적으로 불가능**했다.
LLM 은 글은 좋아지지만 없는 사실을 지어낼 수 있다. 그래서 게이트를 통과한 글만 화면에 내보낸다.

```
프롬프트(상품 사실 통째로 + "이 안에서만") → 생성 → repairDraft() → validateDraft()
   └ 통과 못 하면 무엇이 걸렸는지 알려주고 재시도(최대 3회) → 그래도 실패하면 던짐
     → 호출한 쪽이 기존 글을 그대로 둔다 (나쁜 글로 덮어쓰지 않는다)
```

### ⚠️ 형식은 반려하지 않고 고친다 — `repairDraft()`

요청자 지시(2026-08-03): **게이트를 느슨하게 풀어 통과율을 올린다.**
이미지 자리 한 줄이 빠졌다고 멀쩡한 글 2,000자를 버리는 건 낭비였고,
재시도해도 같은 이유로 또 막히는 일이 잦았다.

| | 처리 |
|---|---|
| 목차 · 이미지 자리 · 캡션 · 길이 초과 · 쓰레드 해시태그 | `repairDraft()` 가 **고친다** |
| 금지 표현 · 근거 없는 단정 · 주제 이탈 · 너무 짧음 | `validateDraft()` 가 **반려한다** |

- **형식 검사를 `validateDraft()` 에 다시 넣지 말 것.** 통과율만 떨어지고 글은 나아지지 않는다.
- `repairDraft()` 는 **형식만** 만진다. 사실 관계를 손대면 안 된다.
- 빠진 이미지 자리는 **글 끝에** 순서대로 채운다. 본문 사이 배치가 더 좋지만 AI 글의 문단 경계를
  기계가 정확히 알 수 없어 엉뚱한 자리에 박히면 더 나쁘다. 서대문구 레퍼런스가 카드뉴스를
  글 끝에 통째로 붙인 방식과 같다.
- 짝 없는 `⤷` 캡션은 버린다. 안 그러면 캡션 수와 이미지 수가 어긋나 인스타 카드와 1:1로 못 맞춘다.
- **보정을 두 번 돌려도 결과가 같아야 한다**(idempotent). 검증 항목에 넣어 뒀다.

마지막 실행 기준: 상품 4 × 톤 4 × 망가뜨린 형태 5종 = 80조합 보정 실패 0건.
실제 Gemini 호출로 세 채널 전부 통과 확인(2,285 / 800 / 458자, 8초).

### ⚠️ `BANNED_PHRASES` 만으로는 부족하다 (실제로 뚫렸다)

`data/products.js` 의 `BANNED_PHRASES` 는 **정확히 일치하는 문구 5개**뿐이다.
조합만 하던 시절엔 충분했지만 LLM 상대로는 "업계 1위", "최고 권위", "매출이 보장됩니다"가
전부 그냥 통과했다. 그래서 어형 변화를 잡는 정규식 `RISKY` 를 따로 뒀다.

두 번 헛디뎠으니 규칙을 건드릴 때 반드시 확인할 것:

1. **부정문을 주장으로 잡으면 안 된다.** 승인된 반론이 "매출이 오르지는 **않습니다**" 라서
   정상 글 336건이 막혔다 → `NOT_NEGATED` 로 뒤따르는 부정어를 확인한다.
2. **상품 데이터의 고유명사를 막으면 안 된다.** 포브스 행사 이름이 실제로
   "고객신뢰도 **1위** 프리미엄 브랜드 대상" 이라 96건이 막혔다 →
   `safePhrasesOf()` 로 승인된 원문을 지운 뒤 남은 부분만 검사한다.

규칙을 고쳤으면 **기존 규칙 기반 글 576조합에 오탐 0건**인지 반드시 다시 확인한다.

---

## 8-3. 블로그 형식 — 네이버 레퍼런스 반영 (2026-08-03)

요청자가 네이버 블로그 4편을 레퍼런스로 줬다. 저장본을 파싱해 구조를 측정하고 옮겼다.

> ### ⚠️ 레퍼런스에서 가져오는 것은 **레이아웃 · 화면 구성 · 이미지 배치**다. **문체가 아니다.**
>
> 한 번 레퍼런스의 '~다' 평서형까지 옮겼다가 같은 날 되돌렸다. 상품을 **소개하는** 글인데
> 말끝마다 '~다'로 끊으니 읽는 사람에게 설명하는 느낌이 사라졌다. 요청자 지적 그대로다.
> 문체는 **읽는 사람에게 설명하듯 하는 존댓말**이다. `voice` 재료를 원문 그대로 쓰면 된다.
>
> 문체 검사도 두지 않는다. 뒀더니 정상 문장을 계속 잡아 AI 글이 매번 반려됐다
> (따옴표 안 인용문 `"대기업만 되는 거 아니에요?"`, '아니다'의 '니다').
> `SYSTEM` 프롬프트의 "존댓말"과 채널 규칙의 "평서형"이 충돌해 모델이 무한 반려되기도 했다.
> **문체를 강제하는 장치를 다시 만들지 말 것.**

| 레퍼런스 | 성격 |
|---|---|
| 로라의 행복한상상(653864) 3편 — 김부장 / 피스마이너스원 / 유미의세포들 | **3편이 오차 없이 같은 골격.** 이게 기준이다. |
| 서대문구(sdmstory) 1편 | 관공서형. 본문 463자 + **끝에 카드뉴스 8장을 통째로 첨부**. |

### 실측값 (레이아웃 목표치)

| | 김부장 | 피스마이너스원 | 유미의세포들 |
|---|---|---|---|
| 본문 글자수 | 2,011 | 2,343 | 2,077 |
| 줄당 평균 | 27자 | 29자 | 27자 |
| 30자 이하 짧은 줄 | 70% | 59% | 71% |
| 소제목 | 6개 | 6개 | 6개 |
| 본문 이미지 | 4장 | 1장 | 5장 |

### 확정된 형식 — ⚠️ 2026-08-13 이후 '필수 + 자유'로 바뀌었다

```
[필수] 제목 두 줄(검색어 / 후킹) → 도입 → ■ 소제목×N (핵심 개수, 사이에 📷+캡션)
       → ■ 한계 단락 → ■ 마무리 단락(승인된 closing) → 📷 자리 전부
[자유] 구분선 ─── · 인용구 요약(> ) · 🔔 개요표 · 해시태그
```

요청자 승인(2026-08-13): **"개요표와 해시태그는 빼거나 변형해도 된다.
저장 유도는 유지하고 나머지는 자유롭게. 그냥 퀄리티 높은 글을 쓰게 해달라."**

- 소제목 개수 N 은 `blogPlanFor(size).points` 다 (1~3장 2개 / 4~6장 3개).
- **개요표는 규칙 기반에서 4장 이상일 때만** 붙는다. 800자 글에 5줄 표가 붙으면 본문보다 커진다.
- **해시태그는 `tagsFor()` 가 seed 로 골라** 개수·순서를 바꾼다(블로그 3개 / 인스타 5개).
  ⚠️ 태그 문자열을 새로 만들지 않는다 — `data/products.js` 의 승인 목록에서만 고른다.
- **저장 유도 한 줄(인스타)은 유지한다** — 요청자 지시. 문구만 매번 다르게.
- 각 채널 프롬프트에 「좋은 글의 기준」 절을 뒀다. 형식을 더 못박는 대신 **무엇이 좋은 글인지**를 알려준다.
- **목차를 넣지 않는다.** 레퍼런스 3편 모두 없다. 예전 구조에는 있었다 — 되살리지 말 것.
- **한 문장 = 한 줄** (`breathe()`). 문단을 덩어리로 붙이면 네이버에서 벽처럼 보인다.
  이건 문체가 아니라 **화면에 보이는 모양**이라 유지한다.
  ⚠️ **빈 줄은 문장마다가 아니라 2~3문장 묶음마다** 넣는다 (2026-08-12, 8-9 참고).
  ⚠️ **쉼표 분할은 '짧고 고른 항목 3개 이상'일 때만** 한다 (`isEvenList()`). 8-9 참고.
- **📷 줄 바로 아래 `⤷ ` 캡션 한 줄이 붙는다.** 레퍼런스가 이미지마다 설명을 단다.
  캡션은 `buildDeck()` 의 카드에서 뽑는다 — 그래야 **블로그 이미지와 인스타 카드가 같은 문구**를 말한다.
  캡션은 **설명**이지 제목이 아니다. 표지만 카드 제목(후킹), 나머지는 카드 본문 첫 문장을 쓴다.
  (처음에 제목을 넣었더니 본문 소제목이 두 번 나왔다.)

### 검증 (블로그를 건드렸으면 다시 돌린다)

`validateDraft()` 의 블로그 검사는 세 가지다 — **캡션 6줄 · 목차 금지 · 줄 리듬**. 문체 검사는 없다.

> ⚠️ 줄 리듬 검사에서 **📷 줄을 제외**하는 것과 같은 이유로, 어떤 검사든 이미지 자리 표시
> ("…카드뉴스 N번을 여기에 넣으세요")를 본문으로 취급하면 안 된다. 한 번 정상 글 96건을 막았다.

마지막 실행 기준: 상품 4 × 톤 4 × variant 2 × 주제 3 × 채널 3 = **288조합 실패 0건**,
블로그 96건 전부 `validateDraft()` 통과.

---

## 8-4. 세 채널 내용 통일 (2026-08-03)

요청자 요구: **블로그·인스타·쓰레드의 내용이 같아야 하고, 이미지 한 벌을 세 채널에 나눠 쓴다.**

예전에는 채널마다 재료를 따로 골랐다(인스타 qa 3개 / 블로그 4개 / 쓰레드는 notes 별도 정렬).
같은 상품·주제인데도 **다루는 사실이 서로 달랐다.**

### `buildCore()` 하나가 모든 걸 정한다

```
buildCore(ctx) → { hook, summary[3], points[3], extra, objection, closing, cta, events }
        │
        ├── blogCopy(core)        소제목 3개 = points
        ├── instagramCopy(core)   ①②③ = points
        ├── threadsCopy(core)     points 에 대응하는 구어체 note
        └── deckFromCore(core)    카드 2·3·4 = points
```

- **`CORE_POINTS = 3`.** 카드뉴스 본문 3장과 1:1로 맞물린다. 여기를 바꾸면 덱도 같이 봐야 한다.
- ⚠️ **채널 생성기 안에서 `byTopic()`·`pick()` 으로 재료를 새로 고르지 말 것.** 그 순간 다시 갈라진다.
  생성기는 코어가 준 것만 쓴다.
- 달라지는 것은 **형식과 화법**뿐이다. 그게 '각 플랫폼에 맞게'의 의미다.
- 블로그만 문단마다 근거를 한 줄씩 덧댄다. **다루는 항목은 같고 설명만 길어지는 것**이라 통일이 깨지지 않는다.

### variant 를 채널별로 두면 안 된다

내용을 정하는 것이 variant 이므로 채널마다 따로 올리면 그 채널만 다른 사실을 다룬다.
`regenerateAll()` 이 **하나의 값을 공유**하고, 채널별 「다시 쓰기」도 전체 재생성으로 넘긴다
(`regenerateOne()` → `regenerateAll()`). 되돌리지 말 것.

### 이미지 배포 (`IMAGE_PLAN`)

카드는 **한 벌만** 만든다. 서대문구 레퍼런스가 카드뉴스 한 벌을 블로그 끝에 붙이고
같은 걸 인스타에 올린 방식 그대로다.

| 채널 | 쓰는 카드 |
|---|---|
| 블로그 | 1~6번을 📷 자리에 순서대로 |
| 인스타그램 | 1~6번 캐러셀 |
| 쓰레드 | 1번(표지)만 — 여러 장 붙이면 광고 톤이 된다 |

2단계 각 채널 탭 아래에 이 안내가 나온다.

### AI 경로도 같은 코어를 본다

규칙 기반은 코어 하나를 세 생성기가 나눠 쓰면 그만이지만, LLM 은 채널마다 따로 부른다.
그래서 `coreBlock()` 이 **코어가 고른 3가지를 프롬프트에 박아 넣는다.** 빼면 채널마다 다른 사실을 고른다.

### 검증

마지막 실행 기준 — 상품 4 × 톤 4 × variant 2 × 주제 3:

- 288건 생성, **실패 0건**
- 카드 2·3·4 ↔ 블로그 소제목 ↔ 인스타 ①②③ **288개 항목 전부 일치**
- 반론·마무리 문장도 블로그·인스타에서 동일
- ⚠️ 문자열 비교 전에 **공백을 정규화할 것.** `breathe()` 가 문장을 줄로 쪼개므로
  원문 그대로 `includes()` 하면 전건 실패한다.

> ⚠️ **쓰레드는 코어 3항목 중 평균 2.7개만 다룬다 (256/288, 89%).**
> `voice.threads.notes` 에 대응하는 구어체 문장이 없는 항목은 **일부러 뺀다**
> (`NOTE_MATCH_MIN`). 예전엔 가장 가까운 note 를 억지로 붙여서, 핵심이 '받은 다음 어디에 쓰나요'인데
> 쓰레드는 '방송사가 직접 여는 브랜드어워즈'를 말했다. 어긋난 걸 말하느니 빼는 게 낫다.
> 완전히 맞추려면 `data/products.js` 의 `threads.notes` 에 빠진 항목을 **승인받아** 채워야 한다.

---

## 8-5. 1단계 프로필 세팅 (2026-08-03)

인스타그램 계정 프로필 초안을 만든다. 요청자가 정한 순서 그대로다.
① 유형(+어워즈형은 브랜드) 선택 → ② 이름·이미지·소개 무작위 제작 → ③ litt.ly 링크.

### 레퍼런스에서 뽑은 구조 (요청자가 준 인스타 계정 5개)

| 유형 | 계정 | 구조 |
|---|---|---|
| 브랜드 어워즈형 | @kbsnawards_official · @kbsnbrand_production · @kcstawards_official | 이름 「브랜드명」 또는 「짧은이름 + 부제」 · 소개 『선언형 슬로건』 → `。` → 🏆 행사명 → ▫️ 사실 → 📩 문의 → 🔗 링크 |
| 마케터형 | @thecontents_lab · @soosangmarket | 이름 「이름 + 분야•분야」 · 소개 정체성 한 줄 → 공유하는 것 → 📨 연락처 → 🔗 링크 |

- **`。` 은 오타가 아니다.** 인스타가 빈 줄을 먹어버려서 여백 대신 넣는 글자다. 레퍼런스 3곳이 같은 방식을 쓴다.
- **litt.ly 링크는 소개 맨 마지막 줄**이다(요청자 지정). 인스타 링크 칸이 아니라 소개 안에 넣는다.
- 인스타 실제 제한을 지킨다 — 이름 30자, 소개 150자 (`LIMITS`).

### 어워즈형은 승인된 상품 4종에 붙는다

`data/products.js` 의 `PRODUCTS` 에서 이름과 사실을 가져온다.
**행사명을 지어내지 않는다** — 처음엔 무작위로 만들었는데 사실성 원칙에 걸린다.

litt.ly 주소는 브랜드마다 다르다 (`AWARD_SLUG`).

| 브랜드 | 주소 | 출처 |
|---|---|---|
| KBS N 브랜드어워즈 | `kbsn_awards` | 레퍼런스 계정 실제 주소 |
| KCST | `_kcst` | 레퍼런스 계정 실제 주소 |
| 중소기업 AI TV CF | `kbsn_ai` | 레퍼런스 계정 실제 주소 |
| 포브스 브랜드어워즈 | `forbes_awards` | ⚠️ **확인 안 된 추정값** |

### 이미지 프롬프트 (2026-08-11 전면 교체)

요청자 지적 두 가지를 고쳤다. **월계수만 나온다**, **마케터형이 1차원적이다.**

#### ⚠️ 어워즈형에는 모드가 둘이다 — 로고를 첨부하는지로 갈린다

| | 심볼을 정하는 주체 | 재료 | 주기 |
|---|---|---|---|
| 로고 없음 | 프롬프트가 지어낸다 | `AWARD_EMBLEM` (심볼 9·테두리 8·색 6·마감 5) | 360 |
| 로고 있음 | **첨부한 로고가 곧 심볼** | `AWARD_LOGO_LOOK` (연출 7·배경 6·조명 5·악센트 4) | 420 |

예전에는 모드 구분 없이 프롬프트가 늘 `a laurel wreath award emblem …` 으로 **시작**했다.
로고를 첨부해도 모델은 글로 적힌 심볼을 우선해서 월계수를 그렸다. 심볼 7개 중 3개가
월계수 계열이었던 것도 겹쳤다(테두리에도 laurel 이 또 있었다).

- ⚠️ **로고 모드에 심볼 이름을 되살리지 말 것.** `laurel` `wreath` `trophy` `crest` 같은 단어가
  하나라도 들어가면 모델이 그걸 붙잡고 로고를 무시한다. 실제로 연출 문구에 남은 `wreath`
  한 단어가 프롬프트 7개 중 1개를 오염시켰다.
- ⚠️ **로고 모드에 `no text, no letters` 를 쓰지 말 것.** 로고 안의 글자까지 지워
  브랜드를 알아볼 수 없게 만든다. 대신 `NO_EXTRA_TEXT` 로 **덧붙이는 것만** 막는다.
- 로고 모드는 배치를 도면처럼 못박지 않는다. "자유롭게 구성하되 로고는 온전히"까지만 말한다 —
  요청자 요구가 "너무 확실한 프롬프트 말고 자유롭게 제작 가능한" 것이다.

> ⚠️ **로고 첨부 UI 는 화면에서 뺐다** (요청자 지시 2026-08-11).
> 미리보기·내려받기·「로고 첨부해서 만들기」 토글이 있었는데 정작 로고 파일이 하나도 없어서
> 「로고 준비 중」과 비활성 버튼만 자리를 차지했다. 로고가 준비되면 다시 붙인다.
>
> **생성 쪽(`AWARD_LOGO_LOOK`, `buildProfile({withLogo:true})`)은 그대로 살아 있다** —
> 화면만 뗀 것이라 다시 붙일 때 프롬프트를 새로 짤 필요가 없다.
> `awardLogoSrc()` 와 `assets/logos/README.md` 도 남겨 뒀다.
> (`api/` 를 배포 안 하면서도 남겨 둔 것과 같은 판단이다.)

#### 마케터형은 사진이 아니라 기하학적 마크다

마크 8 · 색 7 · 재질 5 · 배경 4 (주기 280).

예전에는 "노트북 앞에서 웃는 마케터" 같은 **인물·책상 사진**이었다. 요청자 지적: AI 티가 난다.
실제 상위 마케터 계정의 아바타는 사람 사진이 아니라 **작게 줄여도 읽히는 추상 도형**이다.

⚠️ 여기에 인물·책상·노트북을 다시 넣지 말 것. 아바타는 40px 로도 뜨는데 사진은 그 크기에서
뭉개져서 계정을 구분하지 못한다.

#### ⚠️ 이미지 프롬프트도 `combo()` 를 쓰지 않는다

mixed-radix `combo()` 는 앞자리만 매번 바뀌고 뒷자리는 수십 번에 한 번 바뀐다.
「다시 뽑기」를 눌러도 색·마감이 그대로여서 **같은 그림으로 보였다.** 소개 문장에서 이미 같은
지적을 받았던 것과 똑같은 실패다(아래 「무작위 조합에서 세 번 헛디뎠다」 1번).

지금은 이름·소개·이미지 프롬프트가 **전부 `pick()` 기반**이고 `combo()` 는 삭제했다.
`pick()` 은 재료마다 인덱스가 매번 1씩 도므로 한 번 누를 때마다 **네 축이 전부** 바뀐다.
대신 주기가 재료 개수의 최소공배수로 정해지므로 **개수를 바꿀 때 주기가 짧아지지 않는지 확인할 것.**

4단계와 같은 원칙 — 영문, 정사각형 아바타 기준.
생성 API 연결은 **다음 작업**이다(요청자 지시). 지금은 프롬프트까지만 만든다.

#### 검증 (마지막 실행 기준 2026-08-11)

seed 400회 × 3종:

| | 결과 |
|---|---|
| 로고 모드 | 400종 고유 · 심볼 단어 누출 **0** · `no text` 누출 **0** · 첫 문장 앵커 400/400 |
| 로고 없음 | 360종(설계 주기) · 월계수 비중 11% (예전 57%) |
| 마케터형 | 280종(설계 주기) · 인물·책상 단어 누출 **0** |
| 공통 | 인접 seed 에서 **네 축 전부 변경** 399/399 |

명도 대비 전수 감사 실패 0건, 375px 가로 스크롤 0.

### ⚠️ 무작위 조합에서 세 번 헛디뎠다

1. **재료마다 같은 seed 로 뽑으면 조합이 맞물린다.** 80번 뽑아도 어워즈형 이름이 4종뿐이었다.
   보폭을 다르게 줘도 소용없다 — 재료 개수가 같으면 주기가 같이 돈다.
   한동안 이미지 프롬프트만 `combo()`(mixed-radix)로 모든 조합을 훑었지만, 그쪽에서도
   **뒷자리가 안 바뀐다는 같은 지적**을 받아 2026-08-11 에 `pick()` 으로 통일하고 combo 는 지웠다.
2. **소개 문장은 `combo()` 로 묶으면 안 된다.** 뒷자리로 밀린 재료가 수십 번에 한 번씩만 바뀌어서,
   요청자가 "연차 숫자만 바뀌고 똑같다"고 지적했다.
   소개는 **재료 개수를 서로소로 맞추고 각자 `pick(pool, seed)`** 로 돌린다.
   그래야 한 번 누를 때마다 모든 줄이 바뀐다.
3. **개수를 바꿀 때 서로 나누어떨어지는지 본다.** slogan 6 · contact 4 였을 때 주기가 12로 짧아져
   200번 뽑아도 소개가 12종뿐이었다. 7 · 5 · 4 로 맞춰 455종이 됐다.

### 화면에서 손댄 것 (2026-08-11)

- ⚠️ **하위 패널에 `<fieldset>` 을 쓰지 말 것.** 브랜드 고르기가 `<fieldset class="field">` 였는데
  `.field` 에 테두리 해제가 없어 브라우저 기본 테두리(2px groove)가 그대로 남아 **검은 네모 상자**로
  보였다(`.brief__form fieldset { border:0 }` 규칙은 2단계 전용이라 여기까지 오지 않는다).
  `.subpanel` 로 바꿔 왼쪽 강조선만 남겼다. 라디오 묶음의 접근성은
  `role="radiogroup"` + `aria-labelledby` 로 유지한다.
- litt.ly 칸의 「소개 맨 마지막 줄에 함께 들어갑니다」 안내를 뺐다 — 칸마다 두 줄씩 붙어
  번잡했다. **동작은 그대로다**(링크는 여전히 소개 마지막 줄에 들어간다).
  「litt.ly 만들기」 링크는 요청자 지시로 남긴다.
- ⚠️ **다시 그리는 것과 다시 묶는 것을 짝지을 것.** `remake()` 가 `bindType()` 을 통째로 다시 불러서,
  다시 그려지지도 않은 유형 라디오에 **리스너가 쌓였다** — 「다시 뽑기」를 세 번 누른 뒤 유형을
  바꾸면 토스트가 네 번 떴다. 지금은 `bindBrand()` 를 갈라 두고 remake 는 그것만 부른다.

### 길이 맞추기 — `fitBio()`

150자를 넘으면 **뒤에서부터 자르면 안 된다.** 링크 줄이 먼저 날아간다.
`keep` 이 아닌 줄(여백 `。`·부가 사실)부터 덜어낸다.

### 검증 (마지막 실행 기준)

어워즈 4브랜드 × 200 + 마케터 200 = **실패 0건**.
소개 가짓수 어워즈 455종 / 마케터 200종, 이미지 프롬프트 각 200/200 전부 고유.
「다시 뽑기」 인접 seed 에서 모든 줄이 30/30 변경. 링크는 1,000건 전부 마지막 줄.

---

## 8-1. STEP 5 — 보관함 (2026-08-10 완료)

`pages/library.js` + `lib/librarystore.js`. 저장은 **수동**이다.

### 왜 저장 버튼을 따로 뒀나

보관함 화면만 만들면 안 됐다. **`state.library` 는 store 와 sync 에 등록만 돼 있고 아무도
쓰지 않는 빈 배열이었다.** 게시물을 담는 동선 자체가 없었다.

담는 시점을 정하는 게 진짜 결정이었고, 요청자가 **수동 저장**을 골랐다. 4단계 아래
「보관함에 저장」을 누른 것만 들어간다. 자동으로 쌓으면 만들다 만 게시물까지 전부 남아
보관함이 쓰레기통이 된다.

### 게시물의 신원 = 상품 + 주제 (`postKeyOf`)

톤·템플릿·강조색을 바꿔 가며 다듬는 건 **같은 게시물을 고치는 것**이라 새 항목을 만들지 않는다.
같은 상품·주제가 이미 있으면 **덮어쓰기 전에 물어본다** — 말없이 덮으면 다른 기기에서 쓴 걸 날린다.

### 담는 것과 담지 않는 것

`POST_KEYS` 21개 — 글·문구·템플릿 설정 전부.

- ⚠️ **`profile` 은 담지 않는다.** 게시물이 아니라 **계정** 정보다. 옛 게시물을 불러왔다고
  프로필까지 과거로 되돌리면 안 된다.
- 이미지는 **첫 카드 썸네일 1장**(216×270 JPEG q0.72, 약 6KB)만 IndexedDB 에 남긴다.
  원본 6장은 게시물당 약 6MB 라 몇십 개만 쌓여도 브라우저 용량을 넘긴다 (요청자 결정).
- 썸네일은 **기기를 따라가지 않는다.** 목록(글)은 동기화되지만 그림은 이 기기에만 있다.
  다른 PC 에서는 「미리보기 없음」으로 뜬다 — 그래서 그림이 없어도 목록이 읽히게 만들었다.

### 불러올 때 이미지 기록을 다시 세운다 (`reconcileImages`)

`images` 는 '이 기기에 어떤 이미지가 있는지' 적어 둔 값인데 보관함은 기기 간에 동기화된다.
저장할 때의 값을 그대로 믿으면 **다른 PC 에서 있지도 않은 이미지가 있다고 표시된다.**
불러올 때 IndexedDB 를 실제로 확인해서 있는 것만 남긴다.

`imageKey(productId, conceptId, i)` 는 상품·템플릿 단위라 **게시물 단위가 아니다.**
그래서 게시물을 지워도 카드 원본은 건드리지 않는다 — 지우면 같은 상품의 다른 게시물에서 사라진다.

### 저장 실패를 삼키지 않는다

`setState()` 는 용량 초과를 조용히 넘긴다(화면 동작 유지가 우선이라 그렇게 뒀다).
그대로 두면 "저장했습니다"라고 해 놓고 새로고침하면 사라진다.
`store.js` 에 `lastPersistError()` 를 두고, `saveToLibrary()` 가 실패를 확인하면
**목록을 원래대로 되돌리고 사실대로 알린다.**

### `confirmModal()` (`components/modal.js`)

`modal()` 은 알림 전용(버튼 하나)이라 삭제·덮어쓰기에 못 쓴다.
`confirm()` 을 안 쓰는 이유는 버튼 이름을 못 바꾸기 때문이다 — 「삭제」와 「덮어쓰기」는
결과가 달라서 이름이 중요하다. 포커스 가두기·Esc·원래 자리로 포커스 복귀까지 넣었다.

### 화면 상태는 스토어에 넣지 않는다

검색어·상품 필터·정렬은 모듈 지역 변수다. 스토어에 넣으면 **기기 간 동기화까지 따라가서**
다른 PC 의 필터가 바뀐다. 검색은 주제·상품명뿐 아니라 **글 내용까지** 뒤진다(`haystack`).

목록만 다시 그리는 `repaint()` 를 따로 뒀다 — 검색창을 통째로 다시 그리면 커서가 날아간다.

---

## 8-7. 개인 키 방식 (2026-08-10, 최종 결정)

> ⚠️ **이게 현재 방식이다.** 아래 8-6(서버 프록시)은 코드만 남아 있고 **쓰이지 않는다.**

요청자 결정: **API 키는 각자 발급받아 각자 넣는다.** 공용 키를 서버에 두지 않는다.

- `lib/llm.js` · `lib/imagegen.js` 는 항상 직접 호출 경로(`lib/openai.js` · `lib/gemini.js`)를 쓴다.
- `app.js` 는 `detect()` 를 부르지 않는다 → 로컬에서 `/api/health` 404 도 더 이상 안 뜬다.
- `api/` 와 `lib/serverapi.js` 는 남겨 뒀다. 공용 키 방식으로 되돌리려면 `llm.js` 에서
  갈래를 다시 만들면 된다. **Vercel 에 `OPENAI_API_KEY` 를 넣지 않으면** 그 엔드포인트는
  503 만 돌려주므로 열려 있어도 돈이 나가지 않는다.

### 키 내장 — `config.local.js` (2026-08-11)

요청자 요구: **"api 를 내장되도록 세팅. OpenAI · Terra 모델로."**
화면에서 키를 매번 넣지 않게 하되, **키가 저장소에 들어가면 안 된다.**

> ⚠️ **이 저장소는 Public 이다.** 공개 저장소에 올라간 OpenAI 키는 봇이 수 분 안에 긁어가고
> 요금은 키 주인이 문다. 7절의 "키를 코드·저장소·문서에 절대 넣지 않는다" 는 그대로다.
> **키를 커밋하는 방식으로 되돌리지 말 것.**

그래서 `.gitignore` 된 로컬 파일에서 읽는다.

```
config.example.js   커밋됨 · 템플릿 · 값은 비어 있어야 한다
config.local.js     .gitignore 됨 · 실제 키가 여기 들어간다 · 이 PC 를 벗어나지 않는다
lib/localconfig.js  읽는 쪽
```

- ⚠️ **내장 키가 화면에서 넣은 키를 이긴다** (`lib/openai.js` 의 `getKey()`).
  반대로 두면 예전 키가 남아 있는 브라우저에서 내장 키가 안 먹는다.
- ⚠️ **내장이면 3단계의 AI 설정 바를 통째로 안 그린다** (요청자 지시 2026-08-11).
  쓰는 사람이 만질 것이 하나도 없다 — 제공자·모델은 고정이고 키는 파일에서 온다.
  그래서 `aibarHTML()` 안에 내장 키 분기를 두지 말 것. **닿지 않는다** — 호출하는 쪽이 막는다.
  ⚠️ 「AI가 쓰고 있습니다」 안내는 이 바가 아니라 `toast()` 와 `setAiBusy()` 의 스피너가 낸다.
  바를 지워도 그대로 보인다(요청자 요구). 바를 손대면서 그쪽까지 건드리지 말 것.
- 제공자·모델 **고정은 키 내장과 별개**다. 키 없이 고정만 걸 수도 있어서 그 잠금은 바에 남는다.
- ⚠️ **내장 키를 localStorage 에 쓰지 않는다.** 쓰면 파일을 지워도 브라우저에 남아
  "내장을 껐는데 아직 되네" 가 된다. 읽기 전용으로만 다룬다.
- ⚠️ **모델 고정은 `openai.js`·`gemini.js` 의 `getTextModel()` 안에 있어야 한다.**
  `generateText()` 가 llm.js 를 거치지 않고 자기 `getTextModel()` 을 직접 부른다.
  llm.js 에만 넣으면 화면에는 Terra 로 보이는데 실제 호출은 다른 모델로 나간다.
- ⚠️ `loadLocalConfig()` 는 **첫 렌더 전에** 끝나야 한다(`app.js`). `hasKey()` 가 동기라
  늦게 읽히면 「키를 넣어 주세요」가 한 번 그려졌다 바뀐다. `isServerMode()` 와 같은 이유다.
- 고정 값이 목록에 없으면 **무시한다.** 오타 하나로 글쓰기가 통째로 죽으면 안 된다.
- ⚠️ 파일이 없으면 콘솔에 `config.local.js` 404 가 한 줄 남는다. 고장이 아니라 판정 방식이다
  (`/api/health` · `assets/logos/*.png` 와 같은 성격).

파일이 없으면 예전처럼 화면에서 키를 입력받는다. **둘 다 정상 동작이다** — 개인 키 방식(8-7)은 그대로 살아 있다.

OpenAI·Terra 는 이미 `PROVIDERS[0]` · `TEXT_MODELS[0]` 이라 **기본값 자체가 그것이다.**
`config.local.js` 의 `textProvider`/`textModel` 은 브라우저에 남은 옛 선택을 덮어쓰는 용도다.

### 키 입력칸은 한 곳에서 만든다 (`components/keyfield.js`)

글귀(2단계)와 이미지(3단계)가 **같은 칸**을 쓴다. 한쪽만 고쳐서 화면마다 다르게 보이는 일이
실제로 있었기 때문에 합쳤다. 발급 주소도 이 파일의 `ISSUE` 한 곳에만 둔다.

키는 제공자별로 **하나**다(`bboggl.openai-key`). 어느 화면에서 넣든 반대쪽에서 바로 쓰인다.

### Terra 이상만 쓴다 — Luna 삭제

블로그 형식 지시가 9단계 구조 + 이미지 자리 6줄 + 캡션 6줄이라 지시 준수력이 필요하다.
저가 모델은 검수에 걸려 재시도가 늘면 가격 이점도 함께 사라진다.

되살리려면 **두 곳을 함께** 고쳐야 한다 — `lib/openai.js` 의 `TEXT_MODELS`,
`api/_shared.mjs` 의 `ALLOWED_TEXT_MODELS`.

`getTextModel()` · `getModel()` 은 **목록에 없는 값이 저장돼 있으면 기본값으로 되돌린다.**
예전에 고른 Luna 가 남아 있으면 없앤 모델을 계속 부르게 된다.

### ⚠️ 제공자 목록의 순서가 곧 기본값이다

`PROVIDERS[0]` 이 기본이다. **OpenAI 를 앞에 둔다** — 쓰기로 정한 모델이 Terra(OpenAI)인데
기본이 Gemini 면 키 발급 안내도 Gemini 로 떠서 **엉뚱한 키를 받아 오게 된다.**
실제로 그렇게 떴다. `lib/llm.js` 와 `lib/imagegen.js` 의 순서를 서로 맞출 것.

---

## 8-6. PART 2 — 서버 프록시 (2026-08-10) · 현재 미사용

> ⚠️ 8-7 결정으로 **쓰이지 않는다.** 되돌릴 때를 위해 코드와 설명을 남겨 둔다.
>
> 지우지 않은 이유: **비용이 0이다.** 앱이 부르지 않고, `OPENAI_API_KEY` 가 없으면
> 요청이 와도 벤더 호출 전에 503 으로 끊긴다. 배포도 하지 않으므로 켜질 일 자체가 없다.

키를 브라우저에서 서버로 옮겼다. 배포 절차는 `DEPLOY.md` 에 있다.

### 두 모드를 모두 지원한다

`python -m http.server` 로 쓰던 방식이 죽으면 안 된다. 그래서 시작할 때 `/api/health` 를
한 번 찔러 보고 정한다 (`lib/serverapi.js` 의 `detect()`).

| | 서버 모드 (배포본) | 로컬 모드 |
|---|---|---|
| 판정 | `/api/health` 가 `{ok:true}` | 404 |
| 키 | Vercel 환경 변수 | 각자 localStorage |
| 키 입력칸 | 감춘다 | 보인다 |
| 관문 | 승인된 계정 로그인 | 없음 |

⚠️ **로컬에서는 콘솔에 `/api/health` 404 가 한 줄 남는다.** 고장이 아니라 판정 방식이다.

⚠️ `isServerMode()` 는 **동기**로 유지할 것. `hasKey()` 가 화면 곳곳에서 조건문으로 쓰여서
비동기로 바꾸면 그 자리들이 전부 깨진다. `app.js` 가 첫 렌더 전에 `detect()` 를 await 한다.

### 빌드 도구는 여전히 없다

`api/*.mjs` 로 ESM 을 쓴다. **package.json 을 만들지 않기 위해서다** — 확장자만으로 ESM 이 된다.
의존성은 하나도 없고 Node 18+ 내장 `fetch` 만 쓴다. `npm install` 이 필요 없다.

⚠️ `api/` 안에서 `_` 로 시작하는 파일은 Vercel 이 엔드포인트로 만들지 않는다.
공용 코드는 반드시 `_shared.mjs` 처럼 `_` 로 시작할 것.

### 서버가 막는 것

화면에서 막는 것으로는 부족하다. **서버 주소는 그냥 부르면 된다.**

- **승인된 계정만** (`requireApprovedUser`). 호출자의 토큰을 Supabase 에 되물어 확인한다.
  ⚠️ 그래서 **`service_role` 키가 필요 없다** — users 테이블 RLS 의 '자기 행 읽기'로 status 를 본다.
  만능 키를 서버에 두지 않는 편이 사고 시 피해가 훨씬 작다.
  ⚠️ 결과를 캐시하지 않는다. 승인을 취소하면 다음 요청부터 바로 막혀야 한다.
- **모델 허용 목록** (`ALLOWED_TEXT_MODELS`). 없으면 요청 본문에 비싼 모델을 적어 넣을 수 있고
  그 비용은 우리가 낸다. 허용 밖이면 조용히 기본값으로 바꾸지 않고 **거절한다** —
  다른 모델이 쓰인 걸 모르면 안 된다.
- **프롬프트 24,000자 · 출력 6,000토큰 상한.** 클라이언트 값을 그대로 믿지 않는다.
  요청 본문은 누구나 고칠 수 있다.
- 벤더 오류 메시지를 그대로 흘리지 않는다 — 키가 섞여 나올 수 있다.

### 이미지는 JSON 이 아니라 PNG 바이트로 돌려준다

base64 는 용량이 33% 늘어난다. 서버리스 응답 상한(약 4.5MB)에 1024x1536 고품질 PNG 를
base64 로 부풀리면 걸릴 수 있다. 바이트로 보내면 그만큼 여유가 생긴다.
그래도 넘치면 502 대신 413 으로 이유를 알려준다.

### 남은 것 — 계정별 하루 사용 상한

막아 둔 것은 '누가'와 '한 번에 얼마나'다. **'하루에 몇 번'은 아직 없다.**
인원이 늘면 Supabase 에 사용량 테이블을 두고 계정별 상한을 건다.
그 전까지는 OpenAI 대시보드의 월 사용 한도로 손실 상한을 정해 둔다.

---

## 9. 검증 방법

브라우저 콘솔(`javascript_tool`)에서 돌린다. 매 단계 끝에 확인한다.

### 명도 대비 전수 감사 (실패 0건이어야 한다)

```js
(()=>{
const lum=c=>{const [r,g,b]=c.match(/\d+\.?\d*/g).slice(0,3).map(v=>{v/=255;return v<=0.03928?v/12.92:Math.pow((v+0.055)/1.055,2.4)});return 0.2126*r+0.7152*g+0.0722*b};
const bgOf=el=>{let n=el;while(n&&n!==document.documentElement){const b=getComputedStyle(n).backgroundColor;if(b&&!/rgba\(0, 0, 0, 0\)|transparent/.test(b))return b;n=n.parentElement}return 'rgb(255,255,255)'};
const out=[];
document.querySelectorAll('body *').forEach(el=>{
 const t=[...el.childNodes].filter(n=>n.nodeType===3&&n.textContent.trim()).map(n=>n.textContent.trim()).join('');
 if(!t)return;const cs=getComputedStyle(el);
 if(cs.visibility==='hidden'||cs.display==='none'||el.closest('.sr-only,.skip-link')||el.hidden)return;
 const size=parseFloat(cs.fontSize),w=parseInt(cs.fontWeight)||400;
 const large=size>=24||(size>=18.66&&w>=700);
 const L1=lum(cs.color),L2=lum(bgOf(el));
 const ratio=(Math.max(L1,L2)+0.05)/(Math.min(L1,L2)+0.05);
 if(ratio<(large?3:4.5))out.push(t.slice(0,20)+'|'+(el.className||el.tagName)+'|'+ratio.toFixed(2));});
return JSON.stringify({fails:out.length,list:out},null,1)})()
```

### 글귀 전수 검사 (금지 표현·길이·조사 오류)

상품 4 × 톤 4 × variant × 채널 3 조합을 전부 돌려 위반 0건인지 본다.
`generate()`와 `findBanned()`를 import해서 이중 루프로 검사한다. 과거 실행 기준 위반 0건이었다.

### 가로 스크롤

`resize_window`로 375px에서 `document.documentElement.scrollWidth === clientWidth`인지 확인한다.

### 카드 렌더 전수 검사 (4단계)

상품 4 × 톤 4 × variant 2 × 템플릿 3 × 카드 6 을 `renderCard()` 로 전부 그려 예외 0건인지 본다.
그다음 **글자가 캔버스 밖으로 잘리지 않는지**를 픽셀로 확인한다.

빈 문구로 한 번(배경만), 실제 문구로 한 번 그려 두 이미지를 뺀 차이가 곧 글자 잉크다.
그 잉크의 bounding box 가 가장자리에 닿으면 잘린 것이다.

마지막 실행 기준 (1080×1350):

| 템플릿 | top | bottom | left | right |
|---|---|---|---|---|
| magazine | 93 | 1268 | 87 | 980 |
| card | 164 | 1268 | 88 | 991 |
| note | 109 | 479 | 87 | 1012 |

`fit()`·`fitPair()`·`fitRich()` 의 `sizes`·`maxLines` 를 건드렸다면 반드시 다시 돌린다.

### 색 대비 계산 (캔버스 안은 DOM 감사가 못 잡는다)

canvas 로 그린 글자는 위의 DOM 전수 감사에 걸리지 않는다. 색을 바꿨다면 직접 계산할 것.

- 매거진형 강조색 후보: 하단 스크림 최악값 `#1A1A1A` 기준 4.5:1 이상
- 카드형 흰 글씨: `layout.brand` / `layout.surface` 기준 4.5:1 이상 (현재 4.7:1)
- 노트형: 제목 16.85 / 본문 9.42 / 부제 5.71 (종이 `#F2F2F0` 기준)

---

## 10. 버전 관리

- 원격: `https://github.com/yuki010726-eng/brand-sns-studio` (**Private**)
- 기기를 옮기기 전에 반드시 커밋·푸시하고, 시작할 때 `git pull` 한다.
- 자동 동기화가 아니다. 안 하면 양쪽이 갈라진다.
- 브라우저에 저장된 값(글귀·이미지·API 키)은 기기 간에 따라가지 않는다. README 참고.


---

## 11. 이메일 로그인 · 관리자 승인 · 기기 간 동기화 (2026-08-03)

요청자 요구: **집 PC 에서 쓴 걸 회사 PC 에서 이어서 보고 싶다.**
여태 코드만 git 으로 오갔고 작업 내용은 브라우저에 남아 기기를 따라가지 않았다.

설치 절차는 `SUPABASE_SETUP.md` 에 있다 — SQL, 이메일 로그인, 관리자 승인 설정이 적혀 있다.

### 왜 Supabase 인가

이 프로젝트에는 node/npm 이 없다. Supabase 는 **CDN 에서 ES 모듈로 바로 가져올 수 있어서**
빌드 도구 없이 지금 구조에 그대로 붙는다. Firebase 도 후보였지만 요청자가 Supabase 를 골랐다.

### 인증과 관리자 승인

현재 로그인은 보호 화면의 접근 관문이다. Supabase Auth는 이메일/비밀번호를 검증하고,
`public.users.status`는 관리자가 서비스 접근을 승인했는지 판단한다.

- `pending`, `approved`, `rejected`는 관리자 승인 상태에만 사용한다.
- `email_not_confirmed`, 프로필 조회 실패, RLS 오류를 `pending`으로 바꾸지 않는다.
- `SUPABASE.url` / `anonKey`가 없거나 SDK 로드가 실패하면 로그인할 수 없다고 명확히 표시한다.
- `initAuth()`가 기존 세션을 복구한 다음 라우터가 승인 상태에 따라 화면을 연다.

### 동기화 범위 — 글·문구만

`sync.js` 의 `SYNCED_KEYS` 22개. 요청자 결정으로 1차에서는 이미지를 뺐다.

> ⚠️ **`images` 를 SYNCED_KEYS 에 넣지 말 것.** 그건 '이 기기에 어떤 이미지가 있는지' 적어 둔 값이라,
> 실제 Blob 이 없는 기기로 옮기면 있지도 않은 이미지가 있다고 표시된다.
> 이미지까지 옮기려면 Supabase Storage 를 붙여야 한다 — 별도 작업이다.
> (게시물 하나에 6장 × 약 1MB 라 무료 용량을 금방 쓴다.)

API 키도 동기화하지 않는다. 기기마다 따로 넣는다.

### 되먹임을 막는 장치

`setState()` 가 바뀔 때마다 `scheduleSync()` 를 부른다. 그런데 서버에서 받아온 내용을 얹을 때도
`setState()` 를 쓰므로, 그대로 두면 **받자마자 도로 올리는** 순환이 생긴다.
`applyRemote()` 가 `applying` 플래그를 세우고 `scheduleSync()` 가 그때는 아무 일도 하지 않는다.

불러오기도 **로그인 상태가 잡힐 때 한 번만** 한다. `initAuth()` 안에서도 `onAuth` 가 울리므로
`app.js` 의 구독 한 곳에만 두고 `restored` 로 막는다 — 안 그러면 두 번 받는다.

### 충돌 — 마지막 저장이 이긴다

두 기기에서 동시에 고치는 일은 드물고, 자동 병합은 글을 뒤섞어 더 나쁜 결과를 만든다.
대신 `hasRemoteChange()` 로 다른 기기가 먼저 고쳤는지 확인할 수 있게 열어 뒀다.

### 남은 것

- **실사용하려면 배포가 필요하다.** localhost 만으로는 다른 PC 에서 접속할 주소가 없다.
- 관리자 승인은 `public.users.status`만 사용한다. Supabase Auth의 이메일 Confirm과 섞지 않는다.
  Confirm email을 사용한다면 미확인 상태는 별도 오류로 안내해야 한다.
- 이미지 동기화(Storage)
